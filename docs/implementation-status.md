# Implementation Status

Single source of truth for current wiring between accepted design and running code. Other docs (core-model, AGENTS.md, ADRs) link here instead of restating implementation status inline. This document does not set future delivery sequence, issue status, or roadmap checkpoints; those belong only in [GitHub #459](https://github.com/INONONO66/openomni/issues/459).

**Legend**: ✅ implemented and wired · 🔌 dormant — built and tested, zero production callers · 🚧 partial · 📋 designed, not implemented · Last verified: 2026-08-05 (claims re-checked by code inspection at HEAD).

> Project rule of thumb behind this file: an engine without a consumer does not count as shipped. "Built" and "wired" are tracked separately because the recurring failure mode here is completed schemas/stores that nothing calls.

## Scope

The tables below report what is wired, dormant, partial, or designed in code. Target semantics and package/role contracts live in [Core Model](core-model.md), [Kernel Contract](kernel-contract.md), and [Architecture](architecture.md); the live delivery plan remains [#459](https://github.com/INONONO66/openomni/issues/459).

## Three-layer message flow (ADR-009)

| Component | Status | Code | Notes |
| --- | --- | --- | --- |
| Server channel adapters (Discord/Telegram/GitHub/WS) | ✅ | `apps/server/src/channel/` | Raw → `InboundMessage` |
| IngressEngine (kernel route resolve → decision append → publish → project → execute) | ✅ | `packages/openomni/src/ingress/engine.ts`, `packages/openomni/src/ingress/routing-runtime.ts`, `packages/openomni/src/ingress/routing-execution.ts` | Direct and internal inbound converge on one schema-validation and `Bus.publish` invocation boundary. Blacklist → wait correlation → channel ceiling → actor identity → surface default precedence is resolved before execution; wait correlation is read-only (#215) — ambiguity is recorded solely by the published RoutingDecision and the typed `route_ambiguous` rejection, never by mutating candidates. A matched durable wait whose owner has no ingress delivery path, or whose requested action is not allowed, resolves to the fail-closed `wait_correlation`/`block` outcome instead of falling through to surface routing. #510 C3: every routing decision — accepted AND terminal (drop/block/deny/ambiguous) — awaits a durable `route.decided` fact on the single-fact channel-scoped stream `route:<surface>:<workspace ?? "">:<channel ?? "">:<inboundEventId>` (`Ledger.append`, expectedHead 0; the scope is part of the stream identity because normalizer-minted inbound ids are only unique within a channel — Telegram per-chat counters, GitHub per-issue fallback ids) before the routed action executes or the typed rejection returns; the Bus RoutingDecision publish is the observe-only projection strictly after the append. A redelivered inbound (same scoped id — the stream already holds its fact) passes an EQUIVALENCE GATE: the recorded `route.decided` fact is read back (`Ledger.headFact`) and compared with the fresh decision on stage/outcome/target/sessionId/runId/pendingInteractionId. Equivalent — execution proceeds with the FRESH resolution and fresh decision (identical anyway; recorded payloads never mix with fresh wait/target state): an accepted route re-executes idempotently (the wait fold's `already_resolved` short-circuit re-delivers to the owner, preserving the #519 attach/deliver crash-window recovery), a terminal decision repeats the same typed rejection. Divergent — typed `route_replay_divergent`, fail closed: no action, no second fact, nothing published. Append infrastructure failure fails closed as typed `route_record_failed`. |
| `ActorResolver` / `ActorIdentity` / `ActorEndpoint` / `ActorRegistry` | ✅ | `packages/protocol/src/actor/`, `packages/session/src/actor/`, `packages/openomni/src/ingress/actor-resolver.ts` | Canonical actor identity + endpoint schemas, SQLite-backed registry, and ingress resolution for registered `(channel, workspace, externalId)` endpoints are wired, where `channel` is the platform/surface identifier. Registered actors project canonical `trustTier`; unregistered endpoints are sanitized back to legacy `id`/`role` and may receive a channel default tier when the matching ChannelGrant defines one |
| DispatchRuntime (policy authorize → handler routing) | ✅ | `packages/openomni/src/dispatch/runtime.ts` | Actions: `resident.ask`, `worker.spawn/send/resume/cancel`, `schedule.create/cancel` |
| WorkerGrant evaluation | ✅ | `packages/openomni/src/dispatch/policy.ts`, `packages/openomni/src/dispatch/effective-authority.ts` | WorkerGrant remains the scoped worker-egress grant store and is projected into effectiveAuthority as the `session_ownership_grant` axis for worker-scoped dispatch |
| Blacklist | ✅ | `packages/protocol/src/actor/`, `packages/session/src/blacklist/`, `packages/openomni/src/ingress/middleware/ingress-authority.ts`, `packages/openomni/src/dispatch/policy.ts` | Actor/endpoint/channel/pattern blacklist entries are schema-backed, persisted in SQLite, and checked as an absolute deny gate on both inbound ingress and dispatch authorization. Dispatch default authorization records blacklist outcomes as the effectiveAuthority `blacklist` axis |
| ChannelGrant (+ inbound treatment ceiling) | ✅ | `packages/protocol/src/actor/`, `packages/session/src/channel-grant/`, `packages/openomni/src/ingress/middleware/ingress-authority.ts` | Surface/workspace/channel grants are schema-backed, persisted in SQLite, and enforced fail-closed on inbound ingress after Blacklist. `trusted_channel` allows full access, `broadcast_channel` allows inbound as `evidence_only` and propagates that treatment through resolved event metadata, inbound policy context, and ingress audit projection; `blocked_channel` drops before resident execution. When `defaultTier` is present it supplies canonical TrustTier for unregistered actors before authority evaluation. Dispatch-local authorization records the ChannelGrant axis as `not_required` when no dispatch-local route exists |
| `TrustTier` enum + evaluation | ✅ | `packages/protocol/src/actor/`, `packages/protocol/src/dispatch/index.ts`, `packages/openomni/src/ingress/middleware/ingress-authority.ts`, `packages/openomni/src/dispatch/actor.ts` | `TrustTier` is canonical protocol enum data, `Dispatch.ActorContext.trustTier` is enum-bound, and ingress authority evaluates canonical tiers before legacy role/trusted fields. `owner`, `co_owner`, and `manager` may create top-level inbound work; `collaborator` and `observer` may pass only through `evidence_only` resident inbound treatment; `assigned_worker` remains denied for top-level creation unless an effectiveAuthority path grants a narrower scoped action |
| `effectiveAuthority` (5-axis intersection) | ✅ | `packages/openomni/src/dispatch/effective-authority.ts`, `packages/openomni/src/dispatch/policy.ts` | Dispatch default authorization now records explicit effectiveAuthority axis facts for blacklist, channel grant, personal/channel-default grant, session ownership grant, and pending interaction scope. Existing WorkerGrant evaluation is projected as the session ownership axis for worker-scoped dispatch; absent actor identity fails closed as a missing personal/channel-default grant. ChannelGrant and PendingInteraction routing remain owned by their dedicated ingress/PI rows; dispatch marks those axes `not_required` when no dispatch-local route exists |
| `Wait` primitive (single representation for message-correlated waits; PendingAsk/PendingInteraction writes cut over) | ✅ | `packages/protocol/src/wait/`, `packages/session/src/wait/`, `packages/session/src/storage/sqlite-wait-adapter.ts`, `packages/session/migration/0012_wait/migration.sql`, `packages/openomni/src/wait/`, `apps/server/src/bootstrap/recovery.ts` | #215 shipped: protocol owns the zod Record (owner-correct `ownerRef: workItem\|session`, single correlation owner, `origin_message_id` UNIQUE = exactly one Wait per awaited message) and the pure fold (attach/quorum/expire/cancel plus `recordDeliveryReceipt`, which re-keys `correlation.replyToMessageId` to the platform message id on open waits only; duplicate replyKey and repeat responders never advance quorum; ambiguity never guesses; time is an input; typed rejection codes). Session owns the `wait` table behind a required sub-adapter that fails closed when absent, with revision CAS transitions. The kernel owns THE one correlation lookup (`wait/correlation.ts`: wait table first, frozen legacy rows via read-only upcast), the one sender-matcher core with per-phase evidence extensions, `WaitService` (open on awaited delivery only), and the boot expiry sweep. Not yet absorbed: WorkItem-blocker and WorkerRun `waiting_input` representations still write their own state — remaining #215/#216/#217 scope |
| `PendingAsk` (frozen legacy, read-only) | ✅ | `packages/protocol/src/communication/`, `packages/session/src/pending-ask/`, `packages/openomni/src/wait/correlation.ts`, `packages/openomni/src/wait/upcast.ts`, `packages/openomni/src/dispatch/handlers/resident.ts` | #215 retired the write path: the synchronous in-process `resident.ask` records `wait.sync_ask` audit events only and writes no PendingAsk (and no Wait) row. #510 D2a froze the writer after its final accepted append (production writers were already zero): every `PendingAskStore` write method (`create`/`answer`/`markAmbiguous`/`cancel`/`expire`) throws the typed `Communication.PendingAsk.FrozenError` (`data.code === "pending_ask_frozen"`) and persists nothing, while reads keep serving the immutable historical rows exactly as persisted through the upcast-on-read view inside the single kernel correlation lookup (legacy tier, after the wait table). The frozen `pending_ask` table is enumerated by the archive manifest (`script/generate-ledger-archive-manifest.ts`: source schema version from `_migrations`, row count, id-range identity, sha256 range hash over canonical row JSON in id order; artifact convention `ledger-archive-manifest.json` beside the database file) and the manifest + deterministic upcast-on-read + never-materialized-as-active invariants are pinned by `script/conformance/p2-ledger-baseline.test.ts`. Table deletion still waits for the #510 replay-coverage proof |
| `PendingInteraction` (frozen legacy; status transitions only) | ✅ | `packages/protocol/src/communication/`, `packages/session/src/pending-interaction/`, `packages/session/src/storage/sqlite-pending-interaction-adapter.ts`, `packages/session/migration/0009_pending_interaction/migration.sql`, `packages/openomni/src/wait/correlation.ts`, `packages/openomni/src/wait/upcast.ts`, `packages/openomni/src/ingress/routing-runtime.ts`, `packages/openomni/src/ingress/routing-execution.ts`, `packages/openomni/src/dispatch/pending-interaction-routing.ts`, `apps/server/src/bootstrap/recovery.ts` | #215 retired row creation: no production path writes new PendingInteraction rows. Persisted rows keep answering replies through the single kernel correlation lookup (reply-message > thread > token > conversation/scoped fallback precedence, PI and PendingAsk candidates combined per level, fail-closed on same-level ambiguity) via the read-only upcast view. The dispatch reply path still normalizes plain-text replies to `report_result`, binds assigned senders before authority elevation, strips externally supplied WorkItem selection, pins the match to its owning WorkerRun, and applies the two surviving legacy status transitions (`resolve`/`markFollowUp`) on matched rows; boot recovery still expires stale open/follow-up rows. Those remaining legacy writes disappear when #510 archives the store |
| `executorKind` on WorkerRun (`internal_chat_agent` / `connector_endpoint` / `external_api` / `a2a` / `human_channel`) | ✅ | `packages/protocol/src/worker-run/`, `packages/session/src/worker-run/`, `packages/session/migration/0005_worker_run_executor_kind/migration.sql` | WorkerRun records carry durable coarse executor metadata; `WorkerRun.create()` defaults current in-process Worker runs to `internal_chat_agent`. Installed apps use `connector_endpoint` only as ledger metadata. Dispatch selection uses connector endpoint identity (`endpointId` / `connectorInstallationId`) instead of `executorKind` |
| Connector Endpoint apps (Claude Code / Codex / OpenCode as installed apps) | 🚧 | `packages/protocol/src/dispatch/index.ts`, `packages/protocol/src/app-connector/`, `packages/openomni/src/dispatch/handlers/worker.ts`, `packages/openomni/src/dispatch/handlers/connector-endpoint-worker.ts`, `packages/openomni/src/dispatch/handlers/connector-completion-projector.ts`, `packages/openomni/src/work-item/`, `apps/server/src/connector/`, `apps/server/src/bootstrap/dispatch-owners.ts` | Dispatch targets expose `endpointId` / `connectorInstallationId` and reject stale executor selectors. `worker.spawn` resolves enabled, consented AppConnector installations into the injected server connector driver owner, creates WorkItem / WorkerRun ledger state with `executorKind: connector_endpoint`, records connector telemetry as evidence, then projects explicit criterion facts through the same Worker-origin admission service used by internal Workers. OpenOmni owns dispatch/evidence/admission projection; server owns the process driver, log/question-bridge plumbing, and bootstrap credential injection. The former server connector definitions, discovery, and registry modules were deleted in #473; discover/register/consent as a runtime lifecycle remains pending as tracked in the next row. Connector process dispatch fails closed without a `workspaceRoot` worktree. Terminal connector results can enter via `worker.complete`; `report_result` PendingInteraction matches route there instead of generic chat. Provider-native hook/plugin materialization remains an implementation detail of the server connector driver |
| `AppConnector` schema (declarative connector endpoint ABI) | ✅ | `packages/protocol/src/app-connector/` | ADR-010 §3 schema shipped: detect/spawn/logs/question bridge/evidence/requires plus `profile.kind: "connector_endpoint"`, endpoint task defaults, provider driver capability (`install`, `submit`, observed events, evidence emitters), durable installation `endpointId`, consent records, and optional canonical workspace identity. The ABI schema is shipped; the server-owned example definitions file (`definitions.ts`) was deleted as dead code in the #453 sweep (zero production consumers) — new connectors re-add definitions when a real install path lands. OpenOmni no longer exports provider manifests |
| Install lifecycle (discover → register → consent → disable/uninstall → wire → smoke-verify; version-drift re-verification) | 📋 | `packages/protocol/src/app-connector/index.ts`, `packages/session/src/app-connector/`, `apps/server/src/bootstrap/dispatch-owners.ts` | Reconciled in the #453 dead-code sweep: `ServerConnectorRegistry` / `ServerConnectorDiscovery` / `ServerConnectorDefinitions` had zero production consumers (only their own tests; boot wires only `createConnectorEndpointDriver` from `connector/index`) and were deleted. What actually remains wired: the SQLite installation/consent storage (`packages/session/src/app-connector/`) and the boot-injected connector process driver. Discover/register/consent/smoke-verify as a runtime lifecycle is design (#216), to be rebuilt against the storage when the install path lands |
| App question bridge (permission/clarification prompt → PI / `resident.ask`, suspend/resume) | 🚧 | `packages/openomni/src/dispatch/pending-interaction-routing.ts`, `apps/server/src/connector/question-bridge.ts`, `apps/server/src/connector/process-driver.ts`, `apps/server/src/bootstrap/dispatch-owners.ts` | Connector questions can route through structured PendingInteraction actions. Terminal `report_result` routes to `worker.complete`; `ask_clarification` routes to `resident.ask` or an Owner-facing PI. The current server-owned process driver can start a short-lived localhost bridge when the AppConnector declares a hook/stdio question bridge and the runtime owner provides a handler; durable app suspension/resumption around long-lived interactive questions remains pending |
| Native app log ingestion (raw → artifact, key events → journal; liveness for stall detection) | ✅ | `packages/protocol/src/app-connector/index.ts`, `packages/protocol/src/execution/index.ts`, `apps/server/src/connector/log.ts`, `apps/server/src/connector/log-path.ts`, `apps/server/src/connector/log-telemetry.ts`, `apps/server/src/connector/process-driver.ts`, `apps/server/src/connector/process.ts`, `packages/openomni/src/dispatch/handlers/connector-completion-projector.ts` | AppConnector `logs.path` is rendered for connector process runs, captured after process exit, credential-redacted, stored as an Artifact, returned on `Execution.Result.artifacts`, and recorded as WorkItem evidence. `completionReport.finalMessage: "log"` can use the captured log message as the connector final output. Structured `jsonl` / `stream_json` lines are projected into redacted `Execution.Result.logEvents` with artifact id, message, timestamp, sequence, and data, then recorded as WorkItem evidence for the execution journal. Structured log connectors can declare `tokenUsageField`, `tokenUsageMode`, and `toolCallField`; the runtime normalizes those fields onto log events, aggregates declared token usage onto `Execution.Result.usage`, and records token/tool-call evidence on the WorkItem. `spawn.stallTimeoutMs` liveness is driven by stdout/stderr stream activity for stream-output connectors and by rendered file/glob log mtime/size changes for file-backed connectors |
| Outbound external actions (`external.ask`, `a2a.ask`, `api.ask`) | 🚧 | `packages/protocol/src/dispatch/index.ts`, `packages/openomni/src/dispatch/handlers/outbound.ts`, `packages/openomni/src/dispatch/owners.ts`, `packages/openomni/src/dispatch/setup.ts` | Dispatch exposes canonical outbound action constants and built-in handlers for `external.ask`, `a2a.ask`, and `api.ask`. Handlers require an `external_actor` target, fail closed unless an outbound owner is injected, and pass the audited command plus endpoint, payload, correlation, timeout, wait, and abort signal to that owner. Concrete server/channel/API owner implementations and outbound delivery adapters remain pending |
| `device.*` world-control dispatch actions (driver in `apps/server/`, handler in registry) | 🚧 | `packages/protocol/src/dispatch/index.ts`, `packages/openomni/src/dispatch/handlers/device.ts`, `packages/openomni/src/dispatch/owners.ts`, `packages/openomni/src/dispatch/setup.ts` | Dispatch exposes `device.command` as the first world-control syscall. The built-in handler requires a `system` target with `target.id` or `target.name` as the device id, fails closed unless a device owner is injected, and passes the audited command plus device id, payload, timeout, wait, and abort signal to that owner. Concrete `apps/server` device drivers and broader `device.*` verbs remain pending |

## Evidence loop (design-philosophy §1/§3, ADR-010 §4)

| Component | Status | Code | Missing consumer |
| --- | --- | --- | --- |
| WorkItem schemas (`Info`, `Blocker`, `Evidence`, `VerificationGate`, `deriveStatus`) | ✅ | `packages/protocol/src/work-item/` | `VerificationGate` is a write-only schema slot today (zero writers); per #490 it stays readable legacy and gains no new writers. |
| `WorkItemStore` (CRUD + durable row-CAS substrate + non-completion lifecycle) | ✅ | `packages/session/src/work-item/`, `packages/session/src/storage/sqlite-work-item-adapter.ts` | `worker.spawn` requires at least one acceptance criterion and creates immutable stable criteria. The storage adapter permits create-only insertion and expected-head CAS updates through one shared row revision; it exposes no unconditional WorkItem upsert. The store owns CRUD, blockers, evidence, retry, failure/cancellation, and Owner outcomes. Generic update cannot mutate admission/lifecycle-owned fields. Public raw `complete()` is a typed `admission_required` refusal; OpenOmni alone owns product completion. |
| WorkItem schema deltas (`originSessionId`/`workSessionId` split, `workerRunId`+`executorKind`, `completionReport`, `maxAttempts`, `outcome`) | ✅ | `packages/protocol/src/work-item/` | ADR-011 schema shipped; runtime completion gating is tracked in the row below |
| One-ledger attempt model (`WorkItem.Attempt`, distinct execution identity, fingerprints, replay manifest) | 🚧 | `packages/protocol/src/work-item/attempt.ts`, `packages/session/src/work-item/lifecycle.ts`, `packages/openomni/src/dispatch/handlers/worker-work-item.ts` | #510 C2 shipped the attempt identity vocabulary and its live allocation path: `attemptId` (opaque, globally unique), `attemptSeq` (monotonic per WorkItem via the `work:<hash>` stream's serialized append, never reused), `retryOf` (nullable prior-attempt lineage, never equivalence), and content/environment fingerprints (structured coverage inputs + one canonical-JSON sha256 digest owner; declared best-effort inputs are absent-but-listed with a reason, never silently empty). `WorkItemStore.allocateAttempt` appends `work_item.attempt_allocated` (full identity payload) before the projection, wired at both kernel spawn sites so the attempt fact precedes the WorkerRun record and `attemptId` travels alongside `workerRunId`. **Dormant vocabulary (schema only, no lookup/replay implementation):** `WorkItem.CacheKey` — an explicit lookup from the content digest plus a declared deterministic environment subset, structurally never a row key (no attemptId, derived `key`); a future hit creates a NEW attempt recording `reusedFromAttemptId`. `WorkItem.ReplayKey` — replay-of-record only: binds an immutable archived range, environment digest, schema/upcast versions, and the nondeterminism-manifest digest; it carries no content fingerprint so it cannot serve as a cache key. `WorkItem.NondeterminismManifest` — the enumerated consumed-input categories; a category neither recorded nor declared absent-with-reason fails loudly at parse. **Dual-vocabulary note**: `WorkItem.Info.attempt` (the retry counter `work_item.retried` bumps; what retry-policy evaluates) and `Info.lastAttemptSeq` (the attempt-allocation watermark bound to `work_item.attempt_allocated` facts) are two coexisting counters that advance independently — a retry bumps `attempt` without allocating, an allocation bumps `lastAttemptSeq` without retrying; convergence on the attempt-fact vocabulary is D2b/WorkerRun-absorption scope. Current `WorkItem` and `WorkerRun` stores remain separate — WorkerRun freeze/absorption, cache-hit lookup, replay execution, and JSONL export are #510 phase D / #493. |
| Completion report terminal projection (claims → evidence refs) | ✅ | `packages/openomni/src/dispatch/handlers/worker-completion.ts`, `packages/openomni/src/work-item/completion-admission.ts` | The report remains the human-readable terminal projection and every referenced evidence ID must resolve, but it is not completion authority. Workers must also bind each criterion to durable WorkItem-local evidence; the kernel resolves verifier input from those records rather than trusting claimant-supplied input. Prose, process success, and an unbound artifact alone cannot synthesize a result. Invalid or missing facts remain blocked before terminal completion. |
| ReadBackCheck evidence records + store helper | ✅ | `packages/protocol/src/work-item/`, `packages/session/src/work-item/` | Shipped structured `ReadBackCheck` records for URL fetch, API query, and citation match observations plus `WorkItemStore.addReadBackEvidence()` to persist read-back results as verification evidence |
| Runtime read-back executors (re-fetch URL, re-query API, citation-in-source matching) | ✅ | `packages/openomni/src/evidence/` | Shipped explicit `ReadBackExecutor.execute()` / `record()` API for HTTP URL re-fetch, read-only HTTP API re-query, and citation-in-source matching; `record()` feeds runtime observations into `WorkItemStore.addReadBackEvidence()` |
| Deterministic verifier registry + replay conformance primitives | ✅ | `packages/openomni/src/evidence/verifier-registry*.ts`, `packages/openomni/src/evidence/verifier-{sandbox,conformance}*.ts`, `packages/openomni/src/evidence/verifier-registry-driver.ts`, `packages/openomni/src/dispatch/handlers/worker-completion.ts` | Worker and connector completion resolve recorded verifier input from criterion-bound durable evidence and invoke the deterministic registry to produce basis-bound `verified / refuted / inconclusive / asserted` facts or separate verification-error facts before admission. Executable verifiers may satisfy only a persisted criterion whose text is the exact predicate they checked; citation support is the explicit exception because its frozen classifier directly evaluates the persisted claim. The public verifier driver still exercises the closed sandbox, frozen symbolic-NLI classifier, canonicalization, replay keys/fingerprints/manifests, upcast/interleaving checks, and zero-live-effect substitution. #493 still owns archived projection/replay integration. |
| Windowed ledger-observed Stakes primitive + guarded completion/Voice seams | 🚧 | `packages/openomni/src/ledger/`, `packages/openomni/src/dispatch/handlers/worker-completion.ts` | The pure snapshot-first calculator and broker contracts remain as documented by the public ledger driver. WorkItem completion consumes an injected Stakes resolver seam: when a host supplies a kernel-computed snapshot, high unresolved assertions carry its reference into the fold and escalate. Absence or failure of that authority blocks rather than fabricating a fallback snapshot. #510 supplies the later single-writer durable ledger source. Jester/Voice remain unwired and owned by #458. |
| Automatic read-back invocation before completion admission | ✅ | `packages/openomni/src/dispatch/handlers/worker-completion.ts` | `worker.spawn` succeeded results may include `readBackRequests`; dispatch executes each request through `ReadBackExecutor.execute()`, then persists the returned check only through an attempt-and-basis-fenced `WorkItemStore.addReadBackEvidence()` write before admission. It attaches the resulting evidence IDs to report claims and resolves per-criterion verifier input from those durable records. Failed read-back, missing evidence, malformed detail, or a retry during execution blocks instead of completing. |
| Domain-neutral WorkItem completion admission | ✅ | `packages/protocol/src/work-item/`, `packages/openomni/src/work-item/completion-admission.ts`, `packages/openomni/src/dispatch/handlers/worker-completion.ts`, `packages/openomni/src/work-item/completion-admission-driver*.ts` | Current schemas keep criteria, claims, observations, criterion results, invalidations, verification errors, effects, admissions, and terminal receipts distinct. A pure durable+proposed fold evaluates current basis and graph references; a closure-based resolver acquires configured Policy/Stakes/result/Owner authority; the OpenOmni service appends the admission by WorkItem row CAS before terminal completion and resumes idempotently after restart. Internal and connector Workers are wired consumers. The exhaustive public origin projector covers Resident, Worker, external actor, replay, recovery, and identity-qualified SDK/internal sources without fallback. The public driver exercises known-bad, asserted/Stakes, all-origin, stale-basis, restart, and bypass-refusal scenarios; the legacy-archive upcast path was removed with the pre-deployment legacy read shims (no persisted legacy data exists). #510 later moves the same contract behind the single FULL ledger writer; it does not own a second admission authority. |
| Connector/channel-specific read-back request builders | 🚧 | `packages/protocol/src/work-item/`, `packages/protocol/src/app-connector/index.ts`, `apps/server/src/connector/read-back-builder.ts`, `apps/server/src/connector/process-driver.ts` | AppConnector completion metadata can now declare templated read-back request builders, and the connector process driver fail-closes missing/non-JSON builder sources while appending generated `readBackRequests` to completion envelopes before the existing completion gate runs. Channel adapter builders for outbound publish/send/API actions remain pending |
| Internal worker retry default + kernel-enforced exhaustion blocker | ✅ | `packages/session/src/work-item/`, `packages/openomni/src/dispatch/handlers/worker.ts` | Internal `worker.spawn` ledger items are tagged `executorKind: internal_chat_agent`, default to `maxAttempts: 3`, and `WorkItemStore.retry()` refuses exhausted items after recording a `waiting_input` escalation blocker. A successful retry clears the prior executor/run/session assignment; the next attempt must receive a fresh assignment through `WorkItemStore.assignExecution()`, so late results from the prior attempt cannot be rebound. Owner-visible notification / failed-item surfacing remains pending below |
| Owner-visible retry exhaustion ledger surface | ✅ | `apps/server/src/handler/conversation.ts` | Authenticated local WebSocket `show open tasks` includes failed WorkItems with an active retry-exhaustion Owner escalation blocker, including attempt/max-attempt context, so exhausted internal worker retries are no longer invisible |
| App manifest retry defaults + human reminder policy + non-local Owner notification | 📋 | — | ADR-011 — CLI apps should inherit per-application manifest defaults; humans are not retried, they need reminder policy under the social budget; exhausted failed items still need push-style Owner notification beyond the local ledger surface |
| Owner adoption signal storage primitive (`outcome`: adopted/corrected/redone/ignored) | ✅ | `packages/protocol/src/work-item/`, `packages/session/src/work-item/outcome.ts` | `WorkItem.Info.outcome` is schema-backed and `WorkItemStore.recordOutcome()` records Owner feedback on completed WorkItems while publishing `work_item.outcome_recorded` for downstream Governor consumers |
| Owner adoption collection surfaces + Governor calibration | 📋 | — | ADR-011 — no Owner-facing adopted/corrected/redone/ignored capture UI or dispatch command yet; Governor does not aggregate adoption outcomes or calibrate Resident evaluation leniency |
| Task ledger view ("show open tasks" — the OS's `ps`) | ✅ | `apps/server/src/handler/conversation.ts` | Authenticated local WebSocket chat command returns a capped pending/running/blocked WorkItem diagnostic plus retry-exhausted failed-item escalations from `WorkItemStore`; unauthenticated WebSocket and external-channel actor-scoped visibility remain future work because current `WorkItem` / `InboundMessage` schemas have no owner, trust-tier, or channel-grant field; web view also remains future work |
| Bus persistence (hash-chained event journal) | ✅ | `packages/session/src/bus-persistence/` | #510 D1: telemetry rides its own `synchronous=NORMAL` connection on the same database file (`SqliteStorageAdapter.telemetryDb`; `:memory:` degrades to the single connection) with microtask group-commit batching — a synchronous Bus burst is one `BEGIN IMMEDIATE` transaction, the chain tip is now read INSIDE that transaction (the legacy outside-the-transaction fork window is closed), and per-event fallback keeps one bad row a single lossy drop. Telemetry is observe-only and never a decision/authorization fact; writes can never join a decision-class transaction (separate connection; pinned by test). BusPersistence still runs in two process types (server bootstrap + worker entries) against the same file; cross-process serialization is WAL + immediate transactions + busy_timeout. |
| Clean-ledger append core (`Ledger.append(event, expectedHead)` CAS + boot tail verification) | 🚧 | `packages/protocol/src/ledger-append/`, `packages/session/src/ledger-core/`, `packages/session/src/wait/`, `packages/session/src/work-item/`, `packages/session/migration/0013_ledger/`, `packages/session/migration/0014_work_item_revision/`, `apps/server/src/server/routes.ts`, `script/conformance/p2-ledger-baseline.test.ts` | #510 Phase A. Serialized per-stream CAS append: raw prepared statements inside a sync `behavior "immediate"` transaction, `changes === 1` head receipt, write-path hash chain (tip read inside the same transaction, so it cannot fork like the legacy bus_event chain), typed `appended`/`cas_conflict` outcomes — conflict retry is the caller's decision. `verifyTail` walks stream tails only and returns `chain-break` facts without refusing boot (full-chain check stays the #226 restore drill). Drizzle 0.45.2 / drizzle-kit 0.31.10 are DDL-definition/generator-only; the hand-written `0013_ledger` migration applied by the existing BEGIN IMMEDIATE runner is authoritative. Phase B/C1 wire the first decision classes onto this core: every Wait transition (stream `wait:<id>`) and every WorkItem lifecycle write plus completion-admission verdict (stream `work:<hash>` — create/start/fail/cancel/assign/blockers/evidence/outcome/retry/update/remove, admission accept AND refuse facts before the terminal projection) appends its decision-class fact via `Ledger.append(event, expectedHead)` BEFORE the projection CAS in one sync immediate transaction — no record, no action; fact seq N == projected revision N; Bus publishes fire only after durable commit. Migration `0014` adds the WorkItem revision CAS column and shifts pre-cutover rows to old json revision + 1 (column and json payload in lockstep — the legacy scheme was 0-based, the fact-bound scheme is 1-based, so the transition count is preserved and the created-only remove guard keeps its meaning; caveat: completion admissions recorded pre-shift carry old-base heads, so an in-flight completion resume can fail its head check — boot recovery surfaces each such failure as a loud per-item Operational.Error instead of swallowing). Pre-cutover rows (revision >= 1, EMPTY owner stream) are adopted lazily on their first post-cutover transition via `Ledger.adoptStream(streamId, headRevision, genesisFact)` — a raw-prepared single-transaction insert of the genesis fact at seq === headRevision plus the head row at headRevision, legal only on an empty stream (typed `LedgerAdoptError` otherwise): WorkItems adopt with `work_item.adopted` at the row's own revision, Waits with `wait.adopted` carrying the record snapshot (old-DB open waits with prior transitions would otherwise brick every transition); the boot expiry sweep additionally isolates per-wait failures (one corrupt wait records an Operational.Error and the sweep — and boot — continue). Conflict mapping is per class: duplicate create = stream exists = typed duplicate; stale head = typed `revision_conflict`/`stale_revision`. Phase C2 adds the attempt identity class on the SAME `work:<hash>` stream: `work_item.attempt_allocated` carries the full Attempt identity (see the one-ledger attempt model row), attemptSeq is allocated by that stream's serialized append, and both kernel spawn sites append the attempt fact before the WorkerRun record exists. Phase C3 wires the routing and dispatch-authorization classes as single-fact streams (one decision per id, expectedHead 0 — "no record, no action" needs the durable append before the act, not contention): the ingress engine appends `route.decided` (the full validated RoutingDecisionPayload, accepted AND terminal outcomes) on the channel-scoped stream `route:<surface>:<workspace ?? "">:<channel ?? "">:<inboundEventId>` (scope is stream identity — normalizer ids are only unique within a channel) before wait/handler execution and before typed terminal rejections return, and the dispatch runtime appends `command.authorized` on `command:<dispatchId>` after a passing policy verdict and before the handler is invoked / `command.denied` before every denial result (policy block and pinned-interaction revalidation alike); the Bus RoutingDecision and Events.Authorized/Denied publishes are observe-only projections strictly after the append. Conflict mapping for single-fact streams is per class: a route conflict is an EQUIVALENCE-GATED replay — the recorded `route.decided` fact is read back through the append core's minimal read API (`Ledger.headFact`, raw prepared SELECT of the stream head) and compared with the fresh decision on stage/outcome/target/sessionId/runId/pendingInteractionId; equivalent redelivery proceeds with the FRESH resolution (accepted routes re-run idempotently via the wait fold's `already_resolved` short-circuit, terminal decisions repeat their original typed rejection; no second fact) — preserving the #519 redelivery crash-window recovery — while a divergent redelivery fails closed as typed `route_replay_divergent` (no action, no second fact, nothing published); a command conflict IS an anomaly (dispatchId is minted per submit) and fails closed as typed `CommandRecordError command_replayed`. An append failure or missing ledger sub-adapter fails closed for both (`route_record_failed` / `command_record_failed`) with no publish and no act. The owner-stream naming (`<class>:<id>`, opaque kernel-internal ids) is documented once in `LedgerAppend.StreamRegistry` (wait/work/route/command/effect); the effect class ships as dormant vocabulary only — `effect.intended`/`effect.confirmed`/`effect.failed` fact schemas on `effect:<effectId>` with the intent event id as the idempotency key, wired by #492. Conformance: `script/conformance/p2-ledger-baseline.test.ts` (append-before-act, failed append leaves nothing, lazy adoption at the row's own revision for WorkItems AND Waits, admission verdict facts, attempt-before-WorkerRun, monotonic never-reused attemptSeq, fail-loud nondeterminism manifest, tail verification over `wait:`/`work:` streams, route.decided-before-act, blocked-decision facts, equivalent-redelivery replay + divergent-redelivery `route_replay_divergent` + terminal-equivalent rejection repeat, command.authorized-before-handler, command.denied-on-denial, failing-append blocks ingress and dispatch). Phase D1 shipped the durability split under the recorded blueprint amendment (**Owner sign-off: blueprint amendment** — same-file cutover with a per-connection FULL/NORMAL split instead of a new `ledger.db` file, because append+projection are one fsync unit and SQLite WAL cross-database transactions are not atomic; the ATTACH-ro new-file shape remains the #226 restore drill): the primary storage connection (owner of ALL decision-class writes — ledger appends + projections share its transactions) runs `PRAGMA synchronous=FULL`, telemetry runs NORMAL/group-commit on its own same-file connection (see the Bus persistence row), boot runs `ledger.verifyTail()` in `apps/server/src/bootstrap/recovery.ts` and records each chain-break as an observe-only Operational event without refusing boot (Governor incident is a later phase), and shutdown drains the accepted telemetry batch then checkpoints the WAL (`wal_checkpoint(TRUNCATE)`) in `SqliteStorageAdapter.close()` — decision-class appends need no drain step because bun:sqlite sync transactions cannot be interleaved by the event loop. Phase D2a shipped the first legacy freeze→archive→upcast slice: the `PendingAskStore` writer is frozen behind the typed `pending_ask_frozen` error (see the PendingAsk row), the archive manifest artifact enumerates frozen tables with source schema version/range identity/integrity hash, and the named-consumer audit against the issue's migration list is settled as follows — **already fact-backed (no change needed)**: `packages/session/src/work-item/retry-policy.ts` reads only the WorkItem projection, whose every input is carried by `work:` facts (`work_item.retried` carries the attempt it evaluates, `work_item.blocker_added` carries the exhaustion blocker; head === revision — pinned by conformance) and the completion-resume half of `apps/server/src/bootstrap/recovery.ts` (`recoverRecordedWorkItemCompletions`) replays recorded admission facts through the C1 contract; **deferred to D2b (require WorkerRun/PendingInteraction live state, no fact coverage yet)**: `packages/session/src/bus-persistence/worker-run-history-query.ts` (per-run status timeline of `worker_run_state` — WorkerRun status transitions are not yet facts), the migration-0005 `executor_kind` readers (`sqlite-worker-run-state-adapter` → the `worker.complete` assignment validation in `packages/openomni/src/dispatch/handlers/worker.ts`, which also needs live run status/assignment), the interrupted-run sweep in boot recovery (`recoverInterruptedRuns` reads AND transitions live WorkerRun rows), and the PendingInteraction expiry sweep + surviving `resolve`/`markFollowUp` transitions. Phase D3 shipped the `/admin/ledger/*` authenticated READ-ONLY inspection surface (`apps/server/src/server/routes.ts`): bearer auth per the existing observability convention but fail-closed — the routes are always registered and every request is denied 401 while no admin token is configured (`OPENOMNI_ADMIN_TOKEN` env / `server.adminToken` config); `GET /admin/ledger/attempts` returns the attempt-identity history from `work_item.attempt_allocated` facts (distinct attemptIds, monotonic per-stream attemptSeq, fingerprints reported as digests only, optional `contentFingerprint` digest filter) via the append core's new `factsByType` read (`Ledger.factsByType`, same prepared-SELECT discipline as `headFact`), `GET /admin/ledger/streams/:streamId/head` returns the newest recorded fact of one owner stream, `GET /admin/ledger/verification` runs the same observe-only tail-verification walk boot runs and reports chain-breaks, and `GET /admin/ledger/archive-manifest` serves the D2a artifact beside the database file (404 until `script/generate-ledger-archive-manifest.ts` produces it). No endpoint appends to the ledger or mutates state — the issue Manual QA's scenario-driving `POST /admin/ledger/scenarios` is deliberately not implemented (scenario matrices stay Verification-only conformance) — pinned by `apps/server/test/server/admin-ledger.test.ts` (fail-closed unconfigured/wrong token, happy paths, write-refusing-adapter + no-mutating-route pin). Remaining #510 scope (enumerated — NOT "D2b only"): (1) D2b — WorkerRun/PendingInteraction writer freeze after attempt-fact status coverage lands, then those consumers migrate to fact reads and both tables join the archive manifest; (2) the Governor incident on boot chain-break (today: observe-only Operational event); (3) policy-point grammar — ABSENT entirely, and the issue text's grammar conflicts with the shipped decision-class stream/fact vocabulary: **Owner item**, needs a ruling before implementation; (4) what-if/fork attempt labels (ReplayKey doc promises "a separately labeled NEW attempt" — no label vocabulary exists); (5) benchmarks (issue Verification's append-throughput/latency numbers); (6) the issue Manual QA scenario driver (`POST /admin/ledger/scenarios` was deliberately not implemented — scenario matrices stay Verification-only conformance); (7) cache/replay EXECUTION (`CacheKey`/`ReplayKey`/`NondeterminismManifest` are dormant vocabulary; lookup, replay-of-record execution, and JSONL export are unbuilt — #493). |
| `BusQuery` (stats, errors, worker-run history, chain verify) | ✅ | `packages/session/src/bus-persistence/query.ts`, `apps/server/src/server/routes.ts` | Wired consumer: token-protected `GET /observability/sessions/:sessionId/events` returns event stats, redacted error metadata, worker-run history, and hash-chain verification. Governor aggregation remains pending below |
| Token/cost tracking (per call, per message) | ✅ | `packages/llm/src/token/` | Aggregation (per agent/task type, success-rate correlation) absent |
| System Governor (scheduled/periodic analysis and post-hoc scoring) | 📋 | — | Zero code. Target access is selective ambient raw-transcript/full-ledger read without per-query Owner approval; every read is analysis-query-scoped and audited, raw payload stays outside user-facing sessions, and the target capability excludes write or loosening authority (#214) |
| Jester evaluation lifecycle | 📋 | — | Zero code. Target output is `silent` or one semantic question under an `evaluationId`, using exactly seven lenses and excluding dispatch or command authority. Kernel-host recording, suppression/egress decision, and lifecycle scoring remain planned |
| Voice rendering for authorized role output | 📋 | — | Zero code. Target Voice renders an already-authorized semantic result while preserving disposition and question count and without receiving dispatch authority (#458) |
| Incident fingerprint registry (cause × task type × failure mode; recurrence ladder) | 📋 | — | ADR-012 — match-before-create dedup discipline |
| Governor autonomy boundary + change journal (tighten autonomous / loosen approval / kernel floor) | 📋 | — | ADR-012 — every applied change journaled with scope tag; rate limits |
| Regression ratchet (canary window + counting-rule rollback → RCA on the change itself) | 📋 | — | ADR-012 — no separate machinery; runs through the same RCA pipeline |
| Fabricated-evidence handling (unresolvable claims → immediate RCA + executor reliability record) | 📋 | — | ADR-012 |
| Memory candidates (`MemoryCandidate` schema + emission from `work_item.completed` / Governor triage / Owner request) | 📋 | — | ADR-013 — prerequisites (lineage, provenance, bus) exist; no candidate emission |
| Built-in curated memory (frozen-snapshot system-prompt injection, bounded budgets, add/replace/remove tool) | 📋 | — | ADR-013 — Hermes pattern; injection via `on_system_prompt` policy timing |
| Session search tool (FTS5 over session store) | 📋 | — | ADR-013 — episodic recall, zero engines required |
| `Memory.Engine` port (ingest/recall/profile/feedback; transport-agnostic; mandatory scope filter) | 📋 | `packages/protocol/src/memory/` (planned) | ADR-013 — Anamnesis is the first plugin, not a dependency |

Completion admission has one configurable kernel gateway without exporting its raw request method through `DefaultDispatchRuntime`. Run/session-authenticated internal and connector Workers are the wired production consumers through specialized dispatch handlers. `DefaultDispatchRuntime.submitActorWorkItemCompletion` is a caller-authenticated seam for future Resident, API, A2A, human, SDK, and internal transport adapters; no server transport calls it today. Worker replay and boot recovery reuse the same admission service, durable verifier authority, and terminal writer. Request reservations carry process ownership, a lease, and a fencing counter before read-back work begins. The privileged SQLite completion writer is a composition-root-held closure authorized by a module-private token, not a public package subpath; same-process application modules remain trusted because `Storage.configure/reset/get` are composition-root APIs rather than an OS security boundary.

## Resident model (core-model, ADR-008, ADR-010)

| Component | Status | Code | Notes |
| --- | --- | --- | --- |
| `ResidentRuntime` (in-process, bypasses coordinator for conversation) | ✅ | `packages/openomni/src/resident/runtime.ts`, `ingress/engine.ts` | |
| Resident subagent lane absent | ✅ | `apps/server/src/execution/worker-runner.ts`, `packages/openomni/src/execution-runtime/tool/agent/tools/child-agent.ts` | `child_agent` is registered only inside Worker processes; Resident never receives it |
| Resident toolset = judgment-only (shell model) | 📋 | `apps/server/src/ingress/bridge.ts` | Resident currently gets `filesystem`, `execution`, `delegation`, `mcp`, and `custom` capabilities. #218 removes mutating direct tools, keeps read-only perception, and routes independent work through Worker dispatch; it does not add a subagent |
| Resident-selected policy plan | 🚧 | `packages/policy/`, `packages/agent/src/core/policy/`, `packages/openomni/src/resident/runtime.ts` | Shared actor-loop policy points are production-wired and can govern Resident execution, but explicit profile-selected Resident registrations and tool-catalog obligations remain target work. Policy is system-wide, not Worker-owned |
| Peek budget (per-turn tool-call cap making delegation structural) | 📋 | — | ADR-010 §6 — reuses existing budget hard-stop; profile config only |
| Mutating MCP/custom tools behind dispatch (read-only may stay direct) | 📋 | — | ADR-010 §6 effect-radius rule; direct attachment today is an unaudited side door |
| Intent classification (direct vs delegate) | 📋 | — | Pure in-context LLM choice; no classifier, no routing data |
| Fork / self-loop sessions | 🔌 | `packages/protocol/src/policy/resource.ts` | `"self-loop"` enum value exists; no code path creates one |
| Distilled writeback (worker output summarized before user session) | 🚧 | `packages/openomni/src/ingress/session-bridge.ts` | Raw worker output written directly by default; `writeback.commit` policy hook exists but optional; hygiene comes from session separation only |
| Cost-based model routing ("expensive thinks, cheap works") | 📋 | `apps/server/src/bootstrap/index.ts` | Single `resolveModel(config)` model for Resident and all workers |

## Runtime substrate (ADR-008 — shipped)

| Component | Status | Code | Notes |
| --- | --- | --- | --- |
| On-demand worker manager (spawn on demand, idle shutdown, max active) | ✅ | `packages/coordinator/src/worker-manager/worker-pool.ts` | Used by `apps/server/src/execution/coordinator.ts`. #462 steps 1, 2, and 4 shipped the injected `WorkerPorts` environment boundary (`events: BusEvent.Sink` observation edge — type lives in protocol — plus `toolRelay`/`inboundWait`), session-free protocol-only coordinator, `deliver(runId, task)` driver verb, typed `WorkerDeliveryError` rejections, and single `worker-pool.ts` construction path. `apps/server/src/execution/coordinator.ts` binds `events` to `Bus.publish`; that current binding is not durable/CAS record-before-act. The target `Ledger.append(event, expectedHead)` contract is a separate write gate and retains lossy `Bus.publish` for observation/projection. |
| Gate-side task policy stamping (`worker.spawn` → `PolicyPlan`) | ✅ | `packages/openomni/src/dispatch/handlers/worker.ts`, `packages/openomni/src/policy/resolver.ts`, `packages/openomni/src/dispatch/setup.ts` | #462 step 3 (§7) shipped the gate's actor/target-label resolution through `PolicyResolver` and resulting `policyPlan` stamp (always including required `builtin:tool-permission` + `builtin:idle-nudge`) on the spawn request; the plan rides the existing pipeline (`Execution.Request.policyPlan` → IPC `coordinator.spawn_run` → `worker-runner.ts` → `buildWorkerMiddleware` → worker `PolicyEngine`) as opaque data below the gate — no second authorization surface. Custom rules inject via `BuiltInDispatchOptions.policyResolver` at the composition root. Deferred: `Permission` stamping (the resolver produces policy IDs only; the fail-closed `builtin:tool-permission` policy is the enforcement lever) and connector-endpoint stamping (the P3 package-work leaves under [#459](https://github.com/INONONO66/openomni/issues/459)). The dispatch→driver session invariant is runtime-enforced: `sessionTreeId !== request.sessionId` now rejects with `WorkerDeliveryError code session_mismatch` |
| Worker-local `child_agent` tool (same-runtime nested child agents) | ✅ | `apps/server/src/execution/worker-runner.ts`, `packages/openomni/src/execution-runtime/child-agent/`, `packages/openomni/src/execution-runtime/tool/agent/tools/child-agent.ts` | Worker-only tool registered by the server worker runner when requested. Child agents run in-process under the parent worker run and dispatch canonical `delegation.worker.pre` before construction; deny/pending decisions prevent construction side effects. The spawn path bounds selectable tools to those exposed to the parent, removes delegation tools at `depth >= 1`, and rejects further nesting. Completed, failed, and cancelled children dispatch `delegation.worker.post` exactly once with the real result; parent cancellation during construction or execution is propagated, and Bus policy audit records carry the point ID/version. Completion can still queue back to the parent via `notifyOnComplete`. This is a lightweight nested execution primitive, not a durable WorkerRun/SubagentRuntime replacement |
| Existing-agent messaging (kernel surface + concrete channel delivery owner + Manual QA driver) | 🚧 | `packages/openomni/src/messaging/` (exported as `@openomni/openomni/messaging`), `packages/openomni/test/messaging/`, `apps/server/src/bootstrap/messaging.ts`, `apps/server/src/bootstrap/channels.ts`, `apps/server/src/channel/{discord,telegram}/` | #215 shipped the kernel surface: a policy-plane sender-target grant bounds exactly one sender to one target actor and operation set (the grant vocabulary cannot express Worker creation, delegation, WorkItem/executor, budget, or authority transfer); explicit target resolution reaches exactly one allocated endpoint or fails closed with typed `ungranted`/`target_missing`/`target_stale`/`target_ambiguous` denials; fire-and-forget records one deterministic `messaging.sent` audit and no Wait; awaited delivery appends exactly one Wait via `WaitService.open` before the delivery effect, and a delivery owner that returns the platform message id re-keys the Wait's `correlation.replyToMessageId` to it through the fold `recordDeliveryReceipt` transition (revision CAS), so real platform replies correlate. Phase E wired the concrete production delivery owner: server bootstrap composes `createExistingAgentMessaging` over the channel delivery routes registered in `bootstrap/channels.ts` (Discord DM and Telegram chat surfaces deliver to a resolved ActorEndpoint's externalId and report the platform message id; a channel without a registered route is a hard fail-closed delivery error), with grants sourced from `messaging.grants` server config defaulting to the EMPTY list — granting requires explicit configuration, and until then every send is denied `ungranted`. The N-of-M ingress reachability gap is closed: `kernel-routing-waits` proves a 2-of-3 awaited send delivered to a third target resolving through the ingress engine path from two responders' OWN registered endpoints (ingress evidence; delivery-endpoint pin applies only to the delivery-target responder, registry-anchored). The `existing-agent-message-driver` proves restart over a persisted 2-of-3 Wait resolving for the original owner with `allocationDelta: 0`, plus typed duplicate/ambiguous denials with unchanged quorum. Remaining partial edge: the composed send seam (`serverMessaging()`, precedent: the #490 dispatch entry seam) has no production dispatcher calling it yet — the Worker dispatch tool surface still permits only awaited `resident.ask`; wiring a production sender, plus timeout/cancellation surfaces beyond the fold rules, remains follow-up scope |
| Worker supervisor internals | ✅ | `packages/coordinator/src/worker-supervision/supervisor.ts` | Shared process supervisor used by `worker-manager`; legacy `createWorkerPool` facade removed. #462 step 4: constructed from a `WorkerSupervisorOptions` object (the 7-positional constructor is gone; `events` stays required per the no-silent-sink rule) and the three worker RPCs (`deliver`/`cancel`/`send`) live on the supervisor directly — `supervisor-client.ts` is folded in |
| Session-to-slot affinity | ✅ | `packages/coordinator/src/worker-manager/worker-pool.ts` | Plain per-pool affinity map (session → slot) inside the merged pool module; the standalone SessionRouter module (least-load routing/refCounts) was deleted as dead API |
| Crash recovery (mark interrupted at boot) | ✅ | `apps/server/src/execution/recovery.ts` | Moved out of the coordinator in #462 step 1 — boot recovery is ledger work over session stores, not process-driver work, so it lives with the server composition root |
| Injection queue (async delivery at turn.finish) | ✅ | `packages/openomni/src/execution-runtime/injection-queue.ts` | |
| Cron job registry persistence | ✅ | `packages/openomni/src/execution-runtime/cron-job-registry.ts`, `packages/session/src/storage/sqlite-cron-job-adapter.ts` | `schedule.create` jobs persist in SQLite and survive `Storage` reinitialize; `schedule.cancel` removes persisted jobs |
| Cron firing loop / boot runner | ✅ | `packages/openomni/src/execution-runtime/cron-job-runner.ts`, `apps/server/src/bootstrap/index.ts` | Server boot starts `CronJobRunner`, which reloads stored jobs, initializes missing `nextFireAt`, fires due jobs through `CronAdapter.fire(job)`, emits `cron_job.fired`, and advances the next fire time. Cron grammar is numeric five-field UTC; downtime fires at most once per tick, not once per missed interval |
| Boot-time PendingInteraction restoration | ✅ | `packages/session/src/pending-interaction/index.ts`, `apps/server/src/bootstrap/index.ts`, `apps/server/src/bootstrap/recovery.ts` | Boot recovery calls `PendingInteractionStore.cleanupExpired()` so persisted open/resolved/follow_up interactions that expired while the process was down are marked `expired` before channel dispatch resumes. Active open interactions remain storage-backed and matchable through Dispatch without in-memory rehydration |

## Structural guarantees (kernel surface, ADR-010 §1)

Claims that are actually enforced in code today — docs may say "cannot" only about these:

| Guarantee | Code |
| --- | --- |
| Workers cannot spawn new Workers, even with a matching WorkerGrant | `packages/openomni/src/dispatch/policy.ts`, `packages/openomni/src/execution-runtime/tool/agent/tools/dispatch.ts` |
| Budget hard-stop ends execution at limits (turns, tool calls, wall time) | `packages/agent/src/core/budget.ts`, `packages/agent/src/core/policy/builtin/budget.ts` |
| Tool permission is fail-closed | `packages/agent/src/core/policy/builtin/tool-guard.ts` |
| WorkItem terminal completion requires a matching durable admission for the current contract revision and basis | `packages/openomni/src/work-item/completion-admission.ts`, `packages/session/src/work-item/lifecycle.ts` |

Role-specific judgment remains partly userland: Resident delegation choices, session hygiene, evidence-over-self-report framing, and tool restraint still rely on `packages/openomni/src/agents/resident/prompt/` until #218 attaches the explicit Resident policy plan and judgment-only catalog. The shared policy interception plane itself is production code and applies across actor loops and boundaries.

## Known schema/doc debt

| Item | Action |
| --- | --- |
| `docs/vision.local.md` (v0.2) predates single-mode ingress and current package layout | Refresh or mark archived |
| `docs/persona-runtime-roadmap.local.md` uses pre-ADR-009 vocabulary (Main/Sub Persona) | Re-vocabulary or fold into ADR-010 ordering |

## Maintenance

When a 📋/🔌 item ships, flip it here in the same PR. When adding a new engine, add its **consumer** as a row at the same time — unwired engines must be visible.
