# Implementation Status

Single source of truth for the gap between accepted design and running code. Other docs (core-model, AGENTS.md, ADRs) link here instead of restating status inline.

**Legend**: ✅ implemented and wired · 🔌 dormant — built and tested, zero production callers · 🚧 partial · 📋 designed, not implemented · Last verified: **2026-06-14** (update this date when re-auditing).

> Project rule of thumb behind this file: an engine without a consumer does not count as shipped. "Built" and "wired" are tracked separately because the recurring failure mode here is completed schemas/stores that nothing calls.

## Three-layer message flow (ADR-009)

| Component | Status | Code | Notes |
| --- | --- | --- | --- |
| Server channel adapters (Discord/Telegram/GitHub/WS) | ✅ | `apps/server/src/channel/` | Raw → `InboundMessage` |
| IngressEngine (session candidate resolve → project → dispatch) | ✅ | `packages/openomni/src/ingress/engine.ts` | |
| `ActorResolver` / `ActorIdentity` / `ActorEndpoint` / `ActorRegistry` | ✅ | `packages/protocol/src/actor/`, `packages/session/src/actor/`, `packages/openomni/src/ingress/actor-resolver.ts` | Canonical actor identity + endpoint schemas, SQLite-backed registry, and ingress resolution for registered `(channel, workspace, externalId)` endpoints are wired, where `channel` is the platform/surface identifier. Registered actors project canonical `trustTier`; unregistered endpoints are sanitized back to legacy `id`/`role` and may receive a channel default tier when the matching ChannelGrant defines one |
| DispatchRuntime (policy authorize → handler routing) | ✅ | `packages/openomni/src/dispatch/runtime.ts` | Actions: `resident.ask`, `worker.spawn/send/resume/cancel`, `schedule.create/cancel` |
| WorkerGrant evaluation | ✅ | `packages/openomni/src/dispatch/policy.ts`, `packages/openomni/src/dispatch/effective-authority.ts` | WorkerGrant remains the scoped worker-egress grant store and is projected into effectiveAuthority as the `session_ownership_grant` axis for worker-scoped dispatch |
| Blacklist | ✅ | `packages/protocol/src/actor/`, `packages/session/src/blacklist/`, `packages/openomni/src/ingress/middleware/ingress-authority.ts`, `packages/openomni/src/dispatch/policy.ts` | Actor/endpoint/channel/pattern blacklist entries are schema-backed, persisted in SQLite, and checked as an absolute deny gate on both inbound ingress and dispatch authorization. Dispatch default authorization records blacklist outcomes as the effectiveAuthority `blacklist` axis |
| ChannelGrant (+ inbound treatment ceiling) | ✅ | `packages/protocol/src/actor/`, `packages/session/src/channel-grant/`, `packages/openomni/src/ingress/middleware/ingress-authority.ts` | Surface/workspace/channel grants are schema-backed, persisted in SQLite, and enforced fail-closed on inbound ingress after Blacklist. `trusted_channel` allows full access, `broadcast_channel` allows inbound as `evidence_only` and propagates that treatment through resolved event metadata, inbound policy context, and ingress audit projection; `blocked_channel` drops before resident execution. When `defaultTier` is present it supplies canonical TrustTier for unregistered actors before authority evaluation. Dispatch-local authorization records the ChannelGrant axis as `not_required` when no dispatch-local route exists |
| `TrustTier` enum + evaluation | ✅ | `packages/protocol/src/actor/`, `packages/protocol/src/dispatch/index.ts`, `packages/openomni/src/ingress/middleware/ingress-authority.ts`, `packages/openomni/src/dispatch/actor.ts` | `TrustTier` is canonical protocol enum data, `Dispatch.ActorContext.trustTier` is enum-bound, and ingress authority evaluates canonical tiers before legacy role/trusted fields. `owner`, `co_owner`, and `manager` may create top-level inbound work; `collaborator` and `observer` may pass only through `evidence_only` resident inbound treatment; `assigned_worker` remains denied for top-level creation unless an effectiveAuthority path grants a narrower scoped action |
| `effectiveAuthority` (5-axis intersection) | ✅ | `packages/openomni/src/dispatch/effective-authority.ts`, `packages/openomni/src/dispatch/policy.ts` | Dispatch default authorization now records explicit effectiveAuthority axis facts for blacklist, channel grant, personal/channel-default grant, session ownership grant, and pending interaction scope. Existing WorkerGrant evaluation is projected as the session ownership axis for worker-scoped dispatch; absent actor identity fails closed as a missing personal/channel-default grant. ChannelGrant and PendingInteraction routing remain owned by their dedicated ingress/PI rows; dispatch marks those axes `not_required` when no dispatch-local route exists |
| `PendingAsk` (transitional) | ✅ | `packages/protocol/src/communication/`, `packages/session/src/pending-ask/` | |
| `PendingInteraction` (successor; correlation routing, follow-up window) | ✅ | `packages/protocol/src/communication/`, `packages/protocol/src/dispatch/`, `packages/session/src/pending-interaction/`, `packages/session/src/storage/sqlite-pending-interaction-adapter.ts`, `packages/session/migration/0009_pending_interaction/migration.sql`, `packages/openomni/src/dispatch/pending-interaction-routing.ts`, `apps/server/src/ingress/bridge.ts`, `apps/server/src/handler/conversation.ts`, `apps/server/src/bootstrap/index.ts`, `apps/server/src/bootstrap/recovery.ts` | Canonical PI schema and SQLite-backed store exist with `open / resolved / follow_up / expired / cancelled`, `allowedActions`, `workerRunId`, correlation lookup, and follow-up window filtering. Dispatch pre-authorizes structured `actor.message` correlation matches to `actor.reply`, overrides the target session/run to the owning WorkerRun, and records `pending_interaction_scope` as the effectiveAuthority allow axis. Server channel messages build server-stamped structured correlation and Dispatch owns matching/authorization before Resident fallback. Boot recovery expires stale open/follow-up interactions |
| `executorKind` on WorkerRun (`internal_chat_agent` / `connector_endpoint` / `external_api` / `a2a` / `human_channel`) | ✅ | `packages/protocol/src/worker-run/`, `packages/session/src/worker-run/`, `packages/session/migration/0005_worker_run_executor_kind/migration.sql` | WorkerRun records carry durable coarse executor metadata; `WorkerRun.create()` defaults current in-process Worker runs to `internal_chat_agent`. Installed apps use `connector_endpoint` only as ledger metadata. Dispatch selection uses connector endpoint identity (`endpointId` / `connectorInstallationId`) instead of `executorKind` |
| Connector Endpoint apps (Claude Code / Codex / OpenCode as installed apps) | 🚧 | `packages/protocol/src/dispatch/index.ts`, `packages/protocol/src/app-connector/`, `packages/openomni/src/dispatch/handlers/worker.ts`, `packages/openomni/src/dispatch/handlers/connector-endpoint-worker.ts`, `packages/openomni/src/dispatch/handlers/connector-completion-projector.ts`, `apps/server/src/connector/`, `apps/server/src/bootstrap/dispatch-owners.ts` | Dispatch targets expose `endpointId` / `connectorInstallationId` and reject stale executor selectors. `worker.spawn` resolves enabled, consented AppConnector installations into the injected server connector driver owner, creates WorkItem / WorkerRun ledger state with `executorKind: connector_endpoint`, and records completion through `ConnectorCompletionProjector`. OpenOmni owns only dispatch/evidence projection; server owns external connector definitions, discovery/consent registry, process driver implementation, and bootstrap credential injection. Connector process dispatch fails closed without a `workspaceRoot` worktree. Terminal connector results can enter via `worker.complete`; `report_result` PendingInteraction matches route there instead of generic chat. Provider-native hook/plugin materialization remains an implementation detail of the server connector driver |
| `AppConnector` schema (declarative connector endpoint ABI) | ✅ | `packages/protocol/src/app-connector/`, `apps/server/src/connector/definitions.ts` | ADR-010 §3 schema shipped: detect/spawn/logs/question bridge/evidence/requires plus `profile.kind: "connector_endpoint"`, endpoint task defaults, provider driver capability (`install`, `submit`, observed events, evidence emitters), durable installation `endpointId`, consent records, and optional canonical workspace identity. Server-owned external definitions parse through the public ABI; OpenOmni no longer exports provider manifests |
| Install lifecycle (discover → register → consent → disable/uninstall → wire → smoke-verify; version-drift re-verification) | 🚧 | `packages/protocol/src/app-connector/index.ts`, `packages/session/src/app-connector/`, `apps/server/src/connector/discovery.ts`, `apps/server/src/connector/registry.ts`, `apps/server/src/bootstrap/dispatch-owners.ts` | Server connector discovery runs each server-owned external connector's detect command, extracts versions from the connector `versionPattern`, classifies missing / unsupported / failed / available candidates, treats tested version ranges as the first drift guard, and registers available candidates in SQLite-backed installation storage with an endpoint id. Owner consent is created later through `requestConsent()` / `grantConsent()` and persisted as a permission ceiling constrained to connector requirements. Storing an installation also upserts `Actor.Identity(kind: "ai_agent")` and `Actor.Endpoint(channel: "app_connector")`. Enabled, consented installations are selectable by endpoint target for `worker.spawn`, and server boot wires the default connector driver so endpoint targets can execute through normal dispatch paths. Version-drift re-verification incidents remain detect/smoke-only and do not yet trigger a broader remediation workflow |
| App question bridge (permission/clarification prompt → PI / `resident.ask`, suspend/resume) | 🚧 | `packages/openomni/src/dispatch/pending-interaction-routing.ts`, `apps/server/src/connector/question-bridge.ts`, `apps/server/src/connector/process-driver.ts`, `apps/server/src/bootstrap/dispatch-owners.ts` | Connector questions can route through structured PendingInteraction actions. Terminal `report_result` routes to `worker.complete`; `ask_clarification` routes to `resident.ask` or an Owner-facing PI. The current server-owned process driver can start a short-lived localhost bridge when the AppConnector declares a hook/stdio question bridge and the runtime owner provides a handler; durable app suspension/resumption around long-lived interactive questions remains pending |
| Native app log ingestion (raw → artifact, key events → journal; liveness for stall detection) | ✅ | `packages/protocol/src/app-connector/index.ts`, `packages/protocol/src/execution/index.ts`, `apps/server/src/connector/log.ts`, `apps/server/src/connector/log-path.ts`, `apps/server/src/connector/log-telemetry.ts`, `apps/server/src/connector/process-driver.ts`, `apps/server/src/connector/process.ts`, `packages/openomni/src/dispatch/handlers/connector-completion-projector.ts` | AppConnector `logs.path` is rendered for connector process runs, captured after process exit, credential-redacted, stored as an Artifact, returned on `Execution.Result.artifacts`, and recorded as WorkItem evidence. `completionReport.finalMessage: "log"` can use the captured log message as the connector final output. Structured `jsonl` / `stream_json` lines are projected into redacted `Execution.Result.logEvents` with artifact id, message, timestamp, sequence, and data, then recorded as WorkItem evidence for the execution journal. Structured log connectors can declare `tokenUsageField`, `tokenUsageMode`, and `toolCallField`; the runtime normalizes those fields onto log events, aggregates declared token usage onto `Execution.Result.usage`, and records token/tool-call evidence on the WorkItem. `spawn.stallTimeoutMs` liveness is driven by stdout/stderr stream activity for stream-output connectors and by rendered file/glob log mtime/size changes for file-backed connectors |
| Outbound external actions (`external.ask`, `a2a.ask`, `api.ask`) | 🚧 | `packages/protocol/src/dispatch/index.ts`, `packages/openomni/src/dispatch/handlers/outbound.ts`, `packages/openomni/src/dispatch/owners.ts`, `packages/openomni/src/dispatch/setup.ts` | Dispatch exposes canonical outbound action constants and built-in handlers for `external.ask`, `a2a.ask`, and `api.ask`. Handlers require an `external_actor` target, fail closed unless an outbound owner is injected, and pass the audited command plus endpoint, payload, correlation, timeout, wait, and abort signal to that owner. Concrete server/channel/API owner implementations and outbound delivery adapters remain pending |
| `device.*` world-control dispatch actions (driver in `apps/server/`, handler in registry) | 🚧 | `packages/protocol/src/dispatch/index.ts`, `packages/openomni/src/dispatch/handlers/device.ts`, `packages/openomni/src/dispatch/owners.ts`, `packages/openomni/src/dispatch/setup.ts` | Dispatch exposes `device.command` as the first world-control syscall. The built-in handler requires a `system` target with `target.id` or `target.name` as the device id, fails closed unless a device owner is injected, and passes the audited command plus device id, payload, timeout, wait, and abort signal to that owner. Concrete `apps/server` device drivers and broader `device.*` verbs remain pending |

## Evidence loop (design-philosophy §1/§3, ADR-010 §4)

| Component | Status | Code | Missing consumer |
| --- | --- | --- | --- |
| WorkItem schemas (`Info`, `Blocker`, `Evidence`, `VerificationGate`, `deriveStatus`) | ✅ | `packages/protocol/src/work-item/` | — |
| `WorkItemStore` (full CRUD + lifecycle + evidence + gates) | ✅ | `packages/session/src/work-item/`, `packages/openomni/src/dispatch/handlers/worker.ts` | Wired consumer: `worker.spawn` requires at least one acceptance criterion, creates/starts a ledger item, returns `workItemHash`, reflects failed/interrupted/cancelled coordinator results, and gates succeeded results on an evidence-backed completion report |
| WorkItem schema deltas (`originSessionId`/`workSessionId` split, `workerRunId`+`executorKind`, `completionReport`, `maxAttempts`, `outcome`) | ✅ | `packages/protocol/src/work-item/` | ADR-011 schema shipped; runtime completion gating is tracked in the row below |
| Completion-report evidence gate (claims → evidence refs; unevidenced = not done) | ✅ | `packages/session/src/work-item/`, `packages/openomni/src/dispatch/handlers/worker.ts` | Deterministic gate: `WorkItemStore.complete()` requires a completion report whose claim evidence IDs resolve to passing ledger evidence; `worker.spawn` succeeded results without a valid evidence-backed report remain blocked instead of completed. Runtime read-back invocation is wired below |
| ReadBackCheck evidence records + store helper | ✅ | `packages/protocol/src/work-item/`, `packages/session/src/work-item/` | Shipped structured `ReadBackCheck` records for URL fetch, API query, and citation match observations plus `WorkItemStore.addReadBackEvidence()` to persist read-back results as verification evidence |
| Runtime read-back executors (re-fetch URL, re-query API, citation-in-source matching) | ✅ | `packages/openomni/src/evidence/` | Shipped explicit `ReadBackExecutor.execute()` / `record()` API for HTTP URL re-fetch, read-only HTTP API re-query, and citation-in-source matching; `record()` feeds runtime observations into `WorkItemStore.addReadBackEvidence()` |
| Automatic read-back invocation during completion gating | ✅ | `packages/openomni/src/dispatch/handlers/worker-completion.ts` | `worker.spawn` succeeded results may include `readBackRequests`; dispatch now runs each request through `ReadBackExecutor.record()` before completion, persists verification evidence, attaches generated evidence IDs to completion-report claims, and blocks completion if read-back evidence fails |
| Connector/channel-specific read-back request builders | 🚧 | `packages/protocol/src/work-item/`, `packages/protocol/src/app-connector/index.ts`, `apps/server/src/connector/read-back-builder.ts`, `apps/server/src/connector/process-driver.ts` | AppConnector completion metadata can now declare templated read-back request builders, and the connector process driver fail-closes missing/non-JSON builder sources while appending generated `readBackRequests` to completion envelopes before the existing completion gate runs. Channel adapter builders for outbound publish/send/API actions remain pending |
| Internal worker retry default + kernel-enforced exhaustion blocker | ✅ | `packages/session/src/work-item/`, `packages/openomni/src/dispatch/handlers/worker.ts` | Internal `worker.spawn` ledger items are tagged `executorKind: internal_chat_agent`, default to `maxAttempts: 3`, and `WorkItemStore.retry()` refuses exhausted items after recording a `waiting_input` escalation blocker. Owner-visible notification / failed-item surfacing remains pending below |
| Owner-visible retry exhaustion ledger surface | ✅ | `apps/server/src/handler/conversation.ts` | Authenticated local WebSocket `show open tasks` includes failed WorkItems with an active retry-exhaustion Owner escalation blocker, including attempt/max-attempt context, so exhausted internal worker retries are no longer invisible |
| App manifest retry defaults + human reminder policy + non-local Owner notification | 📋 | — | ADR-011 — CLI apps should inherit per-application manifest defaults; humans are not retried, they need reminder policy under the social budget; exhausted failed items still need push-style Owner notification beyond the local ledger surface |
| Owner adoption signal storage primitive (`outcome`: adopted/corrected/redone/ignored) | ✅ | `packages/protocol/src/work-item/`, `packages/session/src/work-item/outcome.ts` | `WorkItem.Info.outcome` is schema-backed and `WorkItemStore.recordOutcome()` records Owner feedback on completed WorkItems while publishing `work_item.outcome_recorded` for downstream Governor consumers |
| Owner adoption collection surfaces + Governor calibration | 📋 | — | ADR-011 — no Owner-facing adopted/corrected/redone/ignored capture UI or dispatch command yet; Governor does not aggregate adoption outcomes or calibrate Resident evaluation leniency |
| Task ledger view ("show open tasks" — the OS's `ps`) | ✅ | `apps/server/src/handler/conversation.ts` | Authenticated local WebSocket chat command returns a capped pending/running/blocked WorkItem diagnostic plus retry-exhausted failed-item escalations from `WorkItemStore`; unauthenticated WebSocket and external-channel actor-scoped visibility remain future work because current `WorkItem` / `InboundMessage` schemas have no owner, trust-tier, or channel-grant field; web view also remains future work |
| Bus persistence (hash-chained event journal) | ✅ | `packages/session/src/bus-persistence/` | — |
| `BusQuery` (stats, errors, worker-run history, chain verify) | ✅ | `packages/session/src/bus-persistence/query.ts`, `apps/server/src/server/routes.ts` | Wired consumer: token-protected `GET /observability/sessions/:sessionId/events` returns event stats, redacted error metadata, worker-run history, and hash-chain verification. Governor aggregation remains pending below |
| Token/cost tracking (per call, per message) | ✅ | `packages/llm/src/token/` | Aggregation (per agent/task type, success-rate correlation) absent |
| System Governor (postmortem engine: incident RCA + slow aggregation loop) | 📋 | — | Zero code. v0 scoped in ADR-012: two lanes (immediate / daily batch), storm collapse, triage (defect vs preference→memory candidate) |
| Incident fingerprint registry (cause × task type × failure mode; recurrence ladder) | 📋 | — | ADR-012 — match-before-create dedup discipline |
| Governor autonomy boundary + change journal (tighten autonomous / loosen approval / kernel floor) | 📋 | — | ADR-012 — every applied change journaled with scope tag; rate limits |
| Regression ratchet (canary window + counting-rule rollback → RCA on the change itself) | 📋 | — | ADR-012 — no separate machinery; runs through the same RCA pipeline |
| Fabricated-evidence handling (unresolvable claims → immediate RCA + executor reliability record) | 📋 | — | ADR-012 |
| Memory candidates (`MemoryCandidate` schema + emission from `work_item.completed` / Governor triage / Owner request) | 📋 | — | ADR-013 — prerequisites (lineage, provenance, bus) exist; no candidate emission |
| Built-in curated memory (frozen-snapshot system-prompt injection, bounded budgets, add/replace/remove tool) | 📋 | — | ADR-013 — Hermes pattern; injection via `on_system_prompt` policy timing |
| Session search tool (FTS5 over session store) | 📋 | — | ADR-013 — episodic recall, zero engines required |
| `Memory.Engine` port (ingest/recall/profile/feedback; transport-agnostic; mandatory scope filter) | 📋 | `packages/protocol/src/memory/` (planned) | ADR-013 — Anamnesis is the first plugin, not a dependency |

## Resident model (core-model, ADR-008, ADR-010)

| Component | Status | Code | Notes |
| --- | --- | --- | --- |
| `ResidentRuntime` (in-process, bypasses coordinator for conversation) | ✅ | `packages/openomni/src/resident/runtime.ts`, `ingress/engine.ts` | |
| Resident toolset = judgment-only (shell model) | 📋 | `apps/server/src/ingress/bridge.ts` | **Contradicts core-model today**: Resident currently gets `["filesystem", "execution", "delegation", "mcp", "custom"]` — full read/write/edit/bash. Demotion to read-only + dispatch is ADR-010 step 6 |
| Peek budget (per-turn tool-call cap making delegation structural) | 📋 | — | ADR-010 §6 — reuses existing budget hard-stop; profile config only |
| Mutating MCP/custom tools behind dispatch (read-only may stay direct) | 📋 | — | ADR-010 §6 effect-radius rule; direct attachment today is an unaudited side door |
| Intent classification (direct vs delegate) | 📋 | — | Pure in-context LLM choice; no classifier, no routing data |
| Fork / self-loop sessions | 🔌 | `packages/protocol/src/policy/resource.ts` | `"self-loop"` enum value exists; no code path creates one |
| Distilled writeback (worker output summarized before user session) | 🚧 | `packages/openomni/src/ingress/session-bridge.ts` | Raw worker output written directly by default; `writeback.commit` policy hook exists but optional; hygiene comes from session separation only |
| Cost-based model routing ("expensive thinks, cheap works") | 📋 | `apps/server/src/bootstrap/index.ts` | Single `resolveModel(config)` model for Resident and all workers |

## Runtime substrate (ADR-008 — shipped)

| Component | Status | Code | Notes |
| --- | --- | --- | --- |
| On-demand worker manager (spawn on demand, idle shutdown, max active) | ✅ | `packages/coordinator/src/worker-manager/manager.ts` | Used by `apps/server/src/execution/coordinator.ts` |
| Worker supervisor internals | ✅ | `packages/coordinator/src/worker-supervision/supervisor.ts` | Shared process supervisor used by `worker-manager`; legacy `createWorkerPool` facade removed |
| SessionRouting helper | ✅ | `packages/coordinator/src/worker-supervision/session-routing.ts`, `packages/coordinator/src/worker-manager/manager.ts` | `worker-manager` owns an isolated `SessionRouter` instance for live session-to-slot affinity; the old exported singleton and worker-supervision barrel have been removed |
| Crash recovery (mark interrupted at boot) | ✅ | `packages/coordinator/src/recovery/` | |
| Injection queue (async delivery at turn.finish) | ✅ | `packages/openomni/src/execution-runtime/injection-queue.ts` | |
| Cron job registry persistence | ✅ | `packages/openomni/src/execution-runtime/cron-job-registry.ts`, `packages/session/src/storage/sqlite-cron-job-adapter.ts` | `schedule.create` jobs persist in SQLite and survive `Storage` reinitialize; `schedule.cancel` removes persisted jobs |
| Cron firing loop / boot runner | ✅ | `packages/openomni/src/execution-runtime/cron-job-runner.ts`, `apps/server/src/bootstrap/index.ts` | Server boot starts `CronJobRunner`, which reloads stored jobs, initializes missing `nextFireAt`, fires due jobs through `CronAdapter.fire(job)`, emits `cron_job.fired`, and advances the next fire time. Cron grammar is numeric five-field UTC; downtime fires at most once per tick, not once per missed interval |
| Boot-time PendingInteraction restoration | ✅ | `packages/session/src/pending-interaction/index.ts`, `apps/server/src/bootstrap/index.ts`, `apps/server/src/bootstrap/recovery.ts` | Boot recovery calls `PendingInteractionStore.cleanupExpired()` so persisted open/resolved/follow_up interactions that expired while the process was down are marked `expired` before channel dispatch resumes. Active open interactions remain storage-backed and matchable through Dispatch without in-memory rehydration |

## Structural guarantees (kernel surface, ADR-010 §1)

Claims that are actually enforced in code today — docs may say "cannot" only about these:

| Guarantee | Code |
| --- | --- |
| Workers cannot create top-level work (`worker.spawn/cancel/resume/schedule` denied) | `packages/openomni/src/ingress/middleware/ingress-authority.ts` |
| Budget hard-stop ends execution at limits (turns, tool calls, wall time) | `packages/agent/src/core/execution/stream-policy-dispatch.ts` |
| Tool permission is fail-closed | `packages/agent/src/core/policy/builtin/tool-guard.ts` |

Everything else behavior-shaping (delegation judgment, session hygiene, evidence-over-self-report, tool restraint) is currently **prompt convention** (`packages/openomni/src/agents/resident/prompt/`), i.e. userland.

## Known schema/doc debt

| Item | Action |
| --- | --- |
| `AgentProfile.Definition.permissions` is defined but ignored at runtime (warning + `policyPlan` supersedes) | Remove the field or wire it; a schema field that lies violates ADR-002's intent |
| `docs/vision.local.md` (v0.2) predates single-mode ingress and current package layout | Refresh or mark archived |
| `docs/persona-runtime-roadmap.local.md` uses pre-ADR-009 vocabulary (Main/Sub Persona) | Re-vocabulary or fold into ADR-010 ordering |

## Maintenance

When a 📋/🔌 item ships, flip it here in the same PR. When adding a new engine, add its **consumer** as a row at the same time — unwired engines must be visible.
