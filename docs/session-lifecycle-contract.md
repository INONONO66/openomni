# Unified session lifecycle and transition ownership

**Version 1, target contract for #968, reconciled with main `f9c02a66` (includes `ede6a7f0` and `5b3ff997`).** Canonical authority is [Durable session identity and runtime ownership](kernel-contract.md#durable-session-identity-and-runtime-ownership), [L2 action executor](kernel-contract.md#l2-action-executor), [Session L3 execution contract (#937)](kernel-contract.md#session-l3-execution-contract-937), [State and the ledger fold](kernel-contract.md#state-and-the-ledger-fold), and [Waiting on the world](core-model.md#waiting-on-the-world). This document supplies the cross-region transition delta and child handoff, not a replacement for those definitions. Retention follows [SLOP's data receipt](SLOP.md#967-semantic-and-data-retention-receipt).

## 1. Evidence and construction map

The receipt census in section 7 gives each current declaration its qualified name, source span, disposition, and sole child. It was collected with `rg -n` and a TypeScript declaration walk before authoring. A source name below resolves to that census. **Target names are construction destinations, not claims about HEAD.**

| Collaborator key | Exact destination and scope |
| --- | --- |
| DEC | target: `decideSessionTransition` in `packages/agent/src/session-controller.ts` (owner #969). One pure decision entry over the product in section 2; takes the existing executor's captured compiled-policy verdict as input and delegates alarm-case predicates to ALD. Returns a refusal or a proposed action batch, operational patch, and effect descriptors. |
| APPLY | target: `applySessionTransition` in `packages/ledger/src/storage/sqlite-l0-adapter.ts` (owner #969). Extends the existing session commit transaction, not a new storage authority. Checks the decision's revision/fence and region CAS predicates; commits actions and operational projections together. |
| DISPATCH | target: `dispatchSessionTransition` in `packages/agent/src/session-controller.ts` (owner #969). Consumes APPLY's committed receipt and dispatches only its recorded effect descriptors. Calls existing `runTurn` for runner entry. |
| ALD | target: `decideAlarmTransition` in `packages/agent/src/session-stop-evidence.ts` (owner #971). Pure alarm/occurrence branch called by DEC; not the existing evidence reader. |
| ALA | target: `applyAlarmTransition` in `packages/ledger/src/storage/sqlite-l0-adapter.ts` (owner #971). Alarm-row and occurrence-action CAS inside APPLY's transaction. No occurrence table. |
| ALX | target: `dispatchAlarmTransition` in `packages/agent/src/session-controller.ts` (owner #971). Physical evaluator start/stop and durable wake dispatch, invoked by DISPATCH. |
| REQUEST | target: `decideRequestTransition` in `packages/agent/src/executor-approval.ts` (owner #969). Pure original-request branch called by DEC. Replaces the independent logical Wait/Approval/delegation decisions in section 7. |
| RCV | target: `commitReceivedMessage` in `packages/ledger/src/session/kernel.ts` (owner #969). Atomic destination action/inbox insert with message-id plus payload-digest equality; returns original receipt on equivalent redelivery. |
| WAKE | target: `dispatchOutboundMessage` in `packages/agent/src/session-controller.ts` (owner #969). Drains source-owned outbound obligations to RCV, then invokes destination `reconcile`; records acknowledgement in the source session. |
| SCHEMA | target: `SessionTransition` in `packages/protocol/src/ledger/l0.ts` (owner #969); target: `AlarmTransition` in that file (owner #971). Versioned command/receipt schemas for sections 2-5, with new `request`, `reply`, `outbound`, `occurrence`, `alarm.control` action kinds added to existing `LedgerAction.Kind` at the owning cutover. |
| RECOVERY | target: `decideRecovery` in `packages/agent/src/session-admission.ts` (owner #970). Supplies the recovery classification to DEC; uses existing `resumeTurn`/`resumeInterrupted`, not an independent driver loop. |
| HARNESS | target: `runLifecycleTrace` in `packages/agent/test/session-lifecycle-conformance.test.ts` (owner #973). Runs section 6's fixed vectors and finite interleavings against the real session/executor/store surfaces. Test names are specified in section 6.7. |

HEAD mechanics are narrower than that target. `createSessions.acquireLease` compares expected fence, permits the same owner or an absent/expired owner, and increments fence; `renewLease` requires a strictly live matching lease. `commitSession` checks lease before revision, action-parent/id validity, and pending inbox consumption. `appendAction` advances revision **per appended action**, not per transaction. `createInbox.commit` appends an action before inserting the inbox row. `createAlarms.arm` appends `alarm.arm` but has no lease or active-key predicate; `createAlarms.cancel` changes only an armed row by id and appends no action; `due` is a reader. These current primitives do not implement section 5's target guards.

## 2. State product and event envelope

| Region | Product, including identity and legal terminals |
| --- | --- |
| Durable session | `{id,parentId,role,state:idle/running/interrupted,revision,toolsGeneration,systemHash,policyGeneration}`. Declaration identity is immutable. |
| Residency/lease | `{handle:absent/resident/closing,owner:null/string,fence,expiresAt:null/ms,retainedRunner:boolean,retainedEffects:set(operationId),retainedFailure:null/error}`. Closing/hibernation is not deletion of the session. |
| Turn | `none` or `{turnId,resultId,resumeCount,boundaryActionId,toolsGeneration,toolsHash,systemHash,policyGeneration,state:open/terminal,terminal:null/result/error/interrupted/waiting}`. Waiting is a **sealed turn**, with session row `idle` at HEAD (`seal`); it is not pending approval. |
| Intake | Ordered `Inbox.Row[]`, each `{id,sessionId,kind:prompt/interrupt/resume,content,origin,status:pending/consumed,consumedBy,consumedAt,createdAt,ordinal}`; target received messages retain their correlation in `origin`. |
| Wave/operation | `{waveId=firstIntentId,turnId,calls:[{callId,index,op,parsedInput,inputHash,effectHash,sequential,preDecisionId,intentId?,phase:prepared/pre_decided/admitted/awaiting_approval/body/post/recovery_pending/terminal,terminal:null/blocked_pre/executed/blocked_post/failed/cancelled/outcome_unknown,disposition?:reverted/irreversible,recovery?:{site,classification,proof,proofReceipt,error,revertReceipt}}]}`. Operation id is its intent id; pre-denied calls have no intent and their pre-policy fact accounts for `blocked_pre`. Only phase terminal has a non-null terminal. Policy facts remain distinct from operation results. `recovery_pending` is a target runtime overlay while a terminal cannot be committed; the immutable last committed phase is not rewritten. |
| Original request | `{requestId=originalIntentId,sessionId,turnId,callId,parsedInput,inputHash,effectHash,generation,toolsHash,domainRevisions,deadline,expectedResponders,correlation,bindingDigest,resolution:first/quorum/all,threshold,seenReplyIds,replies,state:open/resolved/refused/expired/cancelled,outcome:null/answered/outcome_unknown/denied/cancelled}`. Approval is a suspension of this invocation; no reconstructed model call. |
| Outbound | Action-derived `{messageId,sourceSessionId,sourceActionId,destinationSessionId,requestId,replyTo,terminal,content,digest,state:pending/delivered,destinationReceipt:null/{id,revision}}`. Payload is immutable; only verified destination receipt changes pending to delivered. Child result maps to completed on the wire; error/interrupted keep their names. Waiting is not a child final reply. |
| Alarm | Existing alarm identity/row plus target coordination fields `{controlKey,epoch,alarmRevision,ownerSessionId,mode:once/recurrent,source:at/idempotent_poll/live,cursor:null/value,lastBatchHash:null/hash,notificationLimit,notifications,pauseNotified,resumeFrom:null/armed/fired,evaluator:{owner,fence,expiresAt}}`; status `armed/cancelled/fired/paused`. |
| Occurrence | An action-derived record `{occurrenceId=openActionId,alarmId,epoch,sourceKey,batchHash,cursorBefore,cursorAfter,sessionId,turnId?,parentActionId,fireAt,generation,inputHash,deadline,state:open/evaluate/terminal,terminal:null/completed/failed/cancelled/expired/deduped,output?,reason?,wakeMessageId?}`. Evaluator lease is alarm-scoped; occurrence identity is not a lease id. |

Every target command is `{version:1,inputId,sessionId,at,expectedRevision,authority,payload}`. Authority is authenticated ingress evidence for inbox admission; all owner writes carry `{owner,fence}`. Alarm evaluation additionally carries `{evaluatorOwner,evaluatorFence,expectedAlarmRevision}`. Times and authentication evidence are captured before DEC, and after any asynchronous authorization they are revalidated against the current snapshot. A decision returns either `{kind:"refused",code,receiptAction?}` or `{kind:"apply",expectedRevision,authority,actions,consumeInboxIds,regionPatch,effects}`. An effect descriptor is `{kind,causingActionId,idempotencyKey,payload}`. APPLY returns `{ok:true,revision,receipts,effects}` or the current `CommitResult` refusal shape. Revision/fence refusal never dispatches the decision's effects.

Events in the tables below name **commands**, not invented current exports. Current action kinds and fields are spelled out in section 6. New command payloads include all fields shown in their region product; `request.open` and `occurrence.open` carry the complete product with initial state. Result commands carry the identity, expected current state, terminal, reason/output, and captured time. Unknown versions, extra schema fields, absent identity, illegal source/event pairs, and inconsistent products are read-only `invalid_transition` from DEC. Only explicitly listed transitions are admitted. Within a table, the common precedence rules below run before event-specific guards; a catch-all applies only after every earlier case fails. Cross-region rows compose one decision/transaction, not competing transitions.

## 3. Session, intake, operation transitions

In these tables, `C` means the exact current fenced collaborator `commit` in `packages/ledger/src/session/kernel.ts`; `X` means current `createExecutionRecord.commit` in `packages/agent/src/executor-record.ts`. Target DEC/APPLY is the handoff for cross-region CAS; current decision/dispatch names are retained as their implementation collaborators. `none` means zero apply/dispatch, not an unnamed owner.

| Source product | Event + payload | Decision/guard | Destination | Apply | Dispatch |
| --- | --- | --- | --- | --- | --- |
| absent session | declare `{id,parentId,role,tools,system,policyGeneration,actionId,at}` | current `SessionRegistry.declare`; valid declaration, no conflicting durable identity/catalog | idle, generation 1; initial configure action | current `materialize` in ledger/session/kernel.ts | none |
| declared, any lifecycle | repeat declare `{id,runner,role,parentId,tools,system}` | current `assertDeclaration`/`SessionRegistry.declare`; exact identity/catalog and runtime runner binding | same handle or reinstalled handle | no new action | current `SessionRegistry.install` |
| any declared, handle open | prompt/interrupt/resume `{id,kind,content,origin,createdAt,parentActionId}` | current `enqueue`; input id new; target ingress authority and digest check precede RCV | pending inbox, lifecycle unchanged at input commit | current `commitInbox`; target RCV for external dedupe | current `reconcile` |
| idle, no open turn, first pending row is prompt, no retained runner | admit `{inboxIds,resultId,turnId,generation,boundaryActionId}` | current `startTurn`; acquire live lease, latest generation, all prompt policies allow | running/open; listed rows consumed | prompt `runExisting` policy facts via X; deliveries + turn intent via C | current `runTurn` |
| idle/running + pending prompts | prompt pre/post denied `{inboxIds,reason}` | current `evaluatePromptPolicies`; pre denial or post block/invalid transformation | inbox consumed, no new turn; existing turn returns policy error if at boundary | current `consumePolicyBlockedInbox`, C (empty action batch; retain lease at boundary) | none/new runner zero |
| running/open | boundary `{turnId,resultId,checkpointId,boundary:before_llm/after_llm/after_tools,inboxIds}` | current `drainBoundary`; current pinned policy; all pending rows in ordinal order; C validates unique pending ids | same turn/generation, checkpoint + consumed rows; interrupt in batch marks boundary interrupted | prompt policy via X; checkpoint then deliveries via C | current `runTurn` boundary callback returns prompt messages and aborts if interrupt |
| idle/no open turn + leading controls | drain control `{inboxIds}` | current `driveAvailable`; controls before first prompt, or no prompt | same lifecycle; `turnId:"noop"` deliveries consumed | current `consumeNoopInbox`, C; lease released | none |
| running/open | resume inbox | current `enqueue`; running resume is not a new execution | resume remains queued until boundary/no-op drain | only initial inbox commit | none |
| running/open | interrupt inbox then interrupt state | current `enqueue`; commit input first, C changes state with zero actions | interrupted/open briefly, then terminal interrupted | C; `seal` later commits pending interrupt deliveries + preminted terminal | abort in `enqueue` even if state CAS fails; stale runner cannot seal |
| interrupted/no open turn, prompts queued, no resume | reconcile `{sessionId}` | current `driveAvailable`; no pending resume | unchanged, queued prompts retained | none | none |
| interrupted/no open turn, resume queued | resume `{resumeInboxId,newTurnId,newResultId}` | current `resumeInterrupted`; retained runner settled; latest terminal exists; acquire lease; latest generation | running/new turn/new result; only resume row consumed now; queued prompts drain at boundary | deliveries + new intent via C | current `runTurn`, `resume:true` |
| resume with no terminal | resume inbox | current `resumeInterrupted`; no terminal | no-op consumed | current `consumeNoopInbox`, C | none |
| running/open | runner result `{kind:"result",text,finishReason?,usage?}` | current `runTurn`, valid post-policy result; one open terminal slot | idle/sealed result; terminal stores text/boundary/resume count (usage stays in execution evidence) | turn post policy X then current `seal`, C | current `hibernate` after lease release |
| running/open | runner result `{kind:"waiting",text,reason:"live_wait",alarmIds}` | current `sessionStopEvidence` + `runTurn`; armed action from current turn, no unresolved request that forbids stop | idle/sealed waiting, live alarms retained | turn post policy X then `seal`, C | ALX only when a future alarm input wins, not at seal |
| running/open | runner throws or invalid post output `{errorName,text}` | current `runTurn`; abort takes precedence if signal is set | idle/sealed error, or interrupted/sealed interrupted if aborted | `seal`, C | none |
| child running/open, parentId non-null | terminal.seal `{resultId,kind:result/error/interrupted,text,originalRequest}` | target DEC composes the applicable runner terminal row above with section 4's mandatory outbound row; terminal slot empty, original parent/request/replyTo verified | sealed terminal AND pending source obligation, never a terminal-only intermediate product | target APPLY extends current `seal` batch with outbound.open; #970 seal consumes #969 APPLY | WAKE after the whole batch commits |
| open after process loss | recover `{turnId,resultId,capturedGeneration,resumeCount,boundaryActionId}` | current `resumeTurn`; acquire available/expired lease; no pending interrupt; count < 10; `generationForOpen` validates hashes | running, same turn/result and captured generation; count + 1 | resume action via C | current `runTurn`, `resume:true` |
| crash-open + pending interrupt | recover same payload | current `resumeTurn`; pending interrupt checked before budget/generation/provider entry | interrupted/sealed, same result id | current `seal`, C, interrupt deliveries consumed | no runner |
| crash-open count >= 10 | recover same payload | current `resumeTurn` exhausted | idle/error `session resume budget exhausted` | current `seal`, C | no runner |
| crash-open + missing pinned generation | recover same payload | current `generationForOpen` throws | unchanged open, no substitute generation; leased until release/expiry | no resume action | none |
| recovered executable catalog missing/changed | captured runner dispatch | current `createTurnDispatcher` refuses captured mismatch | runner error sealed by `runTurn`; no tool body | C for error terminal | none/tool zero |
| unleased/expired | acquire `{owner,expectedFence,now,expiresAt}` | current `createSessions.acquireLease`; expectedFence exact, no different live owner, expiresAt > now | owner/fence+1/expiry, no revision increment | operational lease CAS | none |
| live lease held by the requesting owner | acquire same payload | current `createSessions.acquireLease`; expectedFence exact, owner matches, expiresAt > now; distinct from renew | same owner/fence+1/new expiry, no revision increment; all old-fence proposals stale | operational lease CAS | none |
| live lease | renew `{owner,fence,now,expiresAt}` | current `createSessions.renewLease`; identity matches and old expiry > now | same fence/new expiry | operational lease CAS | none |
| live lease | renewal failure | current heartbeat in `runTurn` | runner aborted, no stale commit authorized | none | stop heartbeat + abort |
| interrupted/sealed + retained runner/effect | configure `{operation,tools,system,authorization}` | current `configure`; same owner must retain existing fence; generation CAS after authorization | latest generation advances, original execution pin unchanged, lease retained | configure action + pointers via C, releaseLease false | none |
| any declared | configure/revert `{operation,tools,system,authorization}` | current `configure`; generation unchanged across authorization; denied/stale/duplicate tool or block refuses | next generation, open turn pin unchanged | configure action via C; release only if no active/retained runner | none |
| interrupted/sealed + retained runner/effect | raw settlement `{operationId,result}` | current retained continuation in `runTurn`; all raw effects settled; no late result may replace sealed turn | retained false; release matching live lease; release failure captured for next caller | current `releaseHeldLease`, C with zero actions | stop heartbeat then `hibernate` |
| idle/interrupted + no active/retained runner + no live lease + empty inbox | hibernate | current `hibernate` | handle absent; durable row/history preserved | none | lifecycle.release and onHibernate callback |
| any handle | close `{graceMs}` | current `createController` handle.close; closed handle rejects new input; caller wait bounded | closing; live raw runner retains lease after grace | none | abort, then runtime release only after settlement |
| any state | invalid event or stale/repeated apply | DEC; source/identity/revision/fence guard failed | unchanged | none (C returns stale/revision/inbox when invoked) | none |

### Operation and wave region

Post-body exception recovery is a **target** extension, not a claim that HEAD catches these exceptions. `executeBatch` awaits the wave before processing results in call order (`packages/agent/src/executor.ts:198-275`); post policy, reverter, result projection or commit can then throw (`packages/agent/src/executor.ts:354-381`). RECOVERY captures `{operationId,site:post_policy/reverter/result_commit,error:{name},classification:local_transactional/endpoint_idempotent/read_back_reconcilable/ambiguous_no_replay,proof:absent/applied/reverted/indeterminate,proofReceipt:null/{id,digest},revertReceipt:null/id,rawSettled}`. `reverted` requires a durable receipt of the **registered** reverter's successful return; a thrown reverter is never proof of rollback. Local transaction read-back, an endpoint's same-key receipt, or authoritative correlated read-back respectively may prove absent/applied; absent such evidence, proof is indeterminate. A decisive proof carries its source proofReceipt; indeterminate proof has proofReceipt null. Ambiguous-no-replay always has indeterminate proof; incompatible classification/proof/receipt combinations are invalid_transition before recovery precedence. No post-body exception path re-executes the body or fabricates a post-policy verdict.

Recovery precedence is: read the original terminal slot first; if present, preserve it even when commit threw after persistence. Otherwise, if read-back is unavailable or the session cannot commit (lost/expired lease, stale revision, sealed-turn execution port, or unavailable storage), choose recovery_pending. Only a fresh valid owner with an empty slot may commit the classified result. A failed recovery commit repeats the same read-back/authority decision, never a blind append. For **every** recovery row, patch only this operation in retainedEffects: remove it iff rawSettled, otherwise retain/add it until its exact settlement signal; preserve other operations and retainedRunner. Store the captured error in retainedFailure; no recovery row releases a foreign lease. A pending overlay is not durable evidence: after process loss the still-unsettled intent is reclassified from persisted evidence. Proof/error/reverter evidence that can be committed is retained in the result's `recovery` field.

| Source | Event + payload | Decision/guard | Destination | Apply | Dispatch |
| --- | --- | --- | --- | --- | --- |
| unparsed call | prepare `{id,tool,input}` | current `createDispatcher.prepare`; tool exists, input parses, executor captured | prepared parsed invocation, or typed unregistered/invalid/context refusal | none | none |
| prepared wave | preflight `{orderedCalls,pinnedPolicy}` | current `executeBatch` calls `decide` for **all** calls before any intent/body | pre_decided; deny -> terminal blocked_pre without intent; allow/approval -> admitted intent | policy facts X, admitted intents current `appendIntent` | none |
| admitted + any approval needed | approval.request `{originalRequest}` | current `awaitApproval`; request binds original parsed input, intent id, turn, generation, pre-revision/decision id, deadline | whole wave gate closed; B awaiting_approval, siblings admitted but body-zero | request policy fact X; target original-request state via APPLY | schedule captured deadline callback only |
| gate closed | answer `{request,decision:approve/refuse,credential}` | current `approvals.answer` in executor-approval.ts; digest and expiry/signal checked before/after authenticated Owner evidence | approval resolved; refuse denies only its call; all required approvals resolved opens gate | answer policy fact X; target REQUEST/APPLY CAS | current `runWaveBodies` only after gate opens |
| gate closed | deadline `{requestId,at}` | current nested `expire` inside `awaitApproval`; at >= deadline and pending/not answering/not aborted | timeout denies its call; siblings admitted in original order | timeout policy fact X | current `runWaveBodies` |
| gate closed/active wave | interrupt `{turnId}` | current `runWaveBodies` signal; settled body slots stay settled, all unfinished slots cancel; no new sequential body | terminal cancelled per unfinished intent, raw effects retained | positional cancelled results current `appendResult`, X | abort physical work; no Started/Completed for never-started calls |
| admitted and gate open | body `{intentReceipt,parsedInput}` | current `executeBatch`; no denied/refused call body; D waits for preceding group | body, then post or failed | already committed intent | current `runWaveBodies` -> registered body; Started observation |
| body | resolved `{value}` | current `applyPostPolicy` evaluates captured policy over intent/effect/result | post (not terminal until result commit) | separate post decision X | registered reverter only if post blocked |
| post allowed | result `{intentId,value,resultHash,effect,toolResult?}` | current `finishRun` | terminal executed | result X | Completed after result receipt |
| post blocked | result `{intentId,reason,disposition:reverted/irreversible,effect,resultHash,toolResult?}` | current `applyPostPolicy`; reverted only after request.revert resolves; absent reverter -> irreversible | terminal blocked_post | current `finishRun`, X | Completed error; committed message delivery is not undone |
| body rejected | failure `{intentId,error:{name},failure?,callId?,toolResult?}` | current `appendFailure` | terminal failed | X | Completed error if body started |
| refused/timeout call | result `{intentId,terminal:blocked_pre,reason:approval_refused/approval_timeout,callId,toolResult?}` | current `executeBatch`; intent exists only because pre was not deny | terminal blocked_pre | X in original call order | no body observation |
| terminal operation / sealed turn | duplicate result `{inputId,intentId,digest}` | target DEC checks terminal slot; HEAD `createExecutionLedger` rejects a sealed turn/fence, `appendAction` rejects repeated action id (not semantic duplicate under a fresh id) | same terminal; equal input no-op, divergent invalid_transition | none | none |
| post-body exception, terminal found on read-back | recover with captured evidence | RECOVERY first case; exact original intent terminal receipt exists | phase terminal, terminal/disposition/recovery exactly from that receipt; retained patch above | no new action; return original receipt | none; no replayed Completed |
| post-body exception, terminal not found or read-back unavailable, cannot commit | recover with captured evidence | RECOVERY second case; read-back, write authority or storage unavailable as defined above | runtime phase recovery_pending, terminal null, disposition absent, captured recovery evidence; durable phase/actions unchanged; retained patch above | none; typed stale/revision/unavailable or sealed-turn refusal retained, not swallowed | none |
| post-body exception, no terminal, can commit | recover with proof absent | RECOVERY third case; local_transactional/endpoint_idempotent/read_back_reconcilable with authoritative absence proof | phase terminal, terminal failed, disposition absent, recovery evidence retained; retained patch above | APPLY one original-intent result `{phase:"result",terminal:"failed",error,effect,recovery}` | none |
| post-body exception, no terminal, can commit | recover with proof applied or reverted | RECOVERY third case; one of the three provable classifications, matching receipt; reverted requires revertReceipt | phase terminal, terminal failed (completion failed, not proof of body failure), disposition irreversible for applied or reverted for proved revert; retained patch above | APPLY one failed result with effect/error/recovery and exact disposition | none |
| post-body exception, no terminal, can commit | recover with proof indeterminate | RECOVERY third case; any classification lacking decisive proof, including ambiguous_no_replay | phase terminal, terminal outcome_unknown, disposition absent; retained patch above; never claim successful revert | APPLY one original-intent result `{phase:"result",terminal:"outcome_unknown",error,effect,recovery}` | none |
| all remaining operation products/events | any command | DEC; not an admitted case above | unchanged, read-only invalid_transition | none | none |

## 4. Original request, late input, and delivery

HEAD decision symbols are `attachReply` in protocol/wait/fold.ts, `approvals.answer` in agent/executor-approval.ts, `Approval.decide`/`decision` in protocol/approval/fold.ts, and delegation's nested `settle`/`settleFromReply`. Their different current persistence paths are in the receipts. In particular, HEAD Wait rejection calls `WaitStore.commit` but does not persist a Wait fact; it publishes ReplyRejected. `executeWaitRoute` lazily expires `deadline_passed` and writes `recordRouteNotDelivered`. HEAD `approvals.answer` rejects an expired answer even when its timer callback has not run. HEAD delegation `settleFromReply` has no deadline check of its own. REQUEST replaces these independent logical judgments at #969; these are not claims that the new receipts already ship.

**Target `request.answer_admission` is unconditional.** Capture authenticated receipt time from the owner clock, not the sender. After validating the session lease (no stale writer may even append a refusal), apply this order: equivalent processed `inputId` -> return its original receipt; divergent reuse -> read-only invalid_transition; `receivedAt >= deadline` -> `reply.late_unknown`, and if still open, expire the request in the same batch; before deadline, binding/principal/domain/input hash mismatch -> `reply.rejected`; terminal before deadline -> `reply.duplicate`; otherwise attach/resolve through the single open-state CAS. A late answer never reopens the old request and never becomes new inbox input, even if the timer is delayed. This explicitly supersedes logical post-terminal follow-up admission for this target. Physical reply correlation is still retained.

| Source | Event + required payload | Guard / winner | Destination | Apply | Dispatch |
| --- | --- | --- | --- | --- | --- |
| absent request | request.open with complete request product, authenticated responder/binding evidence | REQUEST; original parsed invocation exists, input/effect/generation/domain hashes match; unique original intent | open on that original intent | APPLY, request action | current `deliverSend` in channels/router/messaging/send.ts; approval uses target `awaitRequestResolution` in packages/agent/src/executor-approval.ts (owner #969) |
| open | reply `{replyId,requestId,receivedAt,bindingDigest,responderId,allowedAction,content,inputHash,domainRevisions}` | REQUEST order above; distinct expected responders count; first threshold 1, quorum declared threshold, all expected count | open + attached if below threshold; resolved/answered at threshold | APPLY, reply action then request result if resolved; original open-state CAS | target `awaitRequestResolution` in packages/agent/src/executor-approval.ts (owner #969) releases current `executeBatch`; reply delivery uses WAKE |
| open | refuse `{requestId,principalEvidence,at}` | expected responder/Owner, valid binding and before deadline | refused/denied | APPLY, request result; for approval existing approval answer fact | admitted sibling calls only |
| open | timeout `{requestId,deadline,at}` | at >= deadline regardless of timer order | expired/outcome_unknown (approval call becomes blocked_pre) | APPLY, request result | no reply inbox; timed_out observation |
| open | cancel/interrupt `{requestId,at,principalEvidence}` | authenticated owner; same open-state CAS as reply/timeout | cancelled; interrupt cancels whole enclosing wave | APPLY, request result | cancel physical operation after receipt |
| any request | delivery_receipt `{requestId,sourceActionId,externalMessageId,at}` | REQUEST; authenticated physical delivery receipt binds to that request's admitted send; processed input equality first, divergent reuse read-only invalid_transition | logical state/outcome unchanged; correlation.replyToMessageId becomes externalMessageId; bindingDigest recomputed from the same original binding plus the platform id; no body admission or reopening | target commitRequestTransition via APPLY records receipt and correlation index patch atomically; replaces WaitStore.commit dependency | none |
| any request | late/duplicate/mismatched reply | unconditional order above | old state unchanged except open late request expires | APPLY records `reply.late_unknown/rejected/duplicate` once per inputId; read-only for previously processed input | none |
| source child open, terminal slot empty | terminal.seal + outbound.open `{messageId,sourceSessionId,sourceActionId,destinationSessionId,requestId,replyTo,terminal,content,digest}` | DEC; section 3 terminal accepted; sourceActionId is preminted child result id, destination is durable parentId, original request/replyTo and final text preserved | sealed child AND source-owned pending obligation; parent untouched | **ONE source APPLY transaction** appends terminal then outbound.open, consumes terminal interrupts and patches lifecycle; two action revisions, one visible snapshot | WAKE only after transaction receipt |
| source child terminal + pending obligation after process loss | recover outbound `{messageId}` | RECOVERY -> DEC; verified existing terminal/obligation pair, source lease reacquired; no durable source ack | same terminal and pending obligation; dispatch the exact recorded bytes/id even if destination already received | no source action until ack; no reconstruction or reseal | WAKE -> RCV; equivalent destination receipt is reusable |
| source child terminal + delivered obligation | recover outbound `{messageId}` | RECOVERY; durable ack exists | unchanged | none | none |
| child final terminal missing obligation | recover outbound | DEC; impossible from target atomic seal; legacy conversion must supply the pair before cutover, or refuse cutover with retained bytes | unchanged, invalid_transition with missing source obligation identified; never silently declare delivery complete | none | none |
| destination pending/no inbox yet | receive complete outbound payload | RCV; exact `(destinationSessionId,messageId,digest)`; different digest refuses | one pending inbox row + action | RCV destination transaction | DISPATCH -> current `reconcile` |
| source obligation pending | ack `{messageId,destinationReceiptId,destinationRevision}` | DEC; verified receiving receipt | delivered obligation | APPLY source ack action | none |
| any obligation | retry same message | DEC/RCV; receiving receipt equality | same inbox row, no repeated continuation | no new receiving action; source ack may still be missing | WAKE only until durable ack |

The merged wire is `MessageContract.Send`/`Sender`/`IngressFacts`/`Result` in protocol/gateway/message.ts, not the old `SendInput` API. Send takes `{to,type,content,replyTo?,deadline?}` with authenticated sender separately; `new_session.parent` is `"me"`, resolved from that sender. Ingest takes optional `reply` correlation plus `chain`, with identity/authority outside driver facts. `Result` distinguishes blocked_pre, executed, blocked_post; **only executed actor delivery** carries accepted/rejected/unknown, not a session commit. `Deliver.waitContext` remains present only for an admitted open-request resumption; a non-responder's ordinary independently admitted traffic has none. A reply already bound to an old expired request follows the late-input rule, not fallback ordinary routing. #969 owns the consuming cutover; the current `deliverSend` physical idempotency/grant/budget behavior is retained.

## 5. Alarm and occurrence transition product

Target authority is ALD/ALA/ALX through DEC/APPLY/DISPATCH. HEAD `sessionStopEvidence` only reads current actions/approvals/armed ids. `Alarm.Status` already lists four states, but there is no HEAD occurrence evaluator to cite or delete. The census assigns its existing evidence reader and alarm primitives to #971.

Canonical identities: `J` is sorted-key JSON encoding used by existing `canonicalDigest`; all keys below are **UTF-8 `canonicalKey` bytes**, not ad hoc string concatenation. Active key is `K([sessionId,kind,controlKey])`, where `controlKey` is the authenticated caller's stable monitor identity, stored in spec; a scheduled one-shot defaults to `K([fireAt,spec.value ?? null])`. Armed, paused, and fired-with-open-occurrence reserve this key. A cancelled or completed one-shot releases it. Rearm uses the same alarm id, increments epoch, and replaces its reservation atomically; another alarm reserving the key refuses `active_key_conflict`.

Transport duplicate identity is `(alarmId,epoch,sourceKey)`, with `sourceKey` captured from a timer fire slot or backend event/cursor. Repeating it returns the recorded occurrence receipt; differing payload under it refuses. Consecutive polling suppression is a separate tuple `(alarmId,epoch,lastBatchHash)`; equality produces a **new terminal deduped occurrence** only for a newly observed sourceKey. It does not overwrite the previous occurrence's terminal. Takeover preserves epoch, cursor and lastBatchHash. Explicit rearm increments epoch and clears lastBatchHash/cursor only according to the admitted source restart mode, so an explicitly restarted poll A may notify again. Wake key is `(destinationSessionId,occurrenceId)`; duplicate wake returns the original RCV receipt.

Every alarm control command includes `{alarmId,expectedAlarmRevision,epoch,at,owner,fence}`; every evaluator command includes those identity/revision fields plus `{evaluatorOwner,evaluatorFence,sourceKey,batchHash,cursorBefore,cursorAfter}`. Session lease is acquired before ALA mutates session actions, even while the session's runner sleeps. Evaluator lease alone cannot write session history. All control mutations fence evaluator callbacks by incrementing evaluator fence and clearing owner/expiry before physical cleanup.

**Alarm refusal precedence (first match wins):** (1) malformed/missing/mismatched alarm/occurrence identity -> invalid_transition; (2) invalid session authority or old/expired evaluator identity/fence -> stale, before terminal comparison or dedupe (acquire/takeover checks expectedEvaluatorFence instead of requiring a live evaluator); (3) previously processed inputId -> original receipt on equal payload, invalid_transition on divergent reuse; (4) old epoch -> stale; repeated transport sourceKey for fire/new-poll only -> original receipt on equal payload, invalid_transition on divergent reuse; (5) a result for a terminal occurrence -> original receipt on equal result digest, terminal_conflict otherwise; (6) differing expected session/alarm revision -> revision refusal; (7) event-specific guards below, then the catch-all. A control replay of the identical rearm input therefore returns its receipt even though that control advanced epoch; a fresh input with old epoch is stale. Thus an old e/f1 completion of cancelled O1 in A_PAUSED is stale, never terminal_conflict. A fresh current evaluator's differing result for a terminal occurrence is terminal_conflict. Before terminality, at >= occurrence deadline selects expiry even for a failed/completed result; only earlier results can fail, complete or consume notification budget. A fire for a new sourceKey with equal lastBatchHash takes the deduped branch, not the ordinary fire branch.

| Source product | Event + additional payload | Guard | Destination and recorded payload | Apply / dispatch |
| --- | --- | --- | --- | --- |
| absent alarm | arm `{sessionId,kind,controlKey,mode,source,fireAt,spec,notificationLimit,deadline,generation}` | authenticated session owner; active key unreserved; live source recovery mode valid | armed, epoch 1, alarmRevision 1, evaluator unleased/fence 0; record full `alarm.arm` | ALA / ALX installs due scan or source |
| armed/fired + evaluator unleased/expired | acquire/takeover `{expectedEvaluatorFence,expiresAt}` | expected fence exact, expiresAt > at; old owner absent or expiry <= at; source at, first live-source acquisition without host loss, or explicitly admitted idempotent_poll restart; cursor restart requires valid cursor; ineligible recovery is excluded | same alarm status/epoch/cursor/dedupe; evaluator owner=new, fence+1, expiry=new; row-only CAS | ALA / ALX starts evaluator from committed cursor |
| fired alarm + evaluate occurrence after evaluator loss | recover evaluation `{occurrenceId,oldEvaluatorFence,newEvaluatorFence,restartMode}` | eligible takeover CAS won; source explicitly idempotent_poll with valid cursor or restart-from-now; result slot empty; at < deadline | same occurrence/epoch, evaluate at new fence; record `occurrence.recover`; ineligible takeover uses only the recovery_unavailable row below | ALA / ALX restarts only that admitted evaluation |
| any live evaluator | renew `{expiresAt}` | same owner/fence; old expiry > at; source alarm still armed/fired | same fence, extended expiry; row-only CAS | ALA / none |
| armed | fire `{fireAt,occurrenceId,parentActionId,inputHash,generation,deadline}` | due at <= now, current evaluator + session lease, no existing sourceKey; not a consecutively equal poll batch | fired + occurrence open; `alarm.fired {alarmId,epoch,sourceKey,occurrenceId,fireAt}` and full `occurrence.open` in one batch | ALA / none until evaluate admitted |
| open occurrence + fired alarm | evaluate `{occurrenceId}` | both leases valid; before occurrence deadline; not paused/cancelled; evaluation slot still open | occurrence evaluate; `occurrence.evaluate {occurrenceId,evaluatorFence}` | ALA / ALX invokes evaluator once |
| armed alarm + no occurrence for this sourceKey | duplicate new poll batch `{newSourceKey,batchHash}` | both leases valid; new source identity, hash equals committed lastBatchHash | alarm remains armed; new open occurrence -> terminal deduped; previous terminal intact; `occurrence.result {terminal:"deduped",reason:"consecutive_batch"}`; no notification budget debit | ALA / none |
| evaluate occurrence + fired alarm | result `{occurrenceId,terminal:"completed",output,cursorAfter,nextAt}` | current fences; result slot empty; notification budget available; occurrence deadline not reached | terminal completed, cursor/hash committed; one source outbound wake; once -> fired completed/release active key; recurrent -> armed at nextAt > at | ALA commits result + outbound + recurrent rearm action / WAKE; ALX reschedules recurrent source |
| open/evaluate + deadline passed | expire, result or recover evaluation `{occurrenceId,deadline,nextAt?}` | at >= deadline, current fences, result slot empty; expiry precedes result/budget/restart handling | terminal expired, no wake; once stays fired and releases active key; recurrent with valid nextAt > at becomes armed, otherwise pauses/resumeFrom fired; paused evaluator fenced | ALA / ALX source cleanup/reschedule |
| evaluate + failure | result `{occurrenceId,terminal:"failed",errorName,reason}` | current fences, result slot empty, at < deadline | terminal failed, alarm paused, resumeFrom=fired, evaluator fenced; failure + pause actions | ALA / ALX cleanup |
| armed/fired | completed result over budget / owner pause `{reason,occurrenceId?}` | explicit owner pause command, or completed result with empty slot, at < deadline and budget exhausted; serialized at session/alarm revision; automatic excess emits only the first pause after N notifications | paused with resumeFrom previous status; open/evaluate occurrence cancelled; pauseNotified=true; one pause notification; evaluator fenced | ALA / ALX cleanup; WAKE for the one pause receipt |
| paused | resume `{nextAt,restartMode}` | only authenticated session owner may request; expected revision/epoch; no nonterminal occurrence; nextAt >= at; supported source restart mode | armed, same epoch/hash/cursor, pauseNotified=false; new evaluator acquisition required; record `alarm.control {op:"resume",nextAt}` | ALA / ALX restart |
| armed/fired with all occurrences terminal; or paused | explicit rearm `{nextAt,spec,restartMode}` | authenticated session owner; nextAt > at; source permitted; key not reserved by another alarm | armed, epoch+1, notifications=0, pauseNotified=false; reset cursor/hash only if explicit restart-from-now; full new spec and nextAt recorded | ALA / ALX restart |
| armed/fired/paused | cancel `{reason}` | authenticated session owner; revision/epoch exact | cancelled; open/evaluate occurrences terminal cancelled; evaluator fenced; active key released | ALA / ALX closes owned source |
| cancelled | resume/rearm/fire | source ineligible | read-only invalid_transition; a new alarm requires new arm identity | none / none |
| paused | fire/evaluate/result from old evaluator | precedence step 2, even if occurrence already terminal | read-only stale; zero occurrence/result/wake | none / none |
| terminal occurrence | identical result | precedence steps 1-4 passed; exact result digest equals original | original terminal and receipt, no appended action | none / none |
| terminal occurrence | differing result | precedence steps 1-4 passed; same slot already terminal | read-only terminal_conflict | none / none |
| armed/fired, evaluator absent/expired, live-only source after host loss or unusable cursor | takeover | current session lease/revision/epoch/expectedEvaluatorFence; source restart not admitted (disjoint from eligible acquire/takeover); no won takeover required | atomically paused/reason recovery_unavailable/resumeFrom previous status, pauseNotified=false, alarmRevision+1; evaluator fence+1/owner null/expiry null; each open/evaluate occurrence -> terminal cancelled/reason recovery_unavailable; cursor/hash/epoch and existing outbound unchanged | ALA appends `alarm.paused` and each `occurrence.result {state:"terminal",terminal:"cancelled",reason:"recovery_unavailable"}` in ONE batch / ALX cleanup; no new wake |
| all remaining products/events | any schema-valid command | no earlier precedence/transition applies | unchanged; active_key_conflict for a reserved arm/rearm key, otherwise invalid_transition | none / none |

Only session-authorized control can pause/resume/rearm/cancel; evaluator outcomes may request the specific policy pause above through DEC. Timers and due readers cannot grant authority. N+1 concurrent notifications serialize to N completed notifications plus one pause, with every stale contender writing zero. One-shot completed alarms can be explicitly rearmed into a new epoch, but cannot automatically recur.

## 6. Deterministic trace harness

### 6.1 Complete notation, fixtures, and observations

These tables are normative vectors for HARNESS, not claims that target request/alarm logic runs at HEAD. The current session and four executor wave counts were also exercised directly during authoring. Every trace starts at **r0**. The session fixture starts before `session.configure` at r1; an executor-only fixture starts with a captured session snapshot at r0 whose earlier history is outside that fixture. Operational lease CAS has zero action revisions and is always listed separately. A transaction containing two actions has two numbered receipts but one externally visible post-transaction snapshot; intermediate ordinals are not advertised as independently committed snapshots.

Use the following exact expansion rules (each row refers to a fully determined object):

- `H(v)=canonicalDigest(v)`, `E(v)={encodingVersion:1,value:v}`.
- `a(id,parent,kind,intent,effect,t=1000)={id,parentId:parent,sessionId:"S",kind,intent:E(intent),effect:E(effect),irreversible:true,ts:t}`. Action ordinal equals its tabulated revision. Configure alone replaces irreversible with `revert:E({generation:previous})`.
- `G1={generation:1,revertTo:0,tools:[],toolsHash:H([]),systemPreset:"",systemBlocks:[],systemValue:"",systemHash:H([]),policyGeneration:1}`. `G2` is identical except generation 2/revertTo 1/systemBlocks `[{id:"b",source:"fixture",content:"v2"}]`, systemValue `"v2"`, systemHash `H(G2.systemBlocks)`. Tools hash is unchanged; no arbitrary hash marker.
- Origin `O=E({source:"fixture"})`. `in(id,k,text,n,t,status,by=null,at=null)` expands to `{id,sessionId:"S",kind:k,content:text,origin:O,ordinal:n,createdAt:t,status,consumedBy:by,consumedAt:at}`. `P=in("P","prompt","hello",1,1000,"pending")`; `Pc` replaces status consumed/by `"o"`/at 1000. `I` and `Ic` are interrupt id `I`, text empty, ordinal 2; `U/Uc` resume id `U`, ordinal 3, created/consumed time 1050. All rows, including consumed rows, remain in snapshot inbox.
- `S(r,state,lease,generation,turn,inbox,operations=[],requests=[],alarms=[],occurrences=[],outbound=[],retained=[])` is the complete named harness snapshot: durable row `{id:"S",parentId:null,role:"resident",revision:r,state,leaseOwner:lease[0],leaseFence:lease[1],leaseExpiresAt:lease[2],toolsGeneration:generation.generation,systemHash:generation.systemHash,policyGeneration:generation.policyGeneration}`, generation snapshot, turn product, exact inbox and region lists, runtime `{handle:lease[0]===null && inbox.every(x=>x.status==="consumed") && retained.length===0 ? "absent":"resident",retained}`, and `actions` equal the exact ordered row-prefix action list. Tail projection is defined below. Lists not explicitly supplied are empty, never unknown.
- `L0=[null,0,null]`, `L1=["o",1,31000]`, `F1=[null,1,null]`, `L2=["o",2,31050]`, `F2=[null,2,null]`.
- `T={turnId:"T",resultId:"R",resumeCount:0,boundaryActionId:"ppost",toolsGeneration:1,toolsHash:H([]),systemHash:H([]),policyGeneration:1,state:"open",terminal:null}`. `end(T,k,text)` keeps all identity fields, changes state terminal, terminal k, text. Tail is `{turnId,state:terminal==="interrupted"?"interrupted":terminal?"idle":"running",startedAt:1000,messages:[{role:"user",text:"hello"},...(text?[{role:"assistant",text}]:[])],...(terminal?{terminal:{kind:terminal,actionId:resultId,at:terminalAt}}:{})}`. A second turn retains the first tail. Crash fixture has no input messages. `none` is null.
- `policy(id,parent,kind,phase,op,value,verdict="allow",rules=[],reason=null,t=1000)` expands to `a(id,parent,"policy.decision",{hook:kind+"."+phase,op,generation:1,matchedRuleIds:rules,verdict,inputHash:H({kind,phase,op,role:"resident",sessionId:"S",value})},{phase:"result",reason},t)`. Thus hashes cover the **whole policy input**, not only tool arguments.
- `Obs(a,r)={name:"ledger.action.committed",payload:{id:a.id,sessionId:"S",revision:r,kind:a.kind}}`. Each action row expects exactly `[Obs(a,r)]` from the selected storage observation sink. Executor-only fixture uses X's sink with its ledger sink disabled; production's two possible notification paths are not counted as two actions. No-action row expects `[]`. Tool observations are additional: `Started(k,t)={name:"tool.execution.started",payload:{traceId:"T",sessionId:"S",runId:"T",toolCallId:k,toolName:k,time:t}}`; `Completed(k,t)={name:"tool.execution.completed",payload:{traceId:"T",sessionId:"S",runId:"T",toolCallId:k,toolName:k,time:t,durationMs:0,isError:false}}`.
- Effects are a separate dispatched list, not the historical effect payload: `body(k)={kind:"tool.body",callId:k,input:{value:k}}`; `wake(id)={kind:"inbox.receive",messageId:id,destinationSessionId:"S"}`. Replay must equal the **named snapshot** including historical actions/effect evidence while its dispatched list and observations are `[]`.

For snapshots, `with` means a shallow replacement of exactly the named fields on the previously complete object; all unmentioned fields and prior tails/actions remain. `end` derives terminalAt from the listed terminal action timestamp. The replay fixture consists of the seed, exact committed action list, and the recorded coordination/input tape (lease acquire/renew/release, process loss, retained-runner settlement, body completions). It folds recorded coordination values instead of reacquiring a live lease or recreating a runtime. Process loss sets handle absent; runner entry sets it resident; explicit hibernate sets it absent. This distinction is needed to compare residency/lease snapshots as well as action-derived history.

### 6.2 Ordinary, terminal interrupt, resume, crash-open

No model/provider is called by this fixture: the actual session runner is an explicit result/abort gate. Its prompt pre/post and turn pre/post still append policy actions. All times 1000 except where specified. Entropy is the finite id queue named by rows, with the owner token separately fixed to `o`. `pval={inboxId:"P",content:"hello",origin:{source:"fixture"},createdAt:1000,ordinal:1}`; `tval={turnId:"T",resultId:"R",resumeCount:0,resume:false,toolsGeneration:1,toolsHash:H([]),systemHash:H([]),policyGeneration:1}`.

| Input / action | Revision | Complete expected snapshot name and value | Winner; additional dispatch |
| --- | --- | --- | --- |
| `materialize {id:"S",parentId:null,role:"resident",tools:[],system:{preset:"",blocks:[]},policyGeneration:1,actionId:"cfg",at:1000}` -> configure `a("cfg",null,"session.configure",{operation:"create"},{phase:"configured",snapshot:G1})` with revert generation 0 | r1 | `BASE=S(1,"idle",L0,G1,none,[])` | creation; [] |
| input `commitInbox {id:"P",sessionId:"S",kind:"prompt",content:"hello",origin:O,createdAt:1000,parentActionId:"cfg"}` -> `a("P","cfg","prompt",O.value,{inboxKind:"prompt",content:"hello"})` | r2 | `QUEUED=S(2,"idle",L0,G1,none,[P])` | first id; [] |
| acquire `{sessionId:"S",owner:"o",expectedFence:0,now:1000,expiresAt:31000}` | r2 | `LEASED=S(2,"idle",L1,G1,none,[P])` | `{ok:true,fence:1}`; [] |
| `policy("ppre","P","prompt","pre","inbox",pval)` | r3 | `PPRE=S(3,"idle",L1,G1,none,[P])` | pre allow; [] |
| `policy("ppost","P","prompt","post","inbox",{intent:pval,effect:{status:"recorded"},result:{inboxId:"P",status:"recorded"}})` | r4 | `PPOST=S(4,"idle",L1,G1,none,[P])` | post allow; [] |
| one batch: `a("P:delivery","ppost","inbox.deliver",{inboxId:"P"},{phase:"delivery",turnId:"T",inboxId:"P",kind:"prompt",content:"hello",origin:O,boundary:"before_llm"})`, then `a("T","P:delivery","turn",{phase:"intent",resultId:"R",inboxIds:["P"],resumeCount:0,boundaryActionId:"ppost",toolsGeneration:1,toolsHash:H([]),systemHash:H([]),policyGeneration:1},{phase:"pending"})`; consume P | r5, r6 | both notifications observe `OPEN=S(6,"running",L1,G1,T,[Pc])` | admission CAS; [] |
| `policy("tpre","T","turn","pre","session",tval)`; runner entry gate resolves | r7 | `RUN=S(7,"running",L1,G1,T,[Pc])` | turn pre allow; runner entry, tool effects [] |
| release runner gate `{kind:"result",text:"done"}` -> `policy("tpost","T","turn","post","session",{intent:tval,effect:{terminal:"sealed"},result:{kind:"result",text:"done"}})` | r8 | `POST=S(8,"running",L1,G1,T,[Pc])` | result; [] |
| `a("R","tpost","turn",{phase:"terminal",turnId:"T"},{phase:"terminal",turnId:"T",kind:"result",text:"done",boundaryActionId:"ppost",resumeCount:0})`; release | r9 | `DONE=S(9,"idle",F1,G1,end(T,"result","done"),[Pc])` | R terminal once; [] |

Interrupt branches from `RUN/r7`, **not** from DONE. Its preminted result is still `R`:

| Input / exact action | Revision | Snapshot | Winner; dispatch |
| --- | --- | --- | --- |
| `commitInbox(I,parentActionId:"tpre")` -> `a("I","tpre","prompt",O.value,{inboxKind:"interrupt",content:""})` | r8 | `IQ=S(8,"running",L1,G1,T,[Pc,I])` | input commit; [] |
| interrupted-state C with actions `[]`, consume `[]`, release false; then abort | r8 | `IS=S(8,"interrupted",L1,G1,T,[Pc,I],[],[],[],[],[],["raw-runner"])` | interrupt state; abort |
| runner abort outcome -> `policy("tpost","T","turn","post","session",{intent:tval,effect:{terminal:"sealed"},result:{kind:"interrupted",text:""}})` | r9 | `IP=S(9,"interrupted",L1,G1,T,[Pc,I],[],[],[],[],[],["raw-runner"])` | abort; [] |
| one batch `a("I:delivery","tpost","inbox.deliver",{inboxId:"I"},{phase:"delivery",turnId:"T",inboxId:"I",kind:"interrupt",content:"",origin:O,boundary:"before_llm"})`, `a("R","I:delivery","turn",{phase:"terminal",turnId:"T"},{phase:"terminal",turnId:"T",kind:"interrupted",text:"",boundaryActionId:"ppost",resumeCount:0})`; consume I, release false | r10, r11 | `INTERRUPTED_HELD=S(11,"interrupted",L1,G1,end(T,"interrupted",""),[Pc,Ic],[],[],[],[],[],["raw-runner"])` | R interrupt terminal; [] |
| old runner returns `{kind:"result",text:"late"}`; attempts write under sealed turn | r11 | exactly `INTERRUPTED_HELD` | `{ok:false,reason:"stale",currentFence:1,currentRevision:11}` from captured execution ledger; [] |
| settle exact raw-runner/effect promise, releaseHeldLease zero actions; hibernate | r11 | `INTERRUPTED=S(11,"interrupted",F1,G1,end(T,"interrupted",""),[Pc,Ic])` | release not another terminal; [] |

Resume branches from INTERRUPTED, adds a configure at 1050, and does not discard old inbox/tail/history. `T2` is T with turnId `T2`, resultId `R2`, resumeCount 1 and G2 pointers; boundary stays ppost. `tval2` is tval with those ids/pointers, resumeCount 1, resume true.

| Input/actions | Exact revisions | Snapshot | Winner; dispatch |
| --- | --- | --- | --- |
| acquire expected fence 1 at 1050 -> fence2; configure `a("cfg2","R","session.configure",{operation:"system.blocks.set"},{phase:"configured",snapshot:G2},1050)` with revert1; release | r12 | `NEWGEN=S(12,"interrupted",F2,G2,end(T,"interrupted",""),[Pc,Ic])` | configure; [] |
| resume input -> `a("U","cfg2","prompt",O.value,{inboxKind:"resume",content:""},1050)` | r13 | `RESUME_QUEUED=S(13,"interrupted",F2,G2,end(T,"interrupted",""),[Pc,Ic,U])` | input; [] |
| acquire expected fence2 at1050 -> fence3; batch U:delivery parent R, content empty/origin O/turnId T2/boundary before_llm, then T2 intent parent U:delivery/inboxIds[U]/new result R2/G2/resumeCount1/boundary ppost; consume U | r14, r15 | `RESUMED=S(15,"running",["o",3,31050],G2,T2,[Pc,Ic,Uc])` | new ids/latest generation; runner entry |
| `policy("tpre2","T2","turn","pre","session",tval2,"allow",[],null,1050)` | r16 | `RESUME_RUN=S(16,"running",["o",3,31050],G2,T2,[Pc,Ic,Uc])` | pre; [] |
| result `{kind:"result",text:"resumed"}` -> post `policy("tpost2","T2","turn","post","session",{intent:tval2,effect:{terminal:"sealed"},result:{kind:"result",text:"resumed"}},"allow",[],null,1050)` | r17 | `RESUME_POST=S(17,"running",["o",3,31050],G2,T2,[Pc,Ic,Uc])` | post; [] |
| R2 terminal parent tpost2/turnId T2/kind result/text resumed/boundary ppost/resumeCount1/ts1050, release | r18 | `RESUME_DONE=S(18,"idle",[null,3,null],G2,end(T2,"result","resumed"),[Pc,Ic,Uc])` | R2 only; [] |

Crash-open is a separate r0 vector. Use the following complete action inputs: `cfg=a("cfg",null,"session.configure",{operation:"create"},{phase:"configured",snapshot:G1})` with `revert:E({generation:0})`; `T=a("T","cfg","turn",{phase:"intent",resultId:"R",inboxIds:[],resumeCount:0,boundaryActionId:"cfg",toolsGeneration:1,toolsHash:H([]),systemHash:H([]),policyGeneration:1},{phase:"pending"})`; and `cfg2={id:"cfg2",parentId:"T",sessionId:"S",kind:"session.configure",intent:E({operation:"system.blocks.set"}),effect:E({phase:"configured",snapshot:G2}),revert:E({generation:1}),ts:1001}`. Acquire `{sessionId:"S",owner:"dead",expectedFence:0,now:1000,expiresAt:1010}`; commit cfg/T/cfg2 at r1/r2/r3, so **T remains pinned to G1** while the row points at G2. At 2000 acquire `{sessionId:"S",owner:"o",expectedFence:1,now:2000,expiresAt:32000}`. No runner remains in-process. Recovery action is `resumeT={id:"resume",parentId:"cfg",sessionId:"S",kind:"turn",intent:E({phase:"resume",turnId:"T",resultId:"R",resumeCount:1,boundaryActionId:"cfg",toolsGeneration:1,toolsHash:H([]),systemHash:H([]),policyGeneration:1}),effect:E({phase:"pending"}),irreversible:true,ts:2000}`. Let `TC` be T with boundary cfg, resumeCount1, no messages, still G1. `cv` is tval with resume true/resumeCount1.

| Input/actions | Exact revisions | Snapshot | Winner; dispatch |
| --- | --- | --- | --- |
| commit `cfg`, `T`, and `cfg2` above; process exits | r1,r2,r3 | `CRASH_OPEN=S(3,"running",["dead",1,1010],G2,T-with-boundary-cfg,[])` | no terminal |
| takeover at2000 | r3 | `CRASH_LEASED=S(3,"running",["o",2,32000],G2,T-with-boundary-cfg,[])` | fence2 |
| commit `resumeT` above | r4 | `CRASH_RESUMED=S(4,"running",["o",2,32000],G2,TC,[])` | same T/R/G1; runner entry |
| `policy("cpre","T","turn","pre","session",cv,"allow",[],null,2000)`; `policy("cpost","T","turn","post","session",{intent:cv,effect:{terminal:"sealed"},result:{kind:"result",text:"recovered"}},"allow",[],null,2000)` | r5,r6 | `CRASH_PRE=S(5,"running",["o",2,32000],G2,TC,[])`; `CRASH_POST=S(6,"running",["o",2,32000],G2,TC,[])` | allow |
| `a("R","cpost","turn",{phase:"terminal",turnId:"T"},{phase:"terminal",turnId:"T",kind:"result",text:"recovered",boundaryActionId:"cfg",resumeCount:1},2000)`; release | r7 | `CRASH_DONE=S(7,"idle",[null,2,null],G2,end(TC,"result","recovered"),[])` | R once |
| old owner dead/fence1 attempts new action | r7 | exactly CRASH_DONE | `{ok:false,reason:"stale",currentFence:2,currentRevision:7}`; [] |

### 6.3 Whole-wave vectors

This fixture uses actual `createExecutor.runBatch` with four parsed requests; it deliberately excludes prompt/turn wrappers. Its r0 seed is `S(0,"running",["o",7,31000],G1,T,[])` with T recorded before the fixture range; earlier actions are immutable seed data. Each request is `{kind:"tool",op:k,intent:{value:k},effect:{category:"query"},toolObservation:{turnId:"T",callId:k}}` for k=A,B,C,D; only D's **batch item**, not Tool.Call, is sequential. Bodies return `{status:"success",output:k}`. B's sole pre rule is named `B`, requires approval, reason `owner`; all other buckets allow. No model-facing `toolResult` callback is supplied in this executor fixture. End-to-end dispatcher tests additionally compare original model slots, in section 7.

For all rows, the complete per-region snapshot is `W(r,ops,approval)=S(r,"running",["o",7,31000],G1,T,[],ops,approval)`. `ops` lists A,B,C,D in order and abbreviates only their phase; all identity/input/effect fields are fixed above. Prepared=p, pre_decided=d, admitted=a, awaiting_approval=w, post=h, executed=e, blocked_pre=b, cancelled=c. The table's operation phase is the committed projection; physical body-entry/completion is recorded on the separate fixture input tape and does not prematurely set a durable terminal. Durable terminal is set only at the result action, not at post-policy or answer.

| Action payload | Revision | Named full snapshot |
| --- | --- | --- |
| `policy("x1","T","tool","pre","A",{value:"A"})` | r1 | `W1=W(1,[d,p,p,p],[])` |
| `policy("x2","T","tool","pre","B",{value:"B"},"require_approval",["B"],"owner")` | r2 | `W2=W(2,[d,d,p,p],[])` |
| `policy("x3","T","tool","pre","C",{value:"C"})` and `policy("x4","T","tool","pre","D",{value:"D"})` | r3,r4 | `W3=W(3,[d,d,d,p],[])`; `W4=W(4,[d,d,d,d],[])` |
| `a("x5","T","tool",{phase:"intent",op:"A",value:{value:"A"}},{phase:"pending"})`; `a("x6","T","tool",{phase:"intent",op:"B",value:{value:"B"}},{phase:"pending"})`; `a("x7","T","tool",{phase:"intent",op:"C",value:{value:"C"}},{phase:"pending"})`; `a("x8","T","tool",{phase:"intent",op:"D",value:{value:"D"}},{phase:"pending"})` | r5,r6,r7,r8 | `W5=W(5,[a,d,d,d],[])`; `W6=W(6,[a,a,d,d],[])`; `W7=W(7,[a,a,a,d],[])`; `W8=W(8,[a,a,a,a],[])` |
| `a("x9","x6","policy.decision",{phase:"approval",op:"request"},{state:"pending",request:Q})` | r9 | `WAIT=W(9,[a,w,a,a],[Q])`; bodies/Started/Completed `[]` |

`Q={id:"x6",sessionId:"S",turnId:"T",toolsHash:H([]),toolsGeneration:1,callId:"B",inputHash:H({value:"B"}),generation:1,revision:2,policyDecisionId:"x2",intent:{value:"B"},expiresAt:1100}`. Every following branch starts at WAIT with the complete r1-r9 prefix. Let `post(id,k)=policy(id,"T","tool","post",k,{intent:{value:k},effect:{category:"query"},result:{status:"success",output:k}})`; note parent is T, not the intent. `result(id,k,parent)=a(id,parent,"tool",{phase:"result",op:k},{phase:"result",terminal:"executed",effect:{category:"query"},resultHash:H({status:"success",output:k}),result:{status:"success",output:k},callId:k})`. `blocked(id,reason)=a(id,"x6","tool",{phase:"result",op:"B"},{phase:"result",terminal:"blocked_pre",reason,callId:"B"})`; `cancel(id,k,parent)` is the same result envelope with `{phase:"result",terminal:"cancelled",callId:k}`. These are exact parameterized object constructors over the finite rows, not unbound payload fields.

| Branch/trigger/action | Revision | Exact snapshot, additional observation/effect list; winner |
| --- | --- | --- |
| approve credential owner-token, complete Q unchanged: `a("x10","x6","policy.decision",{phase:"approval",op:"answer"},{decision:"approve",evidence:{kind:"owner",principalId:"owner",evidenceId:"credential"},request:Q})` | r10 | `APPROVED=W(10,[a,a,a,a],[])`; answer CAS wins before bodies |
| enter A,B,C, release their gates in order C,A,B, then D entry/settlement | r10 throughout | `BODIES=W(10,[a,a,a,a],[])`; raw body-return tape `[C,A,B,D]`; Started `[A,B,C,D]`, all at r10; effects `[body(A),body(B),body(C),body(D)]`; D starts after B's raw settlement, BEFORE any post/result action; Completed `[]` |
| post x11/A; result x12/A/x5 | r11,r12 | `WA11=W(11,[h,a,a,a],[])`; `WA12=W(12,[e,a,a,a],[])`; Completed A after x12 |
| post x13/B; result x14/B/x6 | r13,r14 | `WA13=W(13,[e,h,a,a],[])`; `WA14=W(14,[e,e,a,a],[])`; Completed B after x14 |
| post x15/C; result x16/C/x7 | r15,r16 | `WA15=W(15,[e,e,h,a],[])`; `WA16=W(16,[e,e,e,a],[])`; Completed C after x16 |
| post x17/D; result x18/D/x8 | r17,r18 | `WA17=W(17,[e,e,e,h],[])`; **`WAVE_APPROVED=W(18,[e,e,e,e],[])`**; Completed D; returned array `[{terminal:"executed",value:{status:"success",output:"A"}}, {terminal:"executed",value:{status:"success",output:"B"}}, {terminal:"executed",value:{status:"success",output:"C"}}, {terminal:"executed",value:{status:"success",output:"D"}}]` |
| refuse: same x10 answer action with decision refuse | r10 | `REFUSED=W(10,[a,w,a,a],[])` with Q resolution refuse; no body B |
| timeout: schedule callback captured before trigger; advance clock1100, first attempt answer with Q, then invoke callback -> `a("x10","x6","policy.decision",{phase:"approval",op:"timeout"},{decision:"timeout",request:Q,evidence:{kind:"deadline",at:1100,expiresAt:1100}},1100)` | r9 unchanged on answer; r10 timeout | stale answer throws `ExecutionApprovalError.code="stale_approval"`, no action/effect; `TIMED=W(10,[a,w,a,a],[])` with Q resolution timeout; delayed timer cannot admit B |
| either refusal/timeout: enter A,C; release C then A; D enters then settles | r10 throughout | same REFUSED/TIMED product; raw body-return tape `[C,A,D]`; Started `[A,C,D]`, all at r10; effects `[body(A),body(C),body(D)]`; D enters after A's raw settlement and BEFORE x11; Completed `[]` |
| post x11/A; result x12/A/x5; blocked x13 with reason approval_refused or approval_timeout | r11,r12,r13 | `WD11=W(11,[h,w,a,a],[])`; `WD12=W(12,[e,w,a,a],[])`; `WD13=W(13,[e,b,a,a],[])`; Completed A after x12; no B observation |
| post x14/C, result x15/C/x7, post x16/D, result x17/D/x8 | r14,r15,r16,r17 | `WD14=W(14,[e,b,h,a],[])`; `WD15=W(15,[e,b,e,a],[])`; `WD16=W(16,[e,b,e,h],[])`; **`WAVE_REFUSED/WAVE_TIMEOUT=W(17,[e,b,e,e],[])`**; Completed C after x15, D after x17; returned slots A success, B `{terminal:"blocked_pre",reason}`, C success, D success |
| interrupt while WAIT; pre-register abort/terminal promises, trigger abort, no answer/timeout receipt; cancel x10/A/x5, x11/B/x6, x12/C/x7, x13/D/x8 | r10,r11,r12,r13 | `WC10=W(10,[c,w,a,a],[])`; `WC11=W(11,[c,c,a,a],[])`; `WC12=W(12,[c,c,c,a],[])`; **`WAVE_INTERRUPTED=W(13,[c,c,c,c],[])`**; returned array four `{terminal:"cancelled"}`; all body/tool observation lists []; interrupt wins |

Every post-gate timestamp is 1100 in the timeout branch and 1000 in the others, including policy/result hashes' time-independent input values and tool observation time. Obs is emitted for every numbered action. There are 18/17/17/13 actions and 4/3/3/0 body effects, respectively. The body-return tape is **not** Tool Completed. Exact observation order after x10 is Started A,B,C,D then Obs(x11), Obs(x12), Completed A, Obs(x13), Obs(x14), Completed B, Obs(x15), Obs(x16), Completed C, Obs(x17), Obs(x18), Completed D for approval. Refusal/timeout is Started A,C,D then Obs(x11), Obs(x12), Completed A, Obs(x13), Obs(x14), Obs(x15), Completed C, Obs(x16), Obs(x17), Completed D. All bodies settle before the first post action; no B Tool observation is emitted when blocked, and interrupt-at-gate emits none. Session wrapper integration adds cfg/P/prompt policies/delivery/turn/turn.pre (7 actions), then this 18/17/17/13-action range, then turn.post and terminal (2): final revisions **27/26/26/22**, with interrupt input and its delivery adding two more only in the integrated interrupt trace (24).

### 6.4 Request race vectors (target)

Inputs are source-owned request transitions, not old Wait stream revisions. Start at r0, materialize cfg exactly as in 6.2 at r1; acquire owner o/expectedFence0/now1000/expiresAt31000 -> L1 (zero actions). Append the crash fixture's original T intent at r2, `policy("q-pre","T","tool","pre","B",{value:"B"})` at r3, and `a("q","T","tool",{phase:"intent",op:"B",value:{value:"B"}},{phase:"pending"})` at r4. This is the actual immutable invocation prefix, not a missing-parent placeholder. `QT=T with boundaryActionId:"cfg"`, tail messages `[]`. The operation product is `QB={waveId:"q",turnId:"T",calls:[{callId:"B",index:0,op:"B",parsedInput:{value:"B"},inputHash:H({value:"B"}),effectHash:H({category:"query"}),sequential:false,preDecisionId:"q-pre",intentId:"q",phase:"admitted",terminal:null}]}`. `QS(r,requests)=S(r,"running",L1,G1,QT,[],[QB],requests)` includes that four-action prefix and every subsequent action. The open-request action id is **q-open, not q**; q remains the original intent and request id.

`Q0={requestId:"q",sessionId:"S",turnId:"T",callId:"B",parsedInput:{value:"B"},inputHash:H({value:"B"}),effectHash:H({category:"query"}),generation:1,toolsHash:H([]),domainRevisions:{person:4},deadline:1100,expectedResponders:["worker"],correlation:{channelId:"ch",replyToMessageId:"platform-1"},bindingDigest:H(["q","worker","ch","platform-1"]),resolution:"first",threshold:1,seenReplyIds:[],replies:[],state:"open",outcome:null}`. `validAnswer={replyId:"reply-1",requestId:"q",receivedAt:1099,bindingDigest:Q0.bindingDigest,responderId:"worker",allowedAction:"report_result",content:"ok",inputHash:Q0.inputHash,domainRevisions:{person:4}}`; authenticated evidence matches worker. `lateAnswer=validAnswer with receivedAt:1100`. Each action is defined **once** here; the table uses these objects without changing their bytes:

- `qopen=a("q-open","q","request",{op:"open",request:Q0},{state:"open"})`.
- `qreply=a("reply-1","q","reply",{op:"answer_admission",reply:validAnswer},{state:"attached",replyId:"reply-1"},1099)`.
- `qresolve=a("q-result","q","request",{op:"resolve",requestId:"q"},{state:"resolved",outcome:"answered",replyIds:["reply-1"]},1099)`.
- `qtimer=a("timer-1","q","request",{op:"expire",requestId:"q",deadline:1100},{state:"expired",outcome:"outcome_unknown"},1100)`.
- `qcancel=a("cancel-1","q","request",{op:"cancel",requestId:"q",at:1099,principalEvidence:{kind:"owner",principalId:"owner",evidenceId:"credential"}},{state:"cancelled",outcome:"cancelled"},1099)`.
- `qlate=a("reply-1","q","reply",{op:"late_unknown",reply:lateAnswer},{requestId:"q",receivedAt:1100,deadline:1100,bindingDigest:Q0.bindingDigest},1100)`.
- `qduplicate=a("reply-1","q","reply",{op:"duplicate",reply:validAnswer},{requestId:"q",winner:"cancel-1"},1099)`.
- For the three independent reject branches, `badWho=validAnswer with replyId:"reject-who",responderId:"stranger"`; `badInput=validAnswer with replyId:"reject-input",inputHash:H({value:"altered"})`; `badDomain=validAnswer with replyId:"reject-domain",domainRevisions:{person:5}`. `qreject(reply,code)=a(reply.replyId,"q","reply",{op:"rejected",reply},{requestId:"q",code},1099)`, with exactly `(badWho,"binding_mismatch")`, `(badInput,"binding_mismatch")`, `(badDomain,"domain_stale")`.

| Race schedule / appended action(s) | Revisions | Named full product and exact effect/observation delta |
| --- | --- | --- |
| commit the immutable cfg/T/q-pre/q prefix above | r1,r2,r3,r4 | `QBASE=QS(4,[])`; Obs each; bodies [] |
| qopen | r5 | `QOPEN=QS(5,[Q0])`; Obs(qopen,5), effects [] |
| delayed timer: first admit lateAnswer, append `[qlate,qtimer]` atomically | r6,r7 | `QLATE=QS(7,[Q0 with state:"expired",outcome:"outcome_unknown",seenReplyIds:["reply-1"]])`; Obs both see r7; no inbox/wake/body; deadline wins |
| invoke delayed timer again or repeat the same lateAnswer/inputId | r7 | exactly QLATE; duplicate original timer-1 or reply-1 receipt respectively; observations/effects [] |
| cancel first from QOPEN: qcancel | r6 | `QCANCEL=QS(6,[Q0 with state:"cancelled",outcome:"cancelled"])`; Obs; abort original invocation only |
| validAnswer after cancel: qduplicate | r7 | `QCANCEL_REPLY=QS(7,[Q0 with state:"cancelled",outcome:"cancelled",seenReplyIds:["reply-1"]])`; Obs, effects [] |
| reply first from QOPEN: `[qreply,qresolve]` atomically | r6,r7 | `QANSWER=QS(7,[Q0 with state:"resolved",outcome:"answered",seenReplyIds:["reply-1"],replies:[{replyId:"reply-1",responderId:"worker",content:"ok",receivedAt:1099}]])`; Obs both see r7; one original-invocation continuation |
| cancel proposal built at QOPEN applies after QANSWER | r7 | exactly QANSWER; `{ok:false,reason:"revision",currentFence:1,currentRevision:7}`; effects [] |
| identical validAnswer/inputId again / altered payload under reply-1 / old lease fence0 | r7 | exactly QANSWER; duplicate original receipt / invalid_transition / `{ok:false,reason:"stale",currentFence:1,currentRevision:7}`; observations/effects [] |
| each of the three qreject cases separately from QOPEN | r6 | `QREJECT(reply)=QS(6,[Q0])` with exactly that qreject action in its prefix; Obs once; no body/inbox |

Deadline comparison still applies to a first-seen reply after a terminal: at1100 it records late_unknown, not duplicate. Replay targets are QLATE/QCANCEL_REPLY/QANSWER/QREJECT(reply), including their exact action objects and original invocation. No row can interpret a receipt refusal as a read-only refusal.

**Child/parent loss boundary (target #946 consumption).** This independent vector has two real session histories, not cross-session action parents. Let `pa(...)`/`ca(...)` be a(...) with sessionId PARENT/CHILD respectively; `PS(...)`/`CS(...)` are S(...) with the durable id and all contained sessionId fields set likewise, child parentId PARENT/role worker, parent parentId null/role resident. G1 is unchanged. Each session starts r0; materialize `pcfg`/`ccfg` as the cfg action with its respective id/sessionId at r1 and acquire owner p/c, expectedFence0, now1000, expiry31000 -> fence1, zero action revisions. For parent, seed one batch at r2/r3/r4: `pa("PT","pcfg","turn",{phase:"intent",resultId:"PR",inboxIds:[],resumeCount:0,boundaryActionId:"pcfg",toolsGeneration:1,toolsHash:H([]),systemHash:H([]),policyGeneration:1},{phase:"pending"})`, `pa("p-pre","PT","policy.decision",{hook:"tool.pre",op:"B",generation:1,matchedRuleIds:[],verdict:"allow",inputHash:H({kind:"tool",phase:"pre",op:"B",role:"resident",sessionId:"PARENT",value:{value:"CHILD"}})},{phase:"result",reason:null})`, then `pa("qp","PT","tool",{phase:"intent",op:"B",value:{value:"CHILD"}},{phase:"pending"})`. Its parsed original invocation is qp, with captured child identity CHILD. `PT=T with turnId:"PT",resultId:"PR",boundaryActionId:"pcfg"`, messages []. Parent operation is QB with waveId qp/turnId PT/call intentId qp/preDecisionId p-pre/parsedInput {value:"CHILD"}/inputHash H({value:"CHILD"}); body dispatch is outside this immutable fixture seed.

The child's durable original input origin is `CO=E({sourceSessionId:"PARENT",requestId:"qp",replyTo:"qp"})`. Seed one batch at child r2/r3/r4: `ca("ci","ccfg","prompt",CO.value,{inboxKind:"prompt",content:"hello"})`, `ca("ci:delivery","ci","inbox.deliver",{inboxId:"ci"},{phase:"delivery",turnId:"CT",inboxId:"ci",kind:"prompt",content:"hello",origin:CO,boundary:"before_llm"})`, `ca("CT","ci:delivery","turn",{phase:"intent",resultId:"CR",inboxIds:["ci"],resumeCount:0,boundaryActionId:"ccfg",toolsGeneration:1,toolsHash:H([]),systemHash:H([]),policyGeneration:1},{phase:"pending"})`. Its inbox is `[in("ci","prompt","hello",1,1000,"consumed","c",1000) with sessionId:"CHILD",origin:CO]`; `CT=T with turnId:"CT",resultId:"CR",boundaryActionId:"ccfg"`. Thus `CP_OPEN={parent:PS(4,"running",["p",1,31000],G1,PT,[],[parent-operation]),child:CS(4,"running",["c",1,31000],G1,CT,child-inbox)}` with exactly those seed prefixes, no requests/alarms/occurrences/outbound/retained effects.

Run each terminal kind k in result/error/interrupted separately, with wire kind w=completed/error/interrupted respectively and final text "done". Define `M0={messageId:"mCR",sourceSessionId:"CHILD",sourceActionId:"CR",destinationSessionId:"PARENT",requestId:"qp",replyTo:"qp",terminal:w,content:"done"}`, `M={...M0,digest:H(M0)}`; `MP={...M,state:"pending",destinationReceipt:null}`. Source batch is exactly `ca("CR","CT","turn",{phase:"terminal",turnId:"CT"},{phase:"terminal",turnId:"CT",kind:k,text:"done",boundaryActionId:"ccfg",resumeCount:0})` then `ca("out-CR","CR","outbound",{op:"open",message:M},{state:"pending",destinationReceipt:null})`. No process can observe just CR. Parent RCV action at2000 is `pa("receive-mCR","qp","prompt",M,{inboxKind:"prompt",content:"done"},2000)`, and its inbox row is `in("mCR","prompt","done",1,2000,"pending") with sessionId:"PARENT",origin:E(M)`; parent runner is held at its boundary throughout this delivery-only vector.

| Loss/command | Child / parent revisions | Complete expected pair; observations/effects |
| --- | --- | --- |
| source transaction fails before commit, including after terminal insert but before outbound insert | c4 / p4 | exactly CP_OPEN; rollback removes both actions; zero dispatch/Obs |
| source batch commits, release child lease, process dies before WAKE | c5,c6 / p4 | `CP_SEALED=CP_OPEN with child:CS(6,k==="interrupted"?"interrupted":"idle",[null,1,null],G1,end(CT,k,"done"),child-inbox,[],[],[],[],[MP])`; both Obs see c6, parent unchanged; process loss handle absent; zero dispatch |
| recover pending obligation at2000; acquire child owner c2/expectedFence1/expiry32000 -> f2; invoke WAKE | c6 / p5 | `CP_RECEIVED=CP_SEALED with child.lease:["c2",2,32000],child.runtime.handle:"resident",parent.revision:5,parent.inbox:[received-row],parent.actions:[...seed,receive-mCR]`; one receiving Obs; one RCV delivery; CR/out-CR unchanged |
| process loss after destination commit but before source ack | c6 / p5 | CP_RECEIVED with child.runtime.handle absent; MP still pending, parent row still exactly one |
| at33000 acquire child c3/expectedFence2/expiry63000 -> f3; WAKE retries same M | c6 / p5 | previous pair with child.lease:["c3",3,63000],child.runtime.handle resident; RCV returns original receive-mCR/revision5; no new receiving action/Obs/inbox row; no repeated parent consumption |
| source ack `ca("ack-CR","out-CR","outbound",{op:"ack",messageId:"mCR",destinationReceiptId:"receive-mCR",destinationRevision:5},{state:"delivered"},33000)` and release | c7 / p5 | `CP_ACKED` is previous pair with child.revision7/lease [null,3,null]/handle absent/actions plus ack-CR/outbound `[MP with state:"delivered",destinationReceipt:{id:"receive-mCR",revision:5}]`; source Obs once; no further WAKE |
| recover delivered obligation / duplicate doorbell | c7 / p5 | exactly CP_ACKED; zero new action/inbox/continuation; replay of every committed pair is effect-free |

The `.lease` and `.revision` replacements above mean the corresponding durable row fields of S, not extra snapshot properties. The source transaction and RCV receipts, not a new dispatcher/store, close both loss windows.

### 6.5 Alarm, pause, rearm, dedupe, takeover vectors (target)

All actions append to S, counted from r0; alarmRevision `v` is a separate coordination CAS version, incremented once per semantic transaction, not per action. Seed `S(0,"idle",["o",7,31000],G1,none,[])`. Alarm A fields: `{id:"A",sessionId:"S",kind:"watch",fireAt:1000,createdAt:1000,updatedAt:1000,spec:E({controlKey:"watch-1",source:"idempotent_poll",mode:"recurrent"}),controlKey:"watch-1",epoch:1,alarmRevision:1,status:"armed",mode:"recurrent",source:"idempotent_poll",cursor:null,lastBatchHash:null,notificationLimit:2,notifications:0,pauseNotified:false,resumeFrom:null,evaluator:{owner:null,fence:0,expiresAt:null}}`. Active key bytes are `K(["S","watch","watch-1"])`. Match batch A is `{value:"A"}`, hash H of that object; B is `{value:"B"}`. `O1` complete occurrence is `{occurrenceId:"O1",alarmId:"A",epoch:1,sourceKey:"poll-1",batchHash:H({value:"A"}),cursorBefore:null,cursorAfter:null,sessionId:"S",parentActionId:"arm",fireAt:1000,generation:1,inputHash:H({value:"A"}),deadline:1100,state:"open",terminal:null}`; O2 is the same complete occurrence shape with `occurrenceId:"O2"`, `sourceKey:"poll-2"`, and `fireAt:1060`; O3 uses `occurrenceId:"O3"`, `sourceKey:"poll-3"`, and the same epoch-2 fire slot. Controls carry session o/fence7 and current v; evaluator commands carry e/fence as listed. `AS(r,alarm,occurrences,outbound=[])` is the full S constructor with those regions, all others empty; r0 has no alarm.

| Exact input/action list | Revisions; alarm CAS | Snapshot; winner; dispatch |
| --- | --- | --- |
| arm command with complete A fields above -> `a("arm",null,"alarm.arm",{alarm:A},{status:"armed"})` | r1; v1 | `A_ARM=AS(1,A,[])`; Obs; source installation only |
| acquire evaluator e/fence0/now1000/expiresAt1050 | r1; v1, evaluator f1 | `A_LEASE=AS(1,A with evaluator {owner:"e",fence:1,expiresAt:1050},[])`; zero actions/Obs |
| fire poll-1 batch A, at1000 -> `a("fire1","arm","alarm.fired",{alarmId:"A",epoch:1,sourceKey:"poll-1",occurrenceId:"O1",fireAt:1000},{status:"fired"})`, `a("O1","fire1","occurrence",{op:"open",occurrence:O1},{state:"open"})` | r2,r3; v2 | `A_OPEN=AS(3,A fired/v2/f1,[O1])`; Obs two; no wake yet |
| evaluate O1 -> `a("eval1","O1","occurrence",{op:"evaluate",occurrenceId:"O1",evaluatorFence:1},{state:"evaluate"})` | r4; v3 | `A_EVAL=AS(4,A fired/v3/f1,[O1 evaluate])`; Obs; one evaluator call |
| owner pause reason budget while evaluating -> pause `a("pause","arm","alarm.paused",{alarmId:"A",reason:"budget",resumeFrom:"fired"},{status:"paused",pauseNotified:true})`, occurrence result cancel, outbound pause-note | r5,r6,r7; v4, evaluator f2/unleased | `A_PAUSED=AS(7,A paused/v4/f2/resumeFrom fired/pauseNotified true,[O1 cancelled],[pause-note pending])`; three Obs; one pause-note wake; cancelled O1 no result wake |
| old e/f1 result or fire callback | r7; unchanged | exactly A_PAUSED; stale, actions/Obs/effects [] |
| owner resume nextAt1050/restartMode idempotent_poll -> `a("resumeA","pause","alarm.control",{op:"resume",alarmId:"A",nextAt:1050,restartMode:"idempotent_poll"},{status:"armed",epoch:1})` | r8; v5, evaluator f3/unleased | `A_RESUMED=AS(8,A armed/v5/epoch1/f3/pauseNotified false,[O1 cancelled],[pause-note pending])`; Obs; source restart |
| owner explicit rearm nextAt1060/same spec/restartMode restart-from-now -> rearm action full admitted spec and `{status:"armed",epoch:2,lastBatchHash:null,cursor:null}` | r9; v6, evaluator f4/unleased | `A_REARMED=AS(9,A armed/v6/epoch2/f4/fireAt1060,[O1 cancelled],[pause-note pending])`; Obs; source restart |
| repeat identical rearm input id / new rearm input with current epoch2 but stale expected v5 | r9 | exactly A_REARMED; original receipt / revision refusal; [] |
| acquire e/f4 at1060 expiry1070 -> f5; fire poll-2 batch A/Open O2 (epoch2), evaluate O2, complete O2 with output `"ok"`, outbound wake O2, automatic recurrent arm nextAt1070 | r10,r11,r12,r13,r14,r15; v7,v8,v9 | `A_COMPLETED=AS(15,A armed/v9/epoch2/f5/lastBatchHash H(A)/notifications1/fireAt1070,[O1 cancelled,O2 completed],[pause-note pending,O2 pending])`; Obs six; evaluator once, one wake O2 |
| takeover e2 at1070 expected f5 expiresAt1200 | r15 | `A_TAKEN=AS(15,A_COMPLETED.alarm with evaluator {owner:"e2",fence:6,expiresAt:1200},A_COMPLETED.occurrences,A_COMPLETED.outbound)`; hashes/cursor/epoch unchanged; zero actions |
| poll-3 batch A -> open O3 then terminal deduped reason consecutive_batch; alarm remains armed, no budget debit | r16,r17; v10 | `A_DEDUPED=AS(17,A_TAKEN.alarm with v10,[O1 cancelled,O2 completed,O3 deduped],A_COMPLETED.outbound)`; Obs two; zero wake |
| exact redelivery poll-3/O3; then old evaluator e/f5 poll | r17 | exactly A_DEDUPED; original occurrence receipt then stale; [] |
| poll-4 batch B -> fire/Open O4/evaluate/result completed/outbound O4/recurrent arm nextAt1090 | r18,r19,r20,r21,r22,r23; v11,v12,v13 | `A_B=AS(23,A armed/v13/epoch2/f6/lastBatchHash H(B)/notifications2/fireAt1090,[O1 cancelled,O2 completed,O3 deduped,O4 completed],[pause-note pending,O2 pending,O4 pending])`; one new wake O4 |

Alarm trace clock is exactly 1000 for arm/fire1/eval1/pause, 1050 for resumeA/rearm, 1060 for O2, 1070 for takeover/O3, and 1080 for O4. Every action in a row uses that clock as ts; updatedAt follows the winning semantic transaction. O2/O3 use fireAt1060/1070 and epoch2, O4 uses fireAt1070/epoch2 and hash H({value:"B"}); each has deadline1100. `H(A)`/`H(B)` in snapshots mean H({value:"A"})/H({value:"B"}), not hashes of the alarm row. The rearm action id is rearmA and parent resumeA. Result/open/outbound actions above expand with the complete O1/A products and changed values in their row, action ids `fire2/O2/eval2/result2/wake2/auto2`, `O3/result3`, `fire4/O4/eval4/result4/wake4/auto4`, in that order; each parent is the prior action except occurrence results parent their own open id. A completed result effect is `{state:"terminal",terminal:"completed",reason:null,output:"ok"}`; a cancelled result effect is `{state:"terminal",terminal:"cancelled",reason:"budget"}`; a deduped result effect is `{state:"terminal",terminal:"deduped",reason:"consecutive_batch"}`. The O2 outbound payload is `{messageId:"wake2",sourceSessionId:"S",sourceActionId:"result2",destinationSessionId:"S",requestId:"O2",replyTo:"O2",terminal:"completed",content:"ok",digest:H(["O2","ok"])}`; the O4 outbound payload is `{messageId:"wake4",sourceSessionId:"S",sourceActionId:"result4",destinationSessionId:"S",requestId:"O4",replyTo:"O4",terminal:"completed",content:"ok",digest:H(["O4","ok"])}`. Automatic arm intent for O2 is `{alarmId:"A",epoch:2,nextAt:1070}`, effect `{status:"armed"}`; for O4 it is `{alarmId:"A",epoch:2,nextAt:1090}`, effect `{status:"armed"}`. Paused cancellation result id cancel1/parent O1/terminal cancelled/reason budget; pause-note payload has messageId pause-note/sourceActionId pause/destination S/content budget/digest H(["A",1,"budget"]). No unnamed output or random id remains.

One-shot boundary vector starts with the same empty r0 seed, not an already leased alarm. `AO=A with kind:"at",mode:"once",source:"at",notificationLimit:1,spec:E({controlKey:"watch-1",source:"at",mode:"once"})`. `OO=O1 with sourceKey:"timer-1000"`. Its active key is K(["S","at","watch-1"]). `MO={messageId:"once-wake",sourceSessionId:"S",sourceActionId:"once-result",destinationSessionId:"S",requestId:"O1",replyTo:"O1",terminal:"completed",content:"ok",digest:H(["O1","ok"]),state:"pending",destinationReceipt:null}`. `MO-payload` is MO without its state and destinationReceipt projection fields. All these actions have ts1000; all unspecified fields are inherited from the complete AO/OO products.

| One-shot input / exact actions | Revisions; alarm CAS | Complete snapshot; dispatch |
| --- | --- | --- |
| `a("arm",null,"alarm.arm",{alarm:AO},{status:"armed"})` | r1; v1 | `ONCE_ARM=AS(1,AO,[])`; source installation |
| acquire evaluator `{evaluatorOwner:"e",expectedEvaluatorFence:0,at:1000,expiresAt:1050}` under session o/f7 | r1; v1 | `ONCE_LEASE=AS(1,AO with evaluator:{owner:"e",fence:1,expiresAt:1050},[])`; zero actions/Obs |
| `a("fire1","arm","alarm.fired",{alarmId:"A",epoch:1,sourceKey:"timer-1000",occurrenceId:"O1",fireAt:1000},{status:"fired"})`, `a("O1","fire1","occurrence",{op:"open",occurrence:OO},{state:"open"})` in one batch | r2,r3; v2 | `ONCE_OPEN=AS(3,ONCE_LEASE.alarm with status:"fired",alarmRevision:2,[OO])`; no evaluator call yet |
| `a("eval1","O1","occurrence",{op:"evaluate",occurrenceId:"O1",evaluatorFence:1},{state:"evaluate"})` | r4; v3 | `ONCE_EVAL=AS(4,ONCE_OPEN.alarm with alarmRevision:3,[OO with state:"evaluate"])`; evaluator once |
| `a("once-result","O1","occurrence",{op:"result",occurrenceId:"O1"},{state:"terminal",terminal:"completed",reason:null,output:"ok"})`, `a("once-wake","once-result","outbound",{op:"open",message:MO-payload},{state:"pending",destinationReceipt:null})` in one batch | r5,r6; v4 | `ONCE_DONE=AS(6,ONCE_EVAL.alarm with alarmRevision:4,notifications:1,lastBatchHash:H({value:"A"}),[OO with state:"terminal",terminal:"completed",reason:null,output:"ok",wakeMessageId:"once-wake"],[MO])`; active key released; WAKE once; **no automatic arm** |
| exact duplicate result at1000 / automatic rearm at1000 | r6 | exactly ONCE_DONE; original receipt / invalid_transition; zero actions/effects |
| authenticated explicit rearm at1000, nextAt1200, same spec, restartMode restart-from-now -> `a("once-rearm","once-result","alarm.control",{op:"rearm",alarmId:"A",nextAt:1200,spec:AO.spec,restartMode:"restart-from-now"},{status:"armed",epoch:2,lastBatchHash:null,cursor:null})` | r7; v5 | `ONCE_REARMED=AS(7,ONCE_DONE.alarm with status:"armed",alarmRevision:5,epoch:2,fireAt:1200,notifications:0,lastBatchHash:null,cursor:null,evaluator:{owner:null,fence:2,expiresAt:null},ONCE_DONE.occurrences,[MO])`; active key reserved again; evaluator requires new acquisition |

Cancel-vs-fire branches from **ONCE_LEASE**, with its acquired e/f1 and complete seed. Cancel at1000 first appends `a("once-cancel","arm","alarm.cancelled",{alarmId:"A",reason:"owner"},{status:"cancelled"})` at r2/v2: `ALARM_CANCELLED=AS(2,ONCE_LEASE.alarm with status:"cancelled",alarmRevision:2,evaluator:{owner:null,fence:2,expiresAt:null},[])`; old fire is stale. Reverse order reaches ONCE_OPEN then appends that cancel and `a("once-cancel-result","O1","occurrence",{op:"result",occurrenceId:"O1"},{state:"terminal",terminal:"cancelled",reason:"owner"})` atomically at r4/r5/v3: `FIRED_CANCELLED=AS(5,ONCE_OPEN.alarm with status:"cancelled",alarmRevision:3,evaluator:{owner:null,fence:2,expiresAt:null},[OO with state:"terminal",terminal:"cancelled",reason:"owner"])`. Both release the active key and dispatch cleanup only. Replay equals the selected recurrent or one-shot named snapshot, with zero dispatch.

### 6.6 Totality/property harness

HARNESS generates the Cartesian product of the finite region states and event tags above, and two-way permutations of contenders built against the same revision (reply/cancel/timeout; interrupt/answer; fire/pause/cancel; renew/takeover; duplicate input/result). Pure DEC is evaluated before APPLY; the loser is retried only as a **new decision against the winning snapshot**, never by dispatching its old proposal. Rejecting a late reply may append its explicit receipt; rejecting a stale lease cannot. For each accepted action prefix assert the named snapshot, exact next ordinal, terminal count per original result slot <=1, input-consumption count <=1, whole-wave gate body count 0, sequential-start barrier, and effect descriptor identity. Corrupt owner, fence, revision, parent session, input hash, generation, domain revision, cursor, deadline edge, and duplicate id independently; each must take the specified refusal branch. Replaying each exact prefix from its named seed must equal that prefix snapshot with dispatched effects `[]`. Refusal receipts are part of replay input, not silently discarded.

Include every post-body recovery classification crossed with absent/applied/reverted/indeterminate proof, terminal found/empty/read-back unavailable, current/lost write authority, and raw settled/unsettled. Inconsistent proof products refuse; accepted products must select exactly one section 3 destination, keep a pre-existing terminal, and patch only the affected retained operation. Exercise both alarm terminal-result digests under current and old evaluator fences, exact rearm replay after epoch advancement versus fresh stale-epoch input, and deadline-versus-failure-versus-budget precedence. An ineligible takeover of an evaluating live source must append pause + cancellation atomically, clear evaluator owner/expiry, increment its fence, leave no nonterminal occurrence, and permit an explicitly supported owner resume without borrowing the eligible-takeover row. Inject failure between child terminal/outbound inserts and after destination commit/before source ack; assert the section 6.4 pair snapshots. These finite cases are construction requirements, not a claim that the target implementation already ships.

Before triggering asynchronous input, register the exact action-commit/runner-entry/body-settlement/abort/source-close signal; await that promise with a 2-second failure deadline. Time itself is advanced by the fixture clock and the captured timer callback, never by sleeping. For real app runs, register the socket response and storage observation before send; reopening file-backed SQLite must preserve the winning action and old immutable prefix. These are behavior assertions; documentation wording is not a test oracle.

### 6.7 Named executable handoff

Existing test names in section 7 are **partial coverage**, not a claim they execute the new vectors. #973 constructs these exact tests in `packages/agent/test/session-lifecycle-conformance.test.ts`: `lifecycle v1 ordinary and mixed-wave approved`, `lifecycle v1 refusal timeout and interrupt`, `lifecycle v1 terminal resume differs from crash-open recovery`, `lifecycle v1 delayed timer cancel reply and duplicate input`, `lifecycle v1 alarm takeover pause rearm and dedupe`, and `lifecycle v1 product totality and effect-free prefix replay`. They call HARNESS with the named vectors above; the request-race registration also owns section 6.4's child/parent loss-boundary vector. Separately, #971 constructs `alarm v1 evaluator acquisition and recovery refusal are atomic` in target `packages/agent/test/alarm-transition.test.ts`, exclusively owning local ALD/ALA tests, not any of these six registrations. The app test `real app SSE compaction commits reversible evidence through the session executor` remains a real-surface compaction test, not the lifecycle harness. The recovery and positional-history tests have one sole owner each in the inventory.

## 7. Exclusive deletion and retention receipts

Inventory unit is **`path#qualified declaration`**, not a file, describe block, or numeric range. Parent declarations own their header/assembly only and explicitly exclude **all listed descendants**; a kept factory therefore cannot retain a moved nested settlement. Every enumerated authority entry has one receipt; spans do not imply whole-file ownership. Approval's request/decide/consuming-decision exports and all registrations in delegation.test.ts, approval/store.test.ts and approval-fold.test.ts are explicitly partitioned below. Local unnamed callbacks travel with their nearest listed declaration. Each test row names the exact test registration string, qualified by its describe chain using ` :: ` (template variables retain their literal source spelling); a parameterized registration is one symbol with all its cases. Shared readers and their tests are assigned to one child even when other children consume them.

`keep` means same destination; `move` means remove the old export/writer at the same cutover as the exact named target; `delete` has no remaining consumer. A move into DEC/REQUEST/APPLY is incorporation of that specific responsibility, not copying the old subsystem. For rows with no durable state, data is `none`. For durable symbols: `migrate` maps the specified rows/facts to their original-session action projection, preserving ids/content/history; `refuse` retains bytes and refuses removal; `drop` is not selected in this contract. Each data-bearing row below states its own disposition. No claim of a nonexistent HEAD monitor/table deletion is made.

### Data migration specifics

#969's guarded forward migration converts every session-owned open Wait/Approval/delegation record with a reconstructable original invocation into a request action with complete binding/hash/deadline/domain preconditions. If those cannot be reconstructed, cutover **refuses**, preserving the database and emitting the missing identity in its operator receipt; it does not guess an invocation or drop unresolved rows. Terminal legacy rows and facts remain immutable evidence and are exported/retained; the old writer APIs are removed. Physical correlation indexes point to original request actions after conversion, retaining channel/message/thread/token/chain evidence. Person mutation manifest digest, protected Owner checks, promotion/endpoint merge inputs, and pending-volume limits move with their consuming checks, not merely the approval label.

#970 migrates session/lease/generation and action projections without changing original terminal/result IDs. Recovery effects are classified local-transactional, endpoint-idempotent, read-back-reconcilable, or ambiguous-no-replay; only a proved absent/idempotent effect may execute again. Existing provider `createAttemptRunner` remains the retry owner; billed/visible attempts survive. Target: `restoreModelSelection` and `restoreContextProjection` in `packages/agent/src/session-admission.ts` (owner #970) append new restoration evidence; target: `recoverOperation` in that file (owner #970) uses section 3's exact recovery product: known evidence -> failed after a completion exception, ambiguous evidence -> terminal outcome_unknown, unavailable writer -> nonterminal recovery_pending. An already committed terminal always wins. The general absent/idempotent replay permission does not permit re-executing a post-body exception. Neither a resume nor a pointer restore erases history.

#971 extends **alarm** coordination and represents occurrences as action records, with no parallel monitor table. Duplicate active keys in upgraded data refuse cutover; legacy alarms lacking an explicit recoverable source pause with recovery_unavailable instead of replaying live output. #972 retains physical message/part bytes and immutable action/migration history; its SDK projections read the migrated original sources. No child is assigned a hypothetical deletion because a previous issue title mentioned it.

The following per-entry receipt rows double as the citation census. `source span` includes the declaration body for evidence; ownership excludes every separately listed descendant. References in the reader column denote consumers of the disposition, not additional source ownership.

### #969 production and dependent tests

The #969 cutover owns the logical request, approval, wait, delegation settlement, and destination wake writers. Physical channel delivery and route facts retain their existing effectful collaborators; correlation lookup is a reader, but platform receipt recording is a **writer**. Its WaitStore.commit dependency is removed with that store writer and incorporated into APPLY's delivery_receipt branch, not kept as a second Wait lifecycle. Each nested declaration is qualified; no enclosing keep row includes a moved child. Test keeps retain registrations/evidence while rebinding the replaced authority; they do not retain legacy writer APIs or prose assertions. The two wall-clock deadline registrations in delegation.test.ts must use a captured timer and injected clock at cutover, and approval observation assertions must await the exact event rather than a microtask flush.

| Kind | Exact source symbol | Source span | Disposition | Destination / sole consumer | Data disposition |
| --- | --- | --- | --- | --- | --- |
| prod | `packages/channels/src/router/wait/lifecycle.ts#WaitService.open` | `packages/channels/src/router/wait/lifecycle.ts:20-24` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969) | wait rows/facts: migrate; unbound open rows: refuse |
| prod | `packages/channels/src/router/wait/lifecycle.ts#WaitService.attachReply` | `packages/channels/src/router/wait/lifecycle.ts:31-35` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969) | wait rows/facts: migrate; unbound rows: refuse |
| prod | `packages/channels/src/router/wait/lifecycle.ts#WaitService.recordDeliveryReceipt` | `packages/channels/src/router/wait/lifecycle.ts:43-50` | move | correlation WRITER through WaitStore.commit; target: commitRequestTransition in packages/ledger/src/session/kernel.ts (owner #969), delivery_receipt branch through APPLY; deliverSend supplies physical receipt, not logical authority | Wait rows and wait.delivery_recorded facts: migrate to original-request correlation projection; platform message/thread/token/chain bytes: refuse removal; unbound Wait: refuse cutover |
| prod | `packages/channels/src/router/wait/lifecycle.ts#WaitService.expire` | `packages/channels/src/router/wait/lifecycle.ts:57-59` | move | target: expireRequest in packages/agent/src/executor-approval.ts (owner #969) | wait rows/facts: migrate |
| prod | `packages/channels/src/router/wait/lifecycle.ts#WaitService.cancel` | `packages/channels/src/router/wait/lifecycle.ts:74-76` | move | target: cancelRequest in packages/agent/src/executor-approval.ts (owner #969) | wait rows/facts: migrate |
| prod | `packages/channels/src/router/wait/lifecycle.ts#WaitService.sweepExpired` | `packages/channels/src/router/wait/lifecycle.ts:78-103` | move | target: recoverRequestDeadlines in packages/agent/src/executor-approval.ts (owner #969) | wait rows/facts: migrate |
| prod | `packages/ledger/src/wait/index.ts#WaitStore.create` | `packages/ledger/src/wait/index.ts:184-235` | move | target: commitRequestOpen in packages/ledger/src/session/kernel.ts (owner #969) | wait rows/facts: migrate |
| prod | `packages/ledger/src/wait/index.ts#WaitStore.commit` | `packages/ledger/src/wait/index.ts:261-335` | move | target: commitRequestTransition in packages/ledger/src/session/kernel.ts (owner #969) | wait facts: migrate; immutable history: refuse |
| prod | `packages/protocol/src/wait/fold.ts#attachReply` | `packages/protocol/src/wait/fold.ts:132-154` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969) | wait facts: migrate |
| prod | `packages/protocol/src/wait/fold.ts#expire` | `packages/protocol/src/wait/fold.ts:218-239` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969) | wait facts: migrate |
| prod | `packages/protocol/src/wait/fold.ts#cancel` | `packages/protocol/src/wait/fold.ts:257-271` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969) | wait facts: migrate |
| prod | `packages/protocol/src/wait/fold.ts#recordDeliveryReceipt` | `packages/protocol/src/wait/fold.ts:241-255` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969), delivery_receipt correlation patch only | Wait correlation/facts: migrate; physical receipt bytes: refuse removal |
| prod | `packages/ledger/src/approval/index.ts#ApprovalStore.request` | `packages/ledger/src/approval/index.ts:93-140` | move | target: commitRequestOpen in packages/ledger/src/session/kernel.ts (owner #969); pending-volume CAS retained | approval rows/facts and subject bindings: migrate; unbound pending rows: refuse |
| prod | `packages/ledger/src/approval/index.ts#ApprovalStore.get` | `packages/ledger/src/approval/index.ts:142-144` | keep | #969 rebinds read to original-request projection, no independent authority | approval facts/subject bytes: refuse removal |
| prod | `packages/ledger/src/approval/index.ts#ApprovalStore.list` | `packages/ledger/src/approval/index.ts:146-148` | keep | #969 rebinds enumeration to original-request projection | approval facts/subject bytes: refuse removal |
| prod | `packages/ledger/src/approval/index.ts#ApprovalStore.decide` | `packages/ledger/src/approval/index.ts:155-202` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969), persisted only by APPLY/commitRequestTransition | approval decisions and owner/deadline evidence: migrate; immutable facts: refuse removal |
| prod | `packages/ledger/src/approval/index.ts#ApprovalStore.decision` | `packages/ledger/src/approval/index.ts:209-220` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969); consuming authorization checks use REQUEST, not a second lazy decision | approval subject/binding/deadline evidence: migrate |
| prod | `packages/protocol/src/approval/fold.ts#request` | `packages/protocol/src/approval/fold.ts:21-33` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969), request.open branch | approval pending facts: migrate; unsupported binding: refuse |
| prod | `packages/protocol/src/approval/fold.ts#decide` | `packages/protocol/src/approval/fold.ts:41-62` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969), original-request answer/deadline CAS | approval decision facts: migrate |
| prod | `packages/protocol/src/approval/fold.ts#decision` | `packages/protocol/src/approval/fold.ts:68-71` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969), captured-time consuming guard | approval subject/deadline facts: migrate |
| prod | `packages/channels/src/router/messaging/send.ts#createExistingAgentMessaging.send` | `packages/channels/src/router/messaging/send.ts:515-528` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969) | request/send admissions: migrate |
| prod | `packages/channels/src/router/messaging/send.ts#openSendWait` | `packages/channels/src/router/messaging/send.ts:376-420` | move | target: commitRequestOpen in packages/ledger/src/session/kernel.ts (owner #969) | wait rows/facts: migrate |
| prod | `packages/channels/src/router/messaging/send.ts#deliverSend` | `packages/channels/src/router/messaging/send.ts:422-452` | keep | physical channel delivery and platform receipt; #972 | message bytes/correlation: refuse |
| prod | `packages/channels/src/router/routing-execution.ts#executeWaitRoute` | `packages/channels/src/router/routing-execution.ts:112-184` | move | target: decideRequestTransition in packages/agent/src/executor-approval.ts (owner #969) | request reply facts: migrate |
| prod | `apps/openomni/src/delegation/kernel.ts#createDelegationKernel.settle` | `apps/openomni/src/delegation/kernel.ts:308-347` | move | target: settleSessionOperation in apps/openomni/src/session-lifecycle/settlement.ts (owner #969) | delegation rows: migrate |
| prod | `apps/openomni/src/delegation/kernel.ts#createDelegationKernel.deliverWake` | `apps/openomni/src/delegation/kernel.ts:349-380` | move | target: dispatchSessionWake in apps/openomni/src/session-lifecycle/settlement.ts (owner #969) | outbound/delegation rows: migrate |
| prod | `apps/openomni/src/delegation/kernel.ts#createDelegationKernel.arm` | `apps/openomni/src/delegation/kernel.ts:382-407` | move | target: armSessionRequest in apps/openomni/src/session-lifecycle/settlement.ts (owner #969) | delegation deadlines: migrate |
| prod | `apps/openomni/src/delegation/kernel.ts#createDelegationKernel.recover` | `apps/openomni/src/delegation/kernel.ts:409-440` | move | target: recoverSessionRequest in apps/openomni/src/session-lifecycle/settlement.ts (owner #969) | delegation rows: migrate |
| prod | `apps/openomni/src/delegation/kernel.ts#createDelegationKernel.dispatch` | `apps/openomni/src/delegation/kernel.ts:442-477` | keep | transport driver invocation; target WAKE owns logical settlement | transport records: migrate |
| prod | `apps/openomni/src/delegation/kernel.ts#createDelegationKernel.awaitDelegation` | `apps/openomni/src/delegation/kernel.ts:657-752` | move | target: awaitSessionOperation in apps/openomni/src/session-lifecycle/settlement.ts (owner #969) | delegation rows: migrate |
| prod | `apps/openomni/src/delegation/kernel.ts#createDelegationKernel.cancelDelegation` | `apps/openomni/src/delegation/kernel.ts:754-769` | move | target: cancelSessionOperation in apps/openomni/src/session-lifecycle/settlement.ts (owner #969) | delegation rows: migrate |
| prod | `apps/openomni/src/delegation/kernel.ts#createDelegationKernel.settleFromReply` | `apps/openomni/src/delegation/kernel.ts:771-783` | move | target: settleSessionReply in apps/openomni/src/session-lifecycle/settlement.ts (owner #969) | delegation rows: migrate |
| prod | `apps/openomni/src/delegation/kernel.ts#settlementWaiters` | `apps/openomni/src/delegation/kernel.ts:124-130` | delete | none; source-owned request receipt replaces private waiter map | delegation rows: refuse |
| prod | `apps/openomni/src/delegation/kernel.ts#createDelegationKernel` | `apps/openomni/src/delegation/kernel.ts:250-306` | keep | construction shell only; excludes every listed nested declaration | delegation rows: migrate |
| prod | `apps/openomni/src/delegation/wake-delivery.ts#createWakeDeliveryQueue` | `apps/openomni/src/delegation/wake-delivery.ts:13-43` | keep | physical pre-target buffering only; #969 WAKE consumes it | queued wake bytes: migrate |
| prod | `packages/ledger/src/delegation/index.ts#DelegationStore.settleOnce` | `packages/ledger/src/delegation/index.ts:89-99` | move | target: commitSessionOperation in packages/ledger/src/session/kernel.ts (owner #969) | delegation rows: migrate |
| prod | `packages/ledger/src/delegation/index.ts#DelegationStore.markWoken` | `packages/ledger/src/delegation/index.ts:113-115` | move | target: commitOutboundReceipt in packages/ledger/src/session/kernel.ts (owner #969) | outbound rows: migrate |
| prod | `packages/agent/src/executor.ts#createExecutor` | `packages/agent/src/executor.ts:42-55` | keep | current executor construction; REQUEST supplies original-request authority | action/effect evidence: migrate |
| prod | `packages/agent/src/executor.ts#createExecutor.decide` | `packages/agent/src/executor.ts:57-103` | keep | current policy-decision writer; #969 request target consumes its receipt | policy facts: migrate |
| prod | `packages/agent/src/executor.ts#createExecutor.runBatch` | `packages/agent/src/executor.ts:120-141` | keep | current wave entry; #969 session target consumes result | operation actions: migrate |
| prod | `packages/agent/src/executor.ts#createExecutor.executeBatch` | `packages/agent/src/executor.ts:143-275` | keep | current staged-pre wave; #969 original request handoff | operation actions: migrate |
| prod | `packages/agent/src/executor.ts#createExecutor.finishRun` | `packages/agent/src/executor.ts:324-364` | keep | current result receipt; blocked_post remains executor-owned | action/effect evidence: migrate |
| prod | `packages/agent/src/executor.ts#createExecutor.applyPostPolicy` | `packages/agent/src/executor.ts:366-383` | keep | current post policy/reverter contract | effect evidence: migrate |
| prod | `packages/agent/src/executor-record.ts#createExecutionRecord` | `packages/agent/src/executor-record.ts:21-33` | keep | current record-before-observe adapter | action/effect evidence: migrate |
| prod | `packages/agent/src/executor-record.ts#createExecutionRecord.appendFailure` | `packages/agent/src/executor-record.ts:36-54` | keep | current failure terminal writer | action/effect evidence: migrate |
| prod | `packages/agent/src/executor-record.ts#createExecutionRecord.appendIntent` | `packages/agent/src/executor-record.ts:113-129` | keep | current intent writer | action/effect evidence: migrate |
| prod | `packages/agent/src/executor-record.ts#createExecutionRecord.appendResult` | `packages/agent/src/executor-record.ts:131-156` | keep | current terminal result writer | action/effect evidence: migrate |
| prod | `packages/agent/src/executor-approval.ts#createExecutionApprovals` | `packages/agent/src/executor-approval.ts:12-15` | keep | per-executor suspension capability; REQUEST owns durable original request | request/action facts: migrate |
| prod | `packages/agent/src/executor-approval.ts#createExecutionApprovals.answer` | `packages/agent/src/executor-approval.ts:33-78` | keep | target REQUEST receives authenticated answer evidence | request/action facts: migrate |
| prod | `packages/agent/src/executor-approval.ts#createExecutionApprovals.awaitApproval` | `packages/agent/src/executor-approval.ts:81-158` | keep | target REQUEST receives suspension/deadline result | request/action facts: migrate |
| prod | `packages/agent/src/tool-dispatcher.ts#createDispatcher` | `packages/agent/src/tool-dispatcher.ts:114-145` | keep | current tool catalog/dispatcher construction | tool/action evidence: migrate |
| prod | `packages/agent/src/tool-dispatcher.ts#createDispatcher.prepare` | `packages/agent/src/tool-dispatcher.ts:141-261` | keep | current parsed-call gate | tool/action evidence: migrate |
| prod | `packages/agent/src/tool-dispatcher.ts#createDispatcher.dispatch` | `packages/agent/src/tool-dispatcher.ts:263-269` | keep | current one-call dispatch after receipt | tool/action evidence: migrate |
| prod | `packages/agent/src/tool-dispatcher.ts#createDispatcher.executeWave` | `packages/agent/src/tool-dispatcher.ts:271-309` | keep | current whole-wave dispatch | tool/action evidence: migrate |
| prod | `packages/agent/src/core/execution/tool-wave.ts#runWaveBodies` | `packages/agent/src/core/execution/tool-wave.ts:27-80` | keep | current body scheduling; #969 consumes terminal slots | operation actions: migrate |
| test | `apps/openomni/test/delegation.test.ts#admission fold :: a worker may open only an inline child and the depth cap is typed` | `apps/openomni/test/delegation.test.ts:41-50` | keep | #969 transport/admission conformance | fixture evidence: refuse removal |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: records before dispatching and emits admitted, delivered, settled in fact order` | `apps/openomni/test/delegation.test.ts:204-289` | keep | #969 receipt-order conformance | fixture evidence: refuse removal |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: cancel aborts local work and CAS-settles cancelled` | `apps/openomni/test/delegation.test.ts:430-491` | keep | #969 cancel conformance | fixture evidence: refuse removal |
| test | `apps/openomni/test/delegation-boundaries.test.ts#delegation defensive boundaries :: channel transport rejects impossible non-actor and missing-wait states` | `apps/openomni/test/delegation-boundaries.test.ts:82-97` | keep | #969 channel transport conformance | fixture evidence: refuse removal |
| test | `apps/openomni/test/delegation-signal.test.ts#recovery observes cancellation on the admitted transport's exact signal after its durable CAS` | `apps/openomni/test/delegation-signal.test.ts:30-75` | keep | #969 cancellation conformance | fixture evidence: refuse removal |
| test | `apps/openomni/test/channel-delegation-e2e.test.ts#the Resident delegates to an external actor over the channel and reports the reply` | `apps/openomni/test/channel-delegation-e2e.test.ts:19-100` | keep | #969 external wake conformance | message bytes: refuse |
| test | `apps/openomni/test/delegation.test.ts#admission fold :: the effective deadline is the minimum of request and durable parent` | `apps/openomni/test/delegation.test.ts:52-76` | keep | #969 REQUEST admission | request/parent fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#admission fold :: the exact deadline is already expired for requests and inherited parent authority` | `apps/openomni/test/delegation.test.ts:78-105` | keep | #969 REQUEST deadline | request/parent fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#admission fold :: the pure fold refuses a full durable root fanout` | `apps/openomni/test/delegation.test.ts:107-114` | keep | #969 REQUEST admission | request fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#admission fold :: a settled parent cannot commission another child` | `apps/openomni/test/delegation.test.ts:116-139` | keep | #969 REQUEST admission | request/parent fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: admits a synchronous no-op path before delegate returns` | `apps/openomni/test/delegation.test.ts:143-165` | keep | #969 REQUEST/APPLY | request fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: carries the parent-clamped deadline into the handle and durable record` | `apps/openomni/test/delegation.test.ts:167-202` | keep | #969 REQUEST deadline | request/parent fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: refuses an ask when its parent settles during transport preparation` | `apps/openomni/test/delegation.test.ts:290-310` | keep | #969 REQUEST admission CAS | request fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: atomically admits at most one concurrent child into the last fanout slot` | `apps/openomni/test/delegation.test.ts:354-384` | keep | #969 REQUEST/APPLY CAS | request fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: process/channel work returns the handle before its outcome and can be awaited again` | `apps/openomni/test/delegation.test.ts:386-428` | keep | #969 REQUEST/WAKE | request/outbound fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: stop rejects and removes a pending delegation awaiter` | `apps/openomni/test/delegation.test.ts:492-502` | keep | #969 REQUEST cancellation | request fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: awaiting an open delegation after stop rejects immediately with the stopped error` | `apps/openomni/test/delegation.test.ts:504-513` | keep | #969 REQUEST cancellation | request fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: an inline caller returns the deadline settlement even if its driver ignores abort` | `apps/openomni/test/delegation.test.ts:515-527` | keep | #969 REQUEST deadline; captured timer, not wall-clock race | request fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: a forged root lineage is replaced by the admission-stamped root` | `apps/openomni/test/delegation.test.ts:529-541` | keep | #969 REQUEST authority | request fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: a deadline timer settles no_response at the effective deadline` | `apps/openomni/test/delegation.test.ts:543-566` | keep | #969 REQUEST deadline; captured timer, not wall-clock race | request fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#durable kernel :: deadline and await timers re-arm instead of settling at the timer cap` | `apps/openomni/test/delegation.test.ts:568-621` | keep | #969 REQUEST deadline scheduling | request fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#delegation controls and tool surface :: start advertises v2 operation and durable controls return immediately` | `apps/openomni/test/delegation.test.ts:625-666` | keep | #969 REQUEST consuming surface; machine values, not prose assertions | request fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#delegation controls and tool surface :: SQLite restart sweep preserves channel correlation and interrupts volatile work` | `apps/openomni/test/delegation.test.ts:668-779` | keep | #969 REQUEST/WAKE recovery, not #970 turn-reopen registration | request/correlation fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#delegation controls and tool surface :: restart re-delivers a settled non-inline wake until its receipt is recorded` | `apps/openomni/test/delegation.test.ts:781-829` | keep | #969 WAKE/RCV receipt recovery | outbound/ack fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#delegation controls and tool surface :: publishes a typed operational error and leaves the wake retryable when delivery fails` | `apps/openomni/test/delegation.test.ts:831-864` | keep | #969 WAKE failure/retry | outbound fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#delegation controls and tool surface :: restart sweep settles an expired channel as no_response and notify closes at acceptance` | `apps/openomni/test/delegation.test.ts:866-920` | keep | #969 REQUEST/WAKE recovery | request/outbound fixture evidence: migrate |
| test | `apps/openomni/test/delegation.test.ts#delegation controls and tool surface :: worker restriction remains inline-only while notify is actor-only` | `apps/openomni/test/delegation.test.ts:922-949` | keep | #969 REQUEST authority | request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: request opens a pending record with its fact at seq 1` | `packages/ledger/test/approval/store.test.ts:49-55` | keep | #969 REQUEST/APPLY opening | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: the pending-volume bound refuses a request storm, fail-closed (§8.13)` | `packages/ledger/test/approval/store.test.ts:57-67` | keep | #969 REQUEST pending-volume CAS | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: duplicate request ids fail closed with a typed duplicate error` | `packages/ledger/test/approval/store.test.ts:69-74` | keep | #969 REQUEST identity | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: decide records the Owner's answer once and stays idempotent` | `packages/ledger/test/approval/store.test.ts:76-97` | keep | #969 REQUEST/APPLY answer CAS | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: an answer past the deadline records the deadline's refusal` | `packages/ledger/test/approval/store.test.ts:99-103` | keep | #969 REQUEST deadline | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: decision reads an unanswered request past its deadline as refused` | `packages/ledger/test/approval/store.test.ts:105-110` | keep | #969 REQUEST consuming deadline guard | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: list filters by state` | `packages/ledger/test/approval/store.test.ts:112-118` | keep | #969 original-request projection | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: transitions on a missing approval fail closed with not_found` | `packages/ledger/test/approval/store.test.ts:120-125` | keep | #969 REQUEST identity guard | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: reads on a bare adapter fail closed (adapter_absent)` | `packages/ledger/test/approval/store.test.ts:127-135` | keep | #969 original-request projection | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: an adapter without the ledger append surface fails closed on request` | `packages/ledger/test/approval/store.test.ts:137-146` | keep | #969 APPLY storage boundary | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: SQLITE_BUSY at the transaction entry surfaces as typed unavailable` | `packages/ledger/test/approval/store.test.ts:148-165` | keep | #969 APPLY storage boundary | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: a non-busy transaction failure propagates untyped` | `packages/ledger/test/approval/store.test.ts:167-181` | keep | #969 APPLY error propagation | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: a stale decision fails closed with revision_conflict and writes nothing` | `packages/ledger/test/approval/store.test.ts:183-202` | keep | #969 REQUEST/APPLY CAS | approval/request fixture evidence: migrate |
| test | `packages/ledger/test/approval/store.test.ts#ApprovalStore :: compareAndSet guards the id and the exactly-once revision advance` | `packages/ledger/test/approval/store.test.ts:204-217` | keep | #969 APPLY projection CAS | approval/request fixture evidence: migrate |
| test | `packages/protocol/test/approval-fold.test.ts#Approval fold :: request opens a pending record at revision 1 with no settlement` | `packages/protocol/test/approval-fold.test.ts:20-33` | keep | #969 REQUEST opening | approval/request fixture evidence: migrate |
| test | `packages/protocol/test/approval-fold.test.ts#Approval fold :: the Owner's timely answer settles the request either way` | `packages/protocol/test/approval-fold.test.ts:35-47` | keep | #969 REQUEST answer | approval/request fixture evidence: migrate |
| test | `packages/protocol/test/approval-fold.test.ts#Approval fold :: an answer at or past the deadline records the deadline's refusal, never an approval` | `packages/protocol/test/approval-fold.test.ts:49-55` | keep | #969 REQUEST deadline | approval/request fixture evidence: migrate |
| test | `packages/protocol/test/approval-fold.test.ts#Approval fold :: deciding a settled request keeps the first recorded settlement` | `packages/protocol/test/approval-fold.test.ts:57-63` | keep | #969 REQUEST terminal CAS | approval/request fixture evidence: migrate |
| test | `packages/protocol/test/approval-fold.test.ts#Approval fold :: decision reads an unanswered request past its deadline as refused (fail-closed)` | `packages/protocol/test/approval-fold.test.ts:65-74` | keep | #969 REQUEST consuming guard | approval/request fixture evidence: migrate |
| test | `packages/protocol/test/approval-fold.test.ts#Approval fold :: the record schema rejects dishonest settlements` | `packages/protocol/test/approval-fold.test.ts:76-93` | keep | #969 SCHEMA request products | approval/request fixture evidence: migrate |

### #970 production and dependent tests

| Kind | Exact source symbol | Source span | Disposition | Destination / sole consumer | Data disposition |
| --- | --- | --- | --- | --- | --- |
| prod | `packages/agent/src/session-admission.ts#createSessionAdmission` | `packages/agent/src/session-admission.ts:30-56` | move | target: createSessionLifecycleAdmission in packages/agent/src/session-lifecycle/admission.ts (owner #970) | session/action/inbox rows: migrate |
| prod | `packages/agent/src/session-admission.ts#createSessionAdmission.startTurn` | `packages/agent/src/session-admission.ts:58-130` | move | target: startSessionTurn in packages/agent/src/session-lifecycle/admission.ts (owner #970) | session/action/inbox rows: migrate |
| prod | `packages/agent/src/session-admission.ts#createSessionAdmission.resumeTurn` | `packages/agent/src/session-admission.ts:247-296` | move | target: recoverOpenSessionTurn in packages/agent/src/session-lifecycle/admission.ts (owner #970) | session/action rows: migrate |
| prod | `packages/agent/src/session-admission.ts#createSessionAdmission.resumeInterrupted` | `packages/agent/src/session-admission.ts:298-344` | move | target: resumeInterruptedSession in packages/agent/src/session-lifecycle/admission.ts (owner #970) | session/action/inbox rows: migrate |
| prod | `packages/agent/src/session-turn.ts#createSessionTurn` | `packages/agent/src/session-turn.ts:34-63` | move | target: createSessionLifecycleTurn in packages/agent/src/session-lifecycle/turn.ts (owner #970) | session/action rows: migrate |
| prod | `packages/agent/src/session-turn.ts#createSessionTurn.runTurn` | `packages/agent/src/session-turn.ts:65-312` | move | target: runSessionTurn in packages/agent/src/session-lifecycle/turn.ts (owner #970) | session/action rows: migrate |
| prod | `packages/agent/src/session-turn.ts#createSessionTurn.drainBoundary` | `packages/agent/src/session-turn.ts:314-369` | move | target: drainSessionBoundary in packages/agent/src/session-lifecycle/turn.ts (owner #970) | session/action/inbox rows: migrate |
| prod | `packages/agent/src/session-turn.ts#createSessionTurn.seal` | `packages/agent/src/session-turn.ts:371-411` | move | target: sealSessionTurn in packages/agent/src/session-lifecycle/turn.ts (owner #970) | session/action rows: migrate |
| prod | `packages/agent/src/session-controller.ts#driveAvailable` | `packages/agent/src/session-controller.ts:255-285` | keep | target DEC reads this ordering; #970 recovery consumer | session/action/inbox rows: migrate |
| prod | `packages/agent/src/session-configuration.ts#createSessionConfiguration` | `packages/agent/src/session-configuration.ts:7-15` | keep | createController retains generation/lease shell | generation/session rows: migrate |
| prod | `packages/agent/src/session-configuration.ts#createSessionConfiguration.configure` | `packages/agent/src/session-configuration.ts:17-90` | keep | createController; generation CAS | generation/session rows: migrate |
| prod | `packages/agent/src/core/execution/run.ts#runAgent` | `packages/agent/src/core/execution/run.ts:48-90` | keep | session runner supplies authority | provider attempt rows: migrate |
| prod | `apps/openomni/src/delegation/process-entry.ts#serveProcessWorker` | `apps/openomni/src/delegation/process-entry.ts:78-105` | keep | createChildKernel/process transport | process records: migrate |
| prod | `apps/openomni/src/delegation/process-entry.ts#PROCESS_WORKER_ACK` | `apps/openomni/src/delegation/process-entry.ts:40-40` | keep | serveProcessWorker | process records: migrate |
| prod | `packages/agent/src/session-controller.ts#createController` | `packages/agent/src/session-controller.ts:21-27` | keep | target DEC/APPLY/DISPATCH composition; #970 | session/runtime rows: migrate |
| prod | `packages/agent/src/session-controller.ts#enqueue` | `packages/agent/src/session-controller.ts:205-243` | keep | current input writer/interrupt gate | session/inbox rows: migrate |
| prod | `packages/agent/src/session-controller.ts#reconcile` | `packages/agent/src/session-controller.ts:245-253` | keep | current drive trigger | runtime lease: migrate |
| prod | `packages/agent/src/session-controller.ts#awaitRetainedRunner` | `packages/agent/src/session-controller.ts:287-294` | keep | current retained-runner barrier | session/runtime rows: migrate |
| prod | `packages/agent/src/session-controller.ts#hibernate` | `packages/agent/src/session-controller.ts:296-305` | keep | current lease/handle release guard | session/runtime rows: migrate |
| prod | `packages/agent/src/session-admission.ts#createSessionAdmission.evaluatePromptPolicies` | `packages/agent/src/session-admission.ts:133-176` | move | target: evaluateSessionPrompt in packages/agent/src/session-lifecycle/admission.ts (owner #970) | session/action/inbox rows: migrate |
| prod | `packages/agent/src/session-admission.ts#createSessionAdmission.consumePolicyBlockedInbox` | `packages/agent/src/session-admission.ts:178-190` | move | target: consumeBlockedSessionInput in packages/agent/src/session-lifecycle/admission.ts (owner #970) | session/action/inbox rows: migrate |
| prod | `packages/agent/src/session-admission.ts#createSessionAdmission.commitSession` | `packages/agent/src/session-admission.ts:192-214` | move | target: commitSessionAdmission in packages/agent/src/session-lifecycle/admission.ts (owner #970) | session/action/inbox rows: migrate |
| prod | `packages/agent/src/session-admission.ts#createSessionAdmission.createExecutionLedger` | `packages/agent/src/session-admission.ts:216-245` | move | target: createSessionExecutionLedger in packages/agent/src/session-lifecycle/admission.ts (owner #970) | session/action rows: migrate |
| prod | `packages/agent/src/session-admission.ts#createSessionAdmission.consumeNoopInbox` | `packages/agent/src/session-admission.ts:346-363` | move | target: consumeSessionNoop in packages/agent/src/session-lifecycle/admission.ts (owner #970) | session/action/inbox rows: migrate |
| test | `packages/agent/test/session-resume-reopen.test.ts#reopened SQLite ${mode} chooses the correct IDs and generation with no stale-fence writes` | `packages/agent/test/session-resume-reopen.test.ts:20-180` | keep | #970 recovery conformance | fixture database: refuse removal |
| test | `packages/agent/test/captured-catalog.test.ts#recovery refuses a missing or changed captured definition instead of executing latest code` | `packages/agent/test/captured-catalog.test.ts:19-39` | keep | #970 captured-generation conformance | fixture catalog: refuse removal |
| test | `packages/agent/test/session-handle.test.ts#durable session handle :: consumes a running interrupt and seals interrupted rather than error` | `packages/agent/test/session-handle.test.ts:729-764` | keep | #970 interrupt conformance | session/action rows: migrate |
| test | `packages/agent/test/session-handle.test.ts#durable session handle :: keeps the durable lease held through an ignored abort so no other runtime can resume` | `packages/agent/test/session-handle.test.ts:800-860` | keep | #970 retained-lease conformance | session rows: migrate |
| test | `packages/agent/test/session-handle.test.ts#durable session handle :: heartbeat loss aborts the runner and the stale fence cannot seal its result` | `packages/agent/test/session-handle.test.ts:1074-1114` | keep | #970 stale-fence conformance | session/action rows: migrate |
| test | `packages/agent/test/session-handle.test.ts#session crash recovery and observation :: boot sweep resumes with the original pre-minted result id` | `packages/agent/test/session-handle.test.ts:1370-1390` | keep | #970 crash-open conformance | session/action rows: migrate |

### #971 production and dependent tests

#971 has no current evaluator or occurrence test registration. Its **distinct future** test receipt below owns ALD/ALA local products and evaluator acquisition, not #973's cross-region harness registration in section 6.7. This is construction, not a HEAD deletion.

| Kind | Exact source symbol | Source span | Disposition | Destination / sole consumer | Data disposition |
| --- | --- | --- | --- | --- | --- |
| prod | `packages/agent/src/session-stop-evidence.ts#sessionStopEvidence` | `packages/agent/src/session-stop-evidence.ts:7-71` | move | target: decideOccurrenceEvidence in packages/agent/src/alarm/evidence.ts (owner #971) | alarm/occurrence actions: migrate |
| prod | `packages/ledger/src/storage/sqlite-l0-adapter.ts#createAlarms` | `packages/ledger/src/storage/sqlite-l0-adapter.ts:571-630` | keep | target ALA wraps current primitive; #971 | alarm rows: migrate |
| prod | `packages/ledger/src/storage/sqlite-l0-adapter.ts#createAlarms.arm` | `packages/ledger/src/storage/sqlite-l0-adapter.ts:577-612` | keep | target applyAlarmTransition wraps current arm; #971 | alarm rows: migrate |
| prod | `packages/ledger/src/storage/sqlite-l0-adapter.ts#createAlarms.cancel` | `packages/ledger/src/storage/sqlite-l0-adapter.ts:614-623` | keep | target applyAlarmTransition adds owner/fence CAS; #971 | alarm rows: migrate |
| prod | `packages/ledger/src/storage/sqlite-l0-adapter.ts#createAlarms.due` | `packages/ledger/src/storage/sqlite-l0-adapter.ts:624-630` | keep | target dispatchAlarmTransition uses due reader; #971 | alarm rows: migrate |
| prod | `packages/protocol/src/ledger/l0.ts#Alarm.Status` | `packages/protocol/src/ledger/l0.ts:512-513` | keep | target AlarmTransition schema extends this vocabulary; #971 | alarm rows: migrate |
| prod | `packages/protocol/src/ledger/l0.ts#Alarm.Row` | `packages/protocol/src/ledger/l0.ts:515-527` | keep | target AlarmTransition schema; #971 | alarm rows: migrate |
| prod | `packages/protocol/src/ledger/l0.ts#Alarm.Arm` | `packages/protocol/src/ledger/l0.ts:529-530` | keep | target AlarmTransition schema; #971 | alarm rows: migrate |
| target-test | `packages/agent/test/alarm-transition.test.ts#alarm v1 evaluator acquisition and recovery refusal are atomic` | future registration; no HEAD span | construct | #971 exclusively; real ALA transaction with ALD precedence, one-shot lease, live-source cancellation and resume eligibility | alarm/action fixture rows and immutable prefix: retain; production alarm rows: migrate |

### #972 production and dependent tests

#972 owns the inspectable projections and retained physical history. It owns the shared read symbols below once; other children consume those reads without a second ownership row.

| Kind | Exact source symbol | Source span | Disposition | Destination / sole consumer | Data disposition |
| --- | --- | --- | --- | --- | --- |
| prod | `packages/agent/src/session-history.ts#sessionHistory` | `packages/agent/src/session-history.ts:6-142` | move | target foldSessionHistory in packages/agent/src/session-lifecycle/history.ts (owner #972) | message/part bytes: refuse; action rows: migrate |
| prod | `packages/ledger/src/session/kernel.ts#pendingInbox` | `packages/ledger/src/session/kernel.ts:96-98` | keep | createController/DEC read-only intake | inbox rows: migrate |
| prod | `packages/ledger/src/session/kernel.ts#inboxRows` | `packages/ledger/src/session/kernel.ts:100-102` | keep | history inspection | inbox rows: migrate |
| prod | `packages/ledger/src/session/kernel.ts#delivery` | `packages/ledger/src/session/kernel.ts:238-242` | keep | foldSessionHistory | inbox/action rows: migrate |
| prod | `packages/channels/src/router/wait/correlation.ts#findWaitCandidates` | `packages/channels/src/router/wait/correlation.ts:43-58` | keep | physical channel correlation reader; #972 | message/correlation bytes: refuse |
| prod | `packages/channels/src/router/routing-resolution.ts#recordRouteDecided` | `packages/channels/src/router/routing-resolution.ts:322-385` | keep | route fact/replay reader | route facts: migrate |
| prod | `packages/channels/src/router/routing-execution.ts#recordRouteNotDelivered` | `packages/channels/src/router/routing-execution.ts:17-47` | keep | correction fact writer | route facts: migrate |
| prod | `packages/protocol/src/json.ts#canonicalDigest` | `packages/protocol/src/json.ts:145-151` | keep | shared input/history identity utility | history bytes: refuse |
| prod | `packages/protocol/src/ledger/l0.ts#LedgerAction.Kind` | `packages/protocol/src/ledger/l0.ts:18-34` | keep | #972 action projection; guarded forward schema update | action rows: migrate |
| test | `packages/agent/test/session-history.test.ts#positional settlements survive ${terminal ?? "open crash"} history and do not cross turns` | `packages/agent/test/session-history.test.ts:12-140` | keep | #972 history projection conformance | message/part bytes: refuse |
| test | `packages/ledger/test/session/kernel.test.ts#session kernel folds :: reads authoritative open and terminal tails from committed session actions` | `packages/ledger/test/session/kernel.test.ts:405-461` | keep | #972 canonical tail conformance | action rows: migrate |
| test | `packages/channels/test/router/wait/lifecycle.test.ts#WaitService :: open records exactly one durable Wait for an awaited delivery` | `packages/channels/test/router/wait/lifecycle.test.ts:14-31` | keep | #972 retained physical correlation conformance | wait rows/facts: migrate |
| test | `packages/channels/test/router/wait/lifecycle.test.ts#WaitService :: sweepExpired expires a wait at exactly its deadline (inclusive boundary)` | `packages/channels/test/router/wait/lifecycle.test.ts:141-154` | keep | #972 deadline boundary conformance | wait rows/facts: migrate |
| test | `packages/channels/test/router/routing-resolution.test.ts#GatewayRouter durable routing resolution :: equivalent accepted redelivery re-delivers with exactly one route fact` | `packages/channels/test/router/routing-resolution.test.ts:132-142` | keep | #972 route replay conformance | route facts: migrate |
| test | `packages/channels/test/router/routing-resolution.test.ts#GatewayRouter durable routing resolution :: divergent redelivery refuses without action, fact, or projection` | `packages/channels/test/router/routing-resolution.test.ts:176-194` | keep | #972 divergent replay conformance | route facts: migrate |

### #973 production and dependent tests

#973 moves no current production writer and deletes no current fixture. Its only current dependent real-surface registration remains a keep; the six target registrations in section 6.7 are future test symbols owned by #973, not HEAD citations.

| Kind | Exact source symbol | Source span | Disposition | Destination / sole consumer | Data disposition |
| --- | --- | --- | --- | --- | --- |
| prod | `no HEAD production symbol` | `no HEAD path` | keep | target: runLifecycleTrace in packages/agent/test/session-lifecycle-conformance.test.ts (owner #973) | none |
| test | `apps/openomni/test/session-loop-e2e.test.ts#real app SSE compaction commits reversible evidence through the session executor` | `apps/openomni/test/session-loop-e2e.test.ts:44-100` | keep | #973 real-surface evidence | message/part/action bytes: refuse |

### Receipt semantics

`move` removes the old writer at the same cutover as its exact target; `delete` has no consumer; `keep` preserves the current symbol at its source path. A parent row never includes a separately listed child. Every test symbol above is assigned to one sole child; in particular, positional history belongs to #972 and SQLite recovery belongs to #970, not both. `migrate` preserves identity and immutable evidence through a guarded forward migration; `refuse` retains bytes and rejects removal; `drop` is unused because no export/backup decision exists. The exact target test registrations named in section 6.7 are not silently counted as current symbols.
### Durable tables and immutable migration evidence

Each table has one data owner below, independent of which code reads it. These are data dispositions, not permission to edit the cited historical DDL. Existing action/policy CHECK constraints must be extended by a guarded **forward** migration when their target vocabulary lands.

| Child | Durable symbol | Declaration evidence | Disposition | Destination / retained bytes |
| --- | --- | --- | --- | --- |
| #970 | table `session` | `packages/ledger/migration/0001_initial/migration.sql:1-6` | migrate | same table, ids/parents/roles/fences/generations preserved |
| #969 | table `action` | `packages/ledger/migration/0031_l0_ledger_base/migration.sql:25-43` | migrate | same append-only history; target CHECK extension preserves every row |
| #969 | table `inbox` | `packages/ledger/migration/0031_l0_ledger_base/migration.sql:46-61` | migrate | same queue, current consumed-row format; id/ordinal/content retained |
| #971 | table `alarm` | `packages/ledger/migration/0031_l0_ledger_base/migration.sql:64-74` | migrate | same coordination table plus epoch/evaluator/dedupe fields; no extra monitor table |
| #969 | table `wait` | `packages/ledger/migration/0012_wait/migration.sql:1-20` | migrate | original request-action projection + retained physical correlation; unresolvable open row refuses whole cutover |
| #969 | table `delegation` | `packages/ledger/migration/0023_delegation/migration.sql:3-11` | migrate | source request/terminal/outbound action projection; original records retained as evidence |
| #969 | table `approval` | `packages/ledger/migration/0028_approval/migration.sql:1-9` | migrate | original request suspension with subject/domain preconditions; unsupported unresolved binding refuses |
| #972 | table `message` | `packages/ledger/migration/0001_initial/migration.sql:8-15` | refuse | keep physical bytes; projection work is not removal approval |
| #972 | table `part` | `packages/ledger/migration/0001_initial/migration.sql:20-26` | refuse | keep physical bytes and message links |
| #969 | table `policy` | `packages/ledger/migration/0031_l0_ledger_base/migration.sql:78-92` | migrate | immutable generations/rows preserved; new action-kind constraints forward only |

All already-numbered files under `packages/ledger/migration` are immutable historical inputs, assigned to #972 for shared archival retention (`refuse` edits/removal); table migration owners above write new migrations only. The absent historical domains documented in SLOP are not reconstructed to make these projections easier.

### Mechanical receipt and citation checks

Run this from the worktree. It verifies exclusivity **and** cited declarations, not merely that a line number is smaller than a file's length. It reads the receipt table and current source, including a completeness check for the fully inventoried approval exports and three suites. Future target-test keys are checked for exclusivity separately, never passed off as HEAD declarations.

```python
import pathlib, re, subprocess
root = pathlib.Path.cwd()
text = (root / "docs/session-lifecycle-contract.md").read_text()
owner, seen, count, targets = None, {}, 0, 0
for line in text.splitlines():
    heading = re.match(r"### #(969|970|971|972|973) production", line)
    if heading:
        owner = heading[1]
    future = re.match(r"\| target-test \| `([^`]+)` \| future registration; no HEAD span", line)
    if future:
        assert future[1] not in seen, ("duplicate target", future[1])
        seen[future[1]] = owner
        targets += 1
    row = re.match(r"\| (prod|test) \| `([^`#]+)#([^`]+)` \| `([^`]+):(\d+)-(\d+)`", line)
    if not row:
        continue
    kind, file, symbol, cited, first, last = row.groups()
    symbol = symbol.replace("\\|", "|")
    key = file + "#" + symbol
    assert key not in seen, ("duplicate", key, seen.get(key), owner)
    seen[key] = owner
    assert file == cited, (file, cited, symbol)
    source = (root / file).read_text().splitlines()
    assert 1 <= int(first) <= int(last) <= len(source), (file, first, last)
    leaf = symbol.rsplit(".", 1)[-1]
    needle = symbol.rsplit(" :: ", 1)[-1] if kind == "test" else leaf
    hits = subprocess.run(["rg", "-n", "-F", "--", needle, file],
                          check=True, capture_output=True, text=True).stdout
    assert any(int(first) <= int(hit.split(":", 1)[0]) <= int(last)
               for hit in hits.splitlines()), ("missing symbol", key, first, last)
    if kind == "prod":
        span = "\n".join(source[int(first)-1:int(last)])
        declared = (r"\b(?:function|class|interface|type|namespace|enum|const|let)\s+"
                    + re.escape(leaf) + r"\b|\b" + re.escape(leaf) + r"\s*\??\s*\(")
        # VariableDeclaration ranges begin after 'const'; its source line retains it.
        assert re.search(declared, span), ("not a declaration", key, first, last)
    count += 1
for file, first, last in re.findall(r"((?:packages|apps)/[\w/.-]+):(\d+)-(\d+)", text):
    lines = (root / file).read_text().splitlines()
    assert 1 <= int(first) <= int(last) <= len(lines), (file, first, last)
# Completeness for the replaced approval entries and the three fully inventoried suites.
for file, prefix in [("packages/ledger/src/approval/index.ts", "ApprovalStore."),
                     ("packages/protocol/src/approval/fold.ts", "")]:
    for name in re.findall(r"export function (\w+)\(", (root / file).read_text()):
        assert file + "#" + prefix + name in seen, ("omitted authority", file, name)
for file in ["apps/openomni/test/delegation.test.ts",
             "packages/ledger/test/approval/store.test.ts",
             "packages/protocol/test/approval-fold.test.ts"]:
    registrations = re.findall(r'^\s*test\("([^"\n]+)"', (root / file).read_text(), re.M)
    for name in registrations:
        keys = [key for key in seen if key.startswith(file + "#")
                and key.rsplit(" :: ", 1)[-1] == name]
        assert len(keys) == 1, ("omitted/duplicate registration", file, name, keys)
assert count > 0 and targets > 0
print(f"{count} HEAD source/test symbols; {targets} separate target-test receipt; duplicate owners: 0")
```

The checker treats a qualified source symbol as one key even if multiple children use it. A deliberate duplicate row under another child must fail; an unknown symbol with an in-range line must fail. Neither formatting/lint nor this lexical checker alone proves transition semantics; section 6's behavioral vectors are the separately owned conformance oracle.
