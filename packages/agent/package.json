{
  "name": "@openomni/agent",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "check-types": "tsc --noEmit && tsc --noEmit -p tsconfig.bench.json && tsc --noEmit -p tsconfig.test.json",
    "lint": "bunx biome check ./src",
    "test": "bun test",
    "bench": "bun run bench/index.ts",
    "test:ci": "bun test --timeout 15000 --coverage --coverage-reporter=lcov --coverage-dir=coverage"
  },
  "dependencies": {
    "@openomni/llm": "workspace:*",
    "@openomni/placement": "workspace:*",
    "@openomni/policy": "workspace:*",
    "@openomni/protocol": "workspace:*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@openomni/telemetry": "workspace:*",
    "@types/bun": "latest"
  }
}
