{
  "name": "@openomni/channels",
  "version": "0.0.1",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "check-types": "tsc --noEmit",
    "lint": "bunx biome check ./src",
    "test": "bun test"
  },
  "dependencies": {
    "@openomni/ledger": "workspace:*",
    "@openomni/policy": "workspace:*",
    "@openomni/protocol": "workspace:*"
  },
  "devDependencies": {
    "@openomni/telemetry": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
