/**
 * Messaging audit descriptors (#215): defined in protocol (descriptor + zod
 * schema are contract vocabulary; the router band defines no schemas of its
 * own) and published here through the injected sink. Names byte-frozen.
 */
export { MessagingEvents as Events } from "@openomni/protocol";
