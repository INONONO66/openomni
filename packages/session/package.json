{
  "name": "@openomni/session",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit && tsc --noEmit -p tsconfig.contract-test.json",
    "lint": "bunx biome check ./src",
    "test": "bun test",
    "bench": "bun run bench/index.ts"
  },
  "dependencies": {
    "@openomni/protocol": "workspace:*",

    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
